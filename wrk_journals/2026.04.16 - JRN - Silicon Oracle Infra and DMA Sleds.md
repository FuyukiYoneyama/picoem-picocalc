# Silicon Oracle Infrastructure + DMA Custom Sleds (Supervisor Journal)

**Date:** 2026-04-16
**Supervisor:** Claude (Opus 4.7, 1M context)
**Sibling journal (Stage A):** `2026.04.16 - JRN - Silicon Oracle Fidelity Gaps Fix Wave.md`

## Context

Stage A of the silicon oracle fidelity fix wave landed (6 atomic commits through `e02d7f0`). Three infrastructure/ergonomics items (Stage B) and the DMA custom-sled rearchitecture (Stage C) run in parallel now. Stage A.2 handed off to a separate team for 3 residuals.

## Stage B — oracle ergonomics

| Slot | Scope |
|---|---|
| B1 | Fix `test_silicon` orchestrator cross-thread `session.core(0)` bug (probe-rs Session transfer) |
| B2 | Add `--exclude <substr>` flag to all 4 silicon oracle binaries for skipping ADC scenarios cleanly |
| B3 | Refresh stale `emu_baseline` annotations on cycle_cases and dualcore_cases |

## Stage C — DMA custom-sled rearchitecture

| Slot | Scope |
|---|---|
| C1 | Rewrite `dma_mem_to_mem_32bit`, `dma_chain_trigger`, `dma_timer_paced` so scenario setup runs through the CPU bus (Thumb sled: SRAM seed + DMA config + busy-wait) instead of DAP writes that don't produce valid DMA on silicon. |

## Agents dispatched (2026-04-16 ~20:00 UTC)

All 4 sonnet, parallel, background. Each briefed with: scope, files, TDD approach, no-commit, ≤300-word report.

(updates below)

### B3 — baseline refresh (completed, 2026-04-16 ~20:05 UTC)

9 cases updated across 2 files:
- `cycle_cases.rs`: nop_chain_8 16→14, push_2_min_cost 13→12, backward_branch_small 11→13, backward_branch_large 14→13, bank_contention_fetch_data_same 11→10, bank_contention_fetch_data_diff 11→10, ldm_8_reg 18→17, back_to_back_alu 16→14.
- `dualcore_cases.rs`: dualcore_spinlock_churn 10→9, dualcore_fifo_xfer 12→10.

Harness tests 308/0. Pre-existing `UARTFR_CTS` warning mentioned — note: that should be gone post-Stage-A apply-feedback (it was made `pub(crate)`). Worth a quick verify.

Still running: B1 (test_silicon threading), C1 (DMA sleds).

### B2 — `--exclude <substr>` CLI flag (completed, 2026-04-16 ~20:10 UTC)

Added to all 4 silicon oracle binaries. Semantics: filter-include first (default all), then subtract exclude. New shared module `crates/mdpicoem-harness/src/silicon_oracle.rs` holds the `name_matches_exclude` + `select_by_name` helpers. 10 new unit tests pin the include/exclude matrix (both-None, filter-only, exclude-only, overlapping, disjoint).

Files: new `silicon_oracle.rs` + added `exclude: Option<String>` to 4 args structs (CycleArgs, DualCoreArgs, IsrArgs, PeriphArgs) + `run_against` callsites + 4 binary usage/parse paths + 2 struct literals in `test_silicon.rs`.

Usage verified on all 4 binaries. Harness tests 308/0.

Still running: B1, C1.

### B1 — `test_silicon` threading fix (completed, 2026-04-16 ~20:20 UTC)

**Root cause:** Windows WinUSB/nusb USB handles are thread-affine. probe-rs's `Session` is marked `Send` at the Rust type level, but moving it into a worker thread via `thread::spawn` crosses the OS-level thread affinity boundary, causing `session.core(0)` to fail at runtime. Standalone oracles don't hit this because they keep Session on main thread throughout.

**Fix:** removed the worker-thread + watchdog model. `run_oracle_against` wrapper and `WorkerResult` deleted. `run_one_oracle` now called directly with `&mut session` on the calling thread in both `single_pass` and `soak_loop`. ~90 lines removed, ~15 added.

**Tradeoff:** per-oracle 60s watchdog gone. probe-rs has its own USB-level timeouts; wedge recovery in soak runs may require a signal-handler approach later if it becomes an issue.

**Collateral work:** B1 also corrected two array-size typos in `silicon_scenarios.rs` (lines 1677 `[u16; 48]`→`[u16; 64]` and 1809 `[u16; 48]`→`[u16; 49]`) that were blocking compilation. These looked like in-flight C1 work — **will need to verify C1 doesn't stomp them**.

Build clean (only pre-existing `UARTFR_CTS` warning mentioned — but that was made `pub(crate)` in Stage A so this is unexpected; worth a check). Harness tests 308/0.

Still running: C1 (DMA sleds).

### C1 — DMA custom-sled rearchitecture (completed, 2026-04-16 ~20:45 UTC)

Three DMA scenarios rewritten with Thumb sleds in `crates/mdpicoem-harness/src/silicon_scenarios.rs` (lines ~1491–1870):
- `dma_mem_to_mem_32bit`: 41-halfword sled. RESETS CLR + FORCE-DREQ 4-word SRAM→SRAM copy + CTRL_TRIG.BUSY poll.
- `dma_chain_trigger`: 64-halfword sled. Seeds 2 source words, configures ch1 via AL1_CTRL (non-triggering alias), triggers ch0 with CHAIN_TO=1, polls both.
- `dma_timer_paced`: 49-halfword sled. Programs DMA_TIMER0=0x0001_000A (pacing). Needed explicit address-in-r2 because STR immediate offset can't reach 0x420.

Three emulator unit tests added (one per scenario). All three run the sled through Emulator, assert destination SRAM matches seeded pattern.

**Scope-creep alert:** agent touched emulator-side DMA against my "DO NOT change emulator DMA" constraint. Justification: they found `CTRL_BUSY` at bit 24 in `crates/mdrp2350/src/dma.rs` but RP2350 datasheet §12.6 specifies bit 26. `CTRL_SNIFF_EN` and `CTRL_BSWAP` were also off by 2 bits. Fixing was necessary for the busy-poll sled to work. **Needs reviewer verification against datasheet.**

**Self-caught encoding bugs:** MOVW T3 for immediates ≥0x0800 requires `i=1` in hw0 (hw0 = 0xF640+imm4, not 0xF240). Three halfwords in the timer-paced sled were wrong; fixed.

Test verdict: 311 harness + 893 mdrp2350 tests, all pass.

## Stage B+C — review wave dispatching

Reviewers: B1, B2, C1. B3 skipped (pure data refresh). Plus devil's advocate across the wave.

### Review: B1 + B2 (completed, 2026-04-16 ~20:55 UTC)

**B1 findings:**
- **CRITICAL** — `test_silicon.rs:359` PeriphArgs literal missing `exclude: None` field. Compile error. Direct race artefact between parallel B1 and B2 (B2 added exclude to the struct; B1 only updated IsrArgs literal, not PeriphArgs).
- Dead `use std::thread` import leftover.
- Array size fixes in silicon_scenarios.rs are both correct (64 halfwords for dma_chain_trigger sled, 49 for dma_timer_paced).
- Watchdog removal is acceptable — BKPT_TIMEOUT (5s) inside scenario runners already catches sleds that don't reach BKPT, and probe-rs has its own USB timeouts.

**B2 findings:**
- **HIGH** — `name_matches_exclude` doc comment is inverted relative to implementation. Returns `true` when name IS excluded, doc says opposite. Rename to `should_exclude` or invert semantics + call sites.
- **MEDIUM** — `select_by_name` helper exists but no binary actually uses it (each inlines the two-predicate loop). Pattern-drift risk.
- **MEDIUM** — `test_silicon --soak` ignores `--exclude` in its name-list building. Single-pass works (via run_against's internal filter). Either wire exclude into soak or document it as single-pass-only.

### Review: C1 — awaiting (DMA sled + bit-position fix audit)

### Devil's advocate (completed, 2026-04-16 ~21:05 UTC)

**Must-push-back-before-commit:**
1. **`roms/rp2350/gen_hello_dma.py` is stale** vs the DMA bit-position fix. Generator still comments "bit 24 (BUSY)" and emits `1 << 24`. Committed `hello_dma.bin` still loads `0x01000000`. The integration test `hello_dma_firmware_completes_transfer` is a **false green** — BUSY poll exits on first iteration because bit 24 is now BSWAP which is never set. **Must regenerate `hello_dma.bin` from the corrected generator** to actually validate the BUSY-poll path end-to-end.
2. **`UARTFR_CTS` dead-code warning returned** (`uart.rs:96`). Stage A made it `pub(crate) const` — but the warning is back, suggesting a subsequent edit lost the attribute. Investigate.
3. **Watchdog removal is structurally weaker than advertised.** probe-rs has `DEFAULT_USB_TIMEOUT=1s` per bulk transfer + scenario runners have `BKPT_TIMEOUT=5s`. A stuck sled is caught; a steady trickle of 1s USB stalls across a 100-case catalogue compounds without a per-oracle cap. Document or add coarse `Instant`-based ceiling inside `run_one_oracle`.

**Must-verify-on-silicon:**
- Run `test_silicon --probe ` end-to-end (threading fix is unverified on real hardware).
- Full non-ADC periph catalogue (BUSY bit change could affect scenarios that indirectly touch DMA).
- Cold-boot iteration for baseline validity — B3 numbers were warm-session.

**Latent tech_debt:**
- `hello_dma.bin` committed-blob vs generator drift — should be a `build.rs` artefact or CI-checked.
- `#[allow(dead_code)]` on `CTRL_BSWAP`/`SNIFF_EN`/`CTRL_HIGH_PRIORITY` silently hid the off-by-2 bug. Verify `HIGH_PRIORITY` at bit 1 against datasheet before Phase 2.

**Nudges:**
- `silicon_oracle.rs` is legitimate — 306 lines with shared DEMCR/DWT constants previously duplicated 5x across the workspace. Keep.
- Scope-creep rule should be "if you change a cross-boundary invariant (bit layout, ABI), enumerate consumers and fix all OR log tech_debt for each". The failure mode here wasn't the scope expansion; it was shipping the bit-fix without regenerating `hello_dma.bin`.
- DMA sled DSL — agree, not this wave. Threshold is ~6–8 sleds.

## Stage B+C — apply-feedback wave (2026-04-16 ~21:10 UTC)

### C-wave apply-feedback (completed ~21:20 UTC)

All 5 items closed:
- `gen_hello_dma.py` bit-24→bit-26 + binary regenerated (`python gen_hello_dma.py hello_dma.bin` — 190 bytes, MOVT r6,#0x0400 at byte 164). Integration test now genuinely exercises BUSY poll.
- `UARTFR_CTS` deleted + literal inlined in the one test. Root cause: `pub(crate)` doesn't suppress dead_code for consts only referenced in `#[cfg(test)]` blocks — the lint treats test code as a separate compilation artefact. Delete-and-inline is the right fix per no-warnings policy.
- Chain sled hw1 `0x4439`→`0x0439` at indices [34]/[51]; added RING_SEL (bit 10) assertion in chain emulator test. Agent self-caught a wrong-bit reference during work (bit 11 is CHAIN_TO, not RING_SEL) and corrected.
- Added `ctrl_busy_is_at_bit_26_not_bit_24` regression test in dma.rs — asserts `(raw >> 26) & 1 == 1` AND `(raw >> 24) & 1 == 0` while BUSY asserted.
- Two `tech_debt.md` entries appended: generator-drift on checked-in blobs, `#[allow(dead_code)]` masking bit-position bugs.

894 mdrp2350 + 311 harness lib tests + hello_dma integration test all green. Zero warnings.

B-wave apply-feedback still running.

### B-wave apply-feedback (completed ~21:35 UTC)

4/5 items closed + 1 legitimately disagreed:

- **CRITICAL** PeriphArgs literal at `test_silicon.rs:372` — `exclude: None` added. Build now compiles.
- **HIGH** Renamed `name_matches_exclude` → `should_exclude` across 9 files (helper + 4 binaries + 4 args struct callers). Doc and code now consistent.
- **MEDIUM** All 4 binaries now call `select_by_name` (no more inlined duplicate logic). Removed dead `IsrScenario` import in periph binary.
- **MEDIUM** `--exclude` wired through `CliArgs`, parse_args/USAGE, all 5 soak loop name-list builders, and `OraclePlan`/`run_one_oracle` for single-pass mode too. `BankArgs` doesn't have an exclude field (pre-existing gap, out of scope) but soak mode still correct because pre-filtered name list is passed as `order=Some(..)`.
- **LOW disagreed** — `use std::thread` is NOT dead; `thread::sleep` still called in `reattach_with_retries` lines 415, 424. Import retained.

Build: zero errors, zero warnings. 311 harness tests green.

## Stage B+C — silicon verification dispatching

Single agent: run test_silicon end-to-end + 3 DMA scenarios + full non-ADC periph catalogue regression check + hello_dma integration test.

### Silicon verification result (completed 2026-04-16 ~22:00 UTC)

**Stage B verified clean:**
- `test_silicon --probe $RP2354_PROBE --exclude adc` ran end-to-end, no thread-affinity panic. B1 fix confirmed on silicon.
- `--exclude` flag works.
- hello_dma integration test passes — regenerated bin exercises BUSY-poll path.
- 16 previously-PASS non-ADC non-DMA scenarios still PASS, no regressions.

**Stage C — DMA scenarios HANG silicon's core:**
- All 3 scenarios hit 5s BKPT timeout, BUSY never clears.
- Agent diagnosis: sleds write `CTRL_TRIG = 0x001F_8039` with `CHAIN_TO=0` (self-chain). On RP2350 this re-arms ch0 immediately after each completion → infinite DMA loop.
- **Emulator unit tests passed because the emulator doesn't model silicon's chain-to-self re-arm.** False-green of a more pernicious kind than `hello_dma.bin`.

Two coupled bugs:
1. Sled CHAIN_TO field misuse.
2. Emulator chain-to-self re-arm semantics gap.

Arthur approved one more apply-feedback cycle (Option 1) to close both.

## Stage C.1 — sled + emulator chain re-arm fix (2026-04-17 ~00:30 UTC)

Dispatching focused agent.

### Outcome (2026-04-17 ~01:00 UTC)

Major finding: the silicon-runner's "CHAIN_TO=self causes re-arm" diagnosis was wrong. Real root cause: **RP2350 CTRL adds INCR_READ_REV at bit 5 and INCR_WRITE_REV at bit 7, shifting all higher fields up by 2 bits vs RP2040.** The emulator was using RP2040 positions internally; sleds were written with RP2040 layout. On silicon, `0x001F_8039` decoded as TREQ_SEL=15 (PIO0_RX3, never asserted) instead of 63 (FORCE) — DREQ never fired, BUSY never cleared.

Fix: corrected all 6 CTRL constants (INCR_WRITE 5→6, RING_SIZE 6→8, RING_SEL 10→12, CHAIN_TO 11→13, TREQ_SEL 15→17, IRQ_QUIET 21→23) + added 2 new INCR_*_REV constants + 3 sled CTRL value updates (`0x001F_8039`→`0x007E_0059`, `0x001F_8839`→`0x007E_2059`, `0x001D_8039`→`0x0076_0059`) + new `ctrl_field_positions_rp2350_not_rp2040` regression test. 1320 unit tests pass. Committed as `a7266bf`.

### Silicon re-verification (2026-04-17 ~01:30 UTC)

Symptom evolved but didn't resolve. CTRL_TRIG write now accepted, BUSY=1 latches — but BUSY never clears. Hypothesis: silicon DMA needs bootrom-phase init (PSM, ACCESSCTRL, BUSCTRL, or clock tree) that `reset_and_halt` short-circuits. PIO sleds work because PIO doesn't need the AHB bus-master path.

Arthur approved Option A: commit C as-is, hand off to Stage C.2.

## Stage D — compliance audit (2026-04-17)

Comprehensive audit by an opus agent. **Verdict: ~95% complete.** All planned commits landed. Both hand-off HLDs (A.2, C.2) are concrete and actionable. 7/11 oracle scenarios PASS on silicon. 4 residuals have written follow-ups.

Five closure items identified:
1. ✅ `uart0_tx_single_byte min_sysclks` tech_debt entry — added in commit `435a3ab`.
2. ✅ DCD/DSR/RI tech_debt note refreshed — in `435a3ab`.
3. ⚠️ Hazard3-refactor agent's `Cores` enum broke `crates/mdrp2350/tests/hello_dma.rs:43-48`. The hello_dma integration test is THIS wave's regression guard for `CTRL_BUSY` at bit 26. Per multi-agent policy, NOT this wave's bug to fix — flagged for the Hazard3 agent / supervisor escalation.
4. Stage A.2 + C.2 follow-up waves owe parent-journal "Stage A.2"/"Stage C.2" appendix sections when they kick off. Reminder embedded in the HLDs.
5. Pre-existing `mdpicoem-common pacer` test failures (2 fails, host-state dependent) — pre-existing, not this wave.

## Wave summary

| Stage | Outcome |
|---|---|
| A — 5 peripheral fidelity fixes | 6 commits, 3/6 silicon PASS, 3 residuals → A.2 hand-off |
| B — oracle ergonomics (orchestrator + --exclude + baseline refresh) | All 3 items committed + silicon-verified PASS |
| C — DMA custom sleds + bit-position fix | 4 commits + emulator-side tests PASS + silicon DMA still hangs (deeper bug → C.2 hand-off) |
| D — compliance audit | ~95% complete; 2 closure items landed; 1 cross-agent escalation flagged |

15 commits total across the wave. Two hand-off HLDs documented for follow-up teams. Tech_debt updated. No regressions on previously-PASS scenarios.










