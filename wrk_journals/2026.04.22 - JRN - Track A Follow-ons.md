# Track A Follow-ons — 2026-04-22

**Lead**: Claude (Opus 4.7) as team lead.
**Background**: Track A from the 2026-04-21 Oracle Fix Team wave landed the probe-rs `(V2, DPv1)` `unreachable!()` patch (`d31f90b`...`9dc122b`) with partial success — the panic is gone but `silicon_periph_diff_rp2350` and `probe_verify_rp2350` now fail with `Arm(WrongApType)` downstream of the sentinel WARN. `probe_diff_rp2040` also remains broken with a separate "ARM specific error" (V1-only AP path, pre-existing). Upstream PR draft for probe-rs#3872 is ready for review/submission.

## Scope (3 items, 3 tracks)

| Track | Issue | Est. effort | Agent |
|---|---|---|---|
| **A.2** | Downstream `Arm(WrongApType)` on silicon_periph_diff_rp2350 / probe_verify_rp2350 after DPv3 cache upgrade fires. Root cause: RP2354 DP genuinely DPv1 (multidrop)? Or probe-rs AP discovery needs second workaround? | 2–6h | TBD |
| **A.1** | `probe_diff_rp2040` "ARM specific error". V1-only AP path, separate root cause from Track A. | 1–4h | TBD |
| **PR**  | Review + refine `wrk_docs/2026.04.21 - DRAFT - probe-rs PR body.md` against final patch on main. Output: submission-ready PR body for Arthur to post against probe-rs/probe-rs#3872. | 30 min | TBD |

## Process

1. Plan agents write HLDs in `wrk_docs/` (parallel dispatch).
2. Devils-advocate agent reviews each HLD; challenges assumptions, scope, risks.
3. Lead reviews each HLD; signs off.
4. Implementation agents work in isolated worktrees. Use `/code-reviewer` skill, then `/code-review-apply-feedback`. TDD where applicable.
5. Lead consolidates, verifies on hardware, commits after each stage.
6. Final verification agent compares implementation against each signed-off HLD. Iterates until 100% complete.
7. Journal updated at each phase transition.

## Timeline

### 2026-04-22 Setup

- Journal initialised (this file).
- Plan agents dispatching in parallel for Tracks A.2, A.1, and PR review.
- Main at `cdb408e` (post-wave-1 close-out).

### 2026-04-22 Plan phase returns

- All three Plan agents returned within ~10 minutes of one another.
- **Track A.2 HLD** (`2026.04.22 - HLD - Track A.2 Downstream WrongApType.md`): pinpoints the failure at `memory_ap/mod.rs:210` (IDR low-nibble = 0 → `JtagComAp` → `WrongApType`). Four hypotheses (H1 hybrid DP, H2 transient DPIDR, H3 cache-path, H4 classifier). HLD is honest that it cannot pick H1 vs H2 from inspection alone and requires a Phase 0 `RUST_LOG=probe_rs=trace` capture to disambiguate. Option A (fork patch tailored to hypothesis) recommended. Effort 3–6h.
- **Track A.1 HLD** (`2026.04.22 - HLD - Track A.1 RP2040 Attach Fix.md`): crucial insight — `eprintln!("fatal: {e}")` drops the `source()` chain, so the bare "ARM specific error occurred" is the *outer* `Error::Arm(_)` Display, not the underlying `ArmError` variant. Six candidates (H1 multidrop handshake most likely per probe-rs#2696). Phase 1 gates between Option A (`attach_under_reset`) and Option B (fork patch) via CLI triage matrix. Effort 3h Option A, 8h Option B.
- **PR refinement** (`2026.04.22 - PR - probe-rs V2-on-DPv1 recovery.md`): found stale commit hash `ffb0126` (real: `d31f90b`), downstream test-binary names that mean nothing upstream, and the "full scenario catalogue" claim that is actually untrue (these still fail downstream per the journal). Refined body ready for submission.
- Both Plan agents are `subagent_type=Plan` which is read-only; they delivered HLDs as text in their responses, lead wrote them to disk.
- Devils-advocate agent dispatched against all three documents. Sign-off pending its return.

### 2026-04-22 Devils-advocate returned — 1 REJECT, 2 EDITS

Devils-advocate (`a6e3748c23bf67ce7`) delivered a 23-challenge critique at `wrk_docs/2026.04.22 - DEVILS-ADVOCATE - Track A Follow-ons.md`. Verdict:

- **Track A.1: REJECT AND REWORK.** Load-bearing upstream citations are stale. `probe-rs#2696` is CLOSED (2025-05-19), root cause attributed to old Raspberry Pi debugprobe firmware. `#2950` CLOSED by PR #3252. The entire Option A vs Option B design space assumes an unfixed upstream bug; actual upstream fix is "flash the probe to firmware 2.x". Before any code work, check probe `bcdDevice` — if <2.0, the HLD is moot.
- **Track A.2: EDITS REQUIRED.** H3 pre-rejectable by inspection (cache flush at line 502 always fires). H4 mis-framed (unknown-TYPE goes to `RegisterParseError`, not `WrongApType`). Already-observed `probe_diff_rp2350 --fuzz 4000` PASS is evidence *against* H1 (if DP really were DPv2, ISA path would break too) — the HLD doesn't use this. HLD asks sign-off on a four-way decision tree gated on data not yet collected — split into A.2a (trace+analysis) + A.2b (patch). Drop "defence-in-depth" framing.
- **PR body: EDITS REQUIRED.** ADIv6 §B2 citation is unverified and load-bearing — replace with probe-rs-internal justification at `communication_interface.rs:285-289`. Changelog goes in the diff not the body. `arm:` prefix not probe-rs convention. nRF9151 checkbox we can't deliver. Open PR #3578 at same call site — acknowledge it.

Lead decisions:
- A.1: dispatch rework agent that runs the Pi debugprobe firmware check *first* (Phase 0a), then either reports "flash the probe" or produces a corrected HLD with D10-D16 addressed. The previous A.1 HLD is the input, not the baseline.
- A.2: dispatch iteration agent to address D1-D8. Revised HLD should explicitly split A.2a (trace capture, lead signs off) from A.2b (patch, signed off post-trace).
- PR: dispatch iteration agent to address D17-D23. All edits are mechanical/factual.
- Three iteration agents dispatched in parallel.

### 2026-04-22 Iteration agents returned — all three revisions in

- **A.2 v2 HLD**: clean split into A.2a (trace) + A.2b (patch). H3 deleted by inspection, H4 dropped, priors updated to H2=0.75/H1=0.15 using 4000/4000 evidence. Added Option A-prime (replace Track A arm in one edit, not two bolted together). Defence-in-depth framing gone. **Signed off for A.2a only.**
- **A.1 v2 HLD**: critical insight — the fork **already** enforces PR#3252's firmware denylist (`tools.rs:325-349`). A pre-2.2.0 probe would fail with `ProbeFirmwareOutdated`, not "ARM specific error" — so our probe must already be ≥2.2.0. New three-phase plan: 0a fingerprint, 0b diagnostic commit (standalone), 1 triage, 2 Option F/A/B (sub-shape decided by matrix). **Signed off for Phase 0a + 0b.**
- **PR v2 body**: tightened known-limitation paragraph; replaced unverifiable ADIv6 §B2 citation with probe-rs-internal citation at `communication_interface.rs:284-289`; changelog placement corrected; `arm:` prefix dropped; nRF9151 checkbox replaced with body-text request; PR#3578 acknowledged; explicit no-unit-test stance with rationale. **Signed off — ready for Arthur's submission.**
- Stripped the `(mdrp-patched workaround)` suffix from the warn-message code block shown in the upstream PR (internal-only phrasing).

### 2026-04-22 Commit Plan phase

- All HLDs + PR body + devils-advocate critique + journal committed to main as a single Plan-phase artefact before dispatching implementation.

### 2026-04-22 A.1 Phase 0a results

**Method used:** Method 1 (PowerShell `Get-PnpDevice`). Zero-install, Windows-native, returned the fingerprint in one invocation — no need to try Methods 2 or 3. The `third_party/probe-rs-0.31.0-mdrp-patched/target/release/probe-rs.exe` binary does not exist (fork CLI not built), so Method 2 would have required a ~10 min build for no extra value.

**Raw output (RP2040 probe, serial `<RP2040 probe serial>`):**

```
FriendlyName : USB Composite Device
InstanceId   : USB\VID_2E8A&PID_000C\<RP2040 probe serial>
HardwareIds  : USB\VID_2E8A&PID_000C&REV_0230 ; USB\VID_2E8A&PID_000C
```

All four `VID_2E8A&PID_000C` devices on this host (both probes' composite devices + their two CMSIS-DAP v2 interfaces + their two USB-CDC/COM interfaces) report `REV_0230`. Both RP2040 probe (`<RP2040 probe serial>`) and RP2354 probe (`<RP2354 probe serial>`) are on the same firmware rev.

**bcdDevice = `0x0230`** (firmware 2.3.0 per the denylist-hex convention where `0x0220` = 2.2.0).

**Decision per HLD §3 table:** `> 0x0220` row → "Firmware is recent. Remaining possibilities are C1/C2/C3/C4/C5. Proceed to **Phase 0b** (diagnostic fidelity)."

**Recommended next step:** Dispatch the Phase 0b agent (source-chain-walker diagnostic commit in `probe_diff_rp2040.rs:59–62`). No firmware action required. `0x0230` is actually ahead of the "2.22 firmware seems to work just fine" comment in PR#3252 (i.e. `0x0222`), so the firmware is genuinely modern and the `probe_diff_rp2040` failure cannot be explained by firmware vintage — it is a real C1/C2/C3/C5-class bug that Phase 0b's source-chain dump will pin down.

### 2026-04-22 A.1 Phase 0b landed

- Phase 0b implementation agent (`a9cb78762006d4a78`) returned. Commit `4baf4873` on worktree branch `worktree-agent-a9cb7876`, cherry-picked to main as `26a2b9a`. 3 files changed, 13 insertions, 2 deletions.
- `probe_diff_rp2040.rs` now walks the `source()` chain on fatal exit. Agent verified via a standalone 30-line reproducer that `std::error::Error::source(&*e)` iteration surfaces inner variants behind `#[source]`.
- Harness patch version bumped 0.35.3 → 0.35.4.
- Unit tests: 377/377 before and after (no regressions).
- Code-reviewer approved; 2 optional suggestions declined (cross-binary rollout out-of-scope per HLD §11; format bikeshed).
- The main clone had the same change uncommitted before cherry-pick — suspected source: the hardware triage agent that claimed "No files modified outside wrk_debug and the journal" in its summary despite evidence to the contrary (CLAUDE.md memory `feedback_verify_agent_summaries` applies). Discarded the uncommitted copy; cherry-picked the clean commit.
- A.1 Phase 1 triage now unblocked; holding until A.2b returns to avoid host-hardware contention.

### 2026-04-22 A.2b dispatched

- A.2b implementation agent (`aa72cdd6401e69c50`) in isolated worktree.
- Agent has the two mandatory gates (BASEPTR0 probe, probe_diff_rp2350 4000/4000 explanation) baked into its prompt.
- Expected shape: new `rp235x.rs` with sequence override; Track A arm either removed (Gate 2=a) or FIXME-tagged (Gate 2=b).

### 2026-04-22 A.2a results

**Procedure executed:** built `probe_verify_rp2350` in **debug profile** (release strips DEBUG/TRACE at compile time via the workspace-wide `release_max_level_info` feature on `tracing`; release output contained only INFO/WARN lines and was insufficient for H1/H2 discrimination). Debug build copied to `%TEMP%\probe_verify_rp2350_dbg.exe` and run 3× in PowerShell with `RUST_LOG=probe_rs=trace`. All three runs exited 1 (as expected — the fail path is what we're measuring). Raw logs: `wrk_debug/trace-probe-verify-rp2350.run{1,2,3}.{stdout,stderr}.log` (~66 KB stderr each). Extracted key lines: `wrk_debug/trace-probe-verify-rp2350.key-lines.log`.

**Hypothesis ruling: H1 confirmed (permanent hybrid DP). Confidence: HIGH.**

The RP2354 DP reports DPv2, not DPv3, and does so consistently. Across all three runs the DPIDR read returns the identical value `0x10212927`, which the INFO line decodes as `Debug Port version: DPv2 MinDP: Implemented`. Quoting run 1:

> `Result of DPIDR read: 0x10212927`
> `Debug Port version: DPv2 MinDP: Implemented`

Runs 2 and 3 are byte-identical on these two lines. Run 1 further shows DPIDR being read **three times** during a single attach (at `debug_port_connect`, `debug_port_start`, and again before AP access) — all three return the same `0x10212927` DPv2 value. This rules out H2 (transient first-read then steady-state DPv3): there is no steady-state DPv3 value to converge to, only DPv2. Patched arm fires exactly as predicted ("`ApV2 access on DPv1 cache; upgrading to DPv3`"), writes `SELECT = 0x2df0` / `DP_BANK_SEL = 5` as DPv3 encoding, and the subsequent AP IDR read returns `IDR { … CLASS: Undefined, … TYPE: JtagComAp }` — decoding to `0x0000_0000`, exactly the "SELECT landed at an address that doesn't exist on this DP" signal in the HLD's §3.3 row-1 description. The Track A `(V2, DPv1)` cache-upgrade arm is actively harmful on this target — it forces DPv3 SELECT encoding on a genuine DPv2 DP, which decodes to `APSEL=0x00, APBANKSEL=0x9` and reads a non-existent register as 0.

**Recommended A.2b option per HLD §4:** Option A-prime with the **H1 fix shape** (new `src/vendor/raspberrypi/sequences/rp235x.rs` with `debug_port_setup` / `debug_port_start` overrides that do dormant+alert+SWD-activation and probe DPIDR1/BASEPTR0 directly; treat ADIv6 as confirmed if BASEPTR0.valid=1 despite DPIDR.version=2). Same commit removes the Track A `(V2, DPv1)` arm and sentinel WARN — on H1 that arm is not just dead code, it is actively wrong and must go. Estimated 3–5 h per HLD §4.3.

**Surprises / ambiguities for lead:**

1. **Pre-trace ranking inversion.** HLD §2.1 had H2 at prior 0.75 and H1 at 0.15, based on the `probe_diff_rp2350 --fuzz 4000` 4000/4000 PASS evidence. Trace firmly favours H1. The 4000/4000 PASS must have a different explanation — possibly that `probe_diff_rp2350` is only hitting ISA-path AP accesses that happen to work under the DPv3-forced-SELECT on a DPv2 DP (`SELECT=0x2df0` decodes on DPv2 as `APSEL=0x02, APBANKSEL=0xD` which might alias to a real AP by coincidence), or that its read path never exercises `memory_interface()` on the V2 AP. Worth a one-line check before A.2b lands: does `probe_diff_rp2350`'s fuzz loop actually hit `MemoryAp::new` at the `0x2000` base? If yes, we have an unexplained data point; if no, the `--fuzz 4000` PASS is simply not comparable to `probe_verify_rp2350`'s failure surface.
2. **Debug-build requirement.** The HLD's §3.2 procedure assumes release `RUST_LOG=probe_rs=trace` produces useful output, but the workspace's `release_max_level_info` feature strips TRACE/DEBUG at compile time. A future trace-capture task should be dispatched with "use debug build" baked in, or the HLD should flag this. Time cost was ~90 s for a debug build + a second copy+run — manageable but non-obvious to an unbriefed agent.
3. **BASEPTR0 / DPIDR1 not in the captured trace.** The current probe-rs path on a target presenting DPv2 never reads BASEPTR0 or DPIDR1 — those are ADIv6-only DP registers (bank 1+). To confirm H1's premise that RP2354 is a true ADIv6 DP that merely reports version=2 for multidrop compatibility, the A.2b implementation agent should add a `select_dp(bank=2)` + `read_dp_register(BASEPTR0)` probe and include that raw value in the journal. If `BASEPTR0.valid = 0` on RP2354 too, H1's fix shape changes (it would mean RP2354 is genuinely a DPv2 target with V2-address-shaped APs via a proprietary protocol, not a mis-reporting ADIv6). HLD §4.2's H1 patch shape handles this correctly already — flagging here so lead knows the first thing A.2b must do is confirm BASEPTR0.valid.

### 2026-04-22 A.2b Gate 1: BASEPTR0 probe

**Procedure.** Added a temporary `tracing::info!` instrumentation inside `select_dp` (after the DPIDR read, when version != DPv3) that calls `self.read_dp_register::<DPIDR1>(dp)` and `self.read_dp_register::<BASEPTR0>(dp)` and logs the raw word plus the decoded `valid` bit for BASEPTR0. Reads go through the DPv1 SELECT cache; DPBANKSEL occupies bits [3:0] in both `SelectV1` and `SelectV3` so the bank routing is identical regardless of cache variant. Built `probe_verify_rp2350` in debug profile (release strips DEBUG so we couldn't see the raw SELECT writes) and ran once with `RUST_LOG="probe_rs::architecture::arm::communication_interface=trace,probe_rs::architecture::arm::dp=trace"`. Raw log: `wrk_debug/gate1-baseptr0-probe.stderr.log`. Key extract: `wrk_debug/gate1-key-lines.log`.

**Raw values read from RP2354 DP:**

| Register | DPBANKSEL | SELECT written (observed) | DP[0x0] read back | Decoded |
|---|---|---|---|---|
| DPIDR    | 0 | `0x00000000` | `0x10212927` | `version=2`, `designer=0x493`, `part_no=2`, `revision=1`, `min=true` |
| DPIDR1   | 1 | `0x00000001` | `0x10212927` | (same bytes as DPIDR — `asize=0x27`, `errmode=false`) |
| BASEPTR0 | 2 | `0x00000002` | `0x10212927` | (same bytes as DPIDR — `ptr=0x00010212`, `valid=true`) |

The SELECT writes with DPBANKSEL=1 and DPBANKSEL=2 ARE reaching the hardware (confirmed by the DEBUG trace of the `Writing DP register SELECT, value=0x00000001` / `0x00000002` lines). But the subsequent read of DP address 0x0 returns **byte-identical** `0x10212927` for all three DPBANKSEL values. That is, on RP2354 the DP register at address 0x0 is **not banked** — it always returns DPIDR regardless of DPBANKSEL.

**Interpretation:** the `valid: true` decode on the "BASEPTR0" row is a false positive. The returned word is DPIDR (bit 0 = revision LSB = 1 on this silicon), not a real BASEPTR0. RP2354 does NOT implement DPIDR1 or BASEPTR0 at all — the DP is a genuine DPv2 with no ADIv6 bank 1/2 presence.

**Gate 1 decision per HLD §9.2: STOP. Do not write the drafted H1 sequence override.**

The HLD's `BASEPTR0.valid == 1` branch assumed a mis-reporting ADIv6 DP (version field lies, but DPIDR1/BASEPTR0 respond). RP2354 does neither: it honestly reports DPv2 and has no DPv3 registers. It is the `BASEPTR0.valid == 0` branch — "RP2354 is a true DPv2 with V2-address-shaped APs via a proprietary multidrop extension" — which the HLD explicitly routes back to lead for a refined HLD. The drafted H1 sub-shape (force `SelectCache::DPv3(SelectV3(0), Select1(0))` in the vendor sequence) would do the exact same thing the current Track A arm does — write DPv3-encoded SELECT to a DPv2 DP — and hit the same `Arm(WrongApType)` failure. It is not a fix on this target.

**Gate 2 not executed.** Per §9.2 the Track A arm removal decision only arises after Gate 1 passes. Since Gate 1 says STOP, Gate 2's scope (how `probe_diff_rp2350 --fuzz 4000` passes under H1) is moot for the drafted fix; it remains a valid open question for the refined HLD.

**Instrumentation reverted.** The `select_dp` additions used for this probe have been removed from `third_party/probe-rs-0.31.0-mdrp-patched/src/architecture/arm/communication_interface.rs`; `git diff` on that file is clean. No code changes committed for A.2b.

**Recommendation to lead.** Open a revised HLD (A.2c?) with a different fix shape. Candidate options, not yet evaluated:

1. **DPv2 with multidrop V2-AP proxy.** RP2354 may address APs via a Raspberry-Pi-specific encoding layered over DPv2 SELECT. The datasheet §3.4.1 should spell this out — the next step is to read the RP2350 datasheet's DAP chapter end-to-end (not just §3.4) and find how the RPi docs say to *address* a V2 AP from a DPv2 DP. The Raspberry Pi OpenOCD config (`target/rp2350-arm.cfg`) is authoritative: mirror the SELECT / AP encoding pattern it uses.
2. **pre-AP-access SELECT shim.** If the RP-specific encoding is "write something non-standard to SELECT[31:4] on a DPv2 DP to reach the V2 address space", then a sequence-level shim in `rp235x.rs` that writes a chip-specific SELECT pattern before every AP access could work — but this is invasive and needs datasheet confirmation before being drafted.
3. **OpenOCD / fallback oracle.** If probe-rs + CMSIS-DAP fundamentally can't do this without a substantial rewrite, fall back to OpenOCD for the silicon oracles and keep probe-rs only for the ISA oracle (`probe_diff_rp2350`) which already works under the existing Track A workaround. Cost: harness-side shims to talk to OpenOCD's GDB RSP instead of probe-rs directly. Out of scope here.

The Track A `(V2, DPv1)` arm + sentinel WARN in `communication_interface.rs:485-496` must stay in place — removing it would reinstate the `unreachable!()` panic. Tagging it `// FIXME(A.2c): belt-and-braces until refined fix lands` is appropriate but not urgent; deferring to the next HLD author.

### 2026-04-22 A.1 Phase 1 triage — COULD NOT REPRODUCE

Triage agent (`aa827d6a466717525`) ran `probe_diff_rp2040 --probe ` after Phase 0b landed. Result: **392/392 PASS, 5/5 runs, exit 0.** No `fatal:` line, no `caused by:` chain — the original "ARM specific error occurred." symptom is not currently reproducing.

```
Attached to target, using core 0
Running 392 M0+-compatible targeted tests...
Total: 392 Passed: 392 Failed: 0 Skipped: 0 Undef: 0 Time: 22.1s
```

Phase 0b's source-chain walker is purely stderr formatting and cannot have affected the attach path. The bug was transient — residual DP / USB enumeration state from a prior session cleared between the original observation and now (possibly via the A.2a trace runs that cycled SWD state several times).

**CLI cross-check:** `probe-rs info --chip RP2040 --probe ` succeeds (exit 0) via upstream 0.31.0 CLI on `%PATH%`. Reports `DPv2, MINDP, Raspberry Pi`, two multidrop instances with V1(0) MemoryAPs exposing the `0xe00ff000` ROM table. `probe-rs reset --connect-under-reset` fails with `Timeout while attaching to target under reset` — Pico V1 does not wire RESET; Option A is contraindicated.

**HLD §5.2 amendment needed (flag).** The fork at `third_party/probe-rs-0.31.0-mdrp-patched/` holds only the library crate — no `probe-rs-tools` — so the fork-CLI build command in §5.2 was never going to work as written. Either strip the fork-CLI option from the HLD or add `probe-rs-tools` to the fork.

**Lead decision: land Option F (bounded retry) as defensive posture, NOT as a bug fix.** Retry exhaustion still surfaces the real error via Phase 0b's source-chain walker. 15-line change per HLD §6 Option F pseudocode; no fork divergence. If the bug returns, future triage has the diagnostic already in place.

Matrix row matched: none (matrix keyed on binary failing; we pass 5/5). Closest row is 6 (flaky) but 0/5 failures is not a flake signature — it's "bug has cleared since last observed". HLD §10 R6 explicitly permits this close-out posture.

Dispatching Option F impl agent next.

### 2026-04-22 A.2c pre-Gate — OpenOCD on RP2354

**OpenOCD version:** `xPack Open On-Chip Debugger 0.12.0+dev-02228-ge5888bda3-dirty (2025-10-04-22:44)`
**Install method:** Downloaded `xpack-openocd-0.12.0-7-win32-x64.zip` from `github.com/xpack-dev-tools/openocd-xpack` release v0.12.0-7 (SHA256 verified against release manifest `6bfd3c97135aafef8affc9af1acf34fd0e2b9ca26044506f6abd7f95b7630052`), extracted to `<user-profile>\tools\openocd\xpack-openocd-0.12.0-7\`. No system PATH pollution. Target config `rp2350.cfg` ships in the bundle at `openocd/scripts/target/rp2350.cfg` (uses `dap create ... -adiv6 -chain-position ...`). No separate RPi-fork binary download needed.
**Command issued:** `openocd.exe -f interface/cmsis-dap.cfg -c "adapter serial <RP2354 probe serial>" -c "adapter speed 5000" -f target/rp2350.cfg -c "init" -c "targets" -c "dap info 0" -c "exit"` (working dir = bin; writes to stderr, stdout was empty — OpenOCD convention. Logs captured at `wrk_debug/openocd-rp2354-attach2.stderr.log` (216 lines) and `wrk_debug/openocd-rp2354-attach2.stdout.log` (0 bytes).) A first attempt used the untyped command-line form without `adapter speed 5000`; it succeeded on the init path but hit a post-gdb-server `clearing lockup after double fault` that we bypassed on the second, command-driven run.
**Exit code:** `0`
**Outcome:** **A (H1' strongly confirmed — with a large surprise)**

OpenOCD attached cleanly, examined both Cortex-M33 cores, and walked the full ADIv6 ROM table. `targets` shows `rp2350.cm0` and `rp2350.cm1` both present with TAP `rp2350.cpu`, endianness little, state `unknown` (halt wasn't requested). `dap info 0` enumerates AP# `0x0000` (ROM), `0x2000` / `0x4000` (SoC-600 AHB5 MEM-AP v2, IDR `0x34770008`), `0x6000` (TSGEN), `0x7000` (ATB Funnel), `0x8000` (TPIU), `0x9000` (CTI) — a textbook SoC-600 debug mesh. Both MEM-APs report `Type is MEM-AP AHB5 with enhanced HPROT`, Dev Arch `0x47700a17` (MAP v2 architecture), and a valid ROM table at `0xe00ff000` chaining to the ARMv8-M STAR Debug / DWT / FPB / ITM / ETM / CTI components in M33 private-peripheral space. The SELECT wire traffic OpenOCD uses to address `0x2000`/`0x4000` APs is standard ADIv6 encoding — which is exactly the wire shape HLD §2.5 predicted Option A would produce after the DPv3⇔V2-AP decoupling refactor.

**The surprise (unexpected but still OUTCOME A).** OpenOCD reads `SWD DPIDR 0x4c013477` on the same probe, same target, same session. probe-rs 0.31 on the same hardware reads `0x10212927` (documented in §2b Gate 1 above). **Same silicon, same probe, different DPIDR** — this is not "silicon variation between boards"; it's a read-path divergence between the two debuggers. Working theories:
- probe-rs reads DPIDR earlier in the JTAG-to-SWD sequence than OpenOCD does, when the DP hasn't yet transitioned out of a legacy/transient state;
- one tool sends a dormant-state wake (JEDEC sequence) that flips the DP into its ADIv6 identity, and the other doesn't;
- one tool issues a line reset after the initial DPIDR read, flushing a stale value.
The OpenOCD log shows only "CMSIS-DAPv2 interface … SWD supported … Interface ready … clock speed 5000 kHz" before the DPIDR read — no separate dormant-wake trace step is logged at this verbosity. To settle this we'd need a USB-level capture of both tools' handshakes. But for the A.2c decision that is out of scope: the CRITICAL bit is that RP2354 silicon can be driven as a DPv3/V2-AP target, and OpenOCD's `-adiv6` dap-create path shows **the wire bytes are standard**. No proprietary SELECT extension. H1'' (proprietary shim) is definitively ruled out; H1''' (sequence-level blocker) is ruled down from 0.30 to low prior.

What this means for probe-rs: the Option A refactor described in HLD §6 is necessary (to let probe-rs emit V2-AP SELECT bytes on a DP it currently classifies as DPv2), AND probe-rs's DPIDR read is currently misreading this silicon. The refactor may need to be accompanied by a dormant-wake sequence or read-retry that lets probe-rs see the same `0x4c013477` OpenOCD sees — if so, H1' becomes cleanly confirmed. If after the refactor probe-rs still reads `0x10212927`, Option A still completes per HLD §6 (addressing is target-property), but a follow-on patch is needed to teach probe-rs the right power-up / dormant wake sequence that OpenOCD implicitly uses.

**Quoted ROM table summary line:** `AP # 0x2000 … Part is 0x9e3, SoC-600 AHB-AP (AHB5 Memory Access Port) … Dev Arch is 0x47700a17, ARM Ltd "Memory Access Port v2 architecture" rev.0 … AP ID register 0x34770008 … Type is MEM-AP AHB5 with enhanced HPROT`.
**Quoted `targets` output:**
```
    TargetName         Type       Endian TapName            State
--  ------------------ ---------- ------ ------------------ ------------
 0* rp2350.cm0         cortex_m   little rp2350.cpu         unknown
 1  rp2350.cm1         cortex_m   little rp2350.cpu         unknown
```

**Gate 3 recommendation for lead:** **proceed with Option A implementation per HLD §6.** The DPIDR surprise is a useful side-finding — note it in the Option A implementation dispatch so the agent is prepared to add a dormant-wake / DPIDR re-read step if the refactor alone doesn't unlock IDR on silicon. The Option A refactor is still the right first move regardless.

### 2026-04-22 Lead sign-off A.2c Option A; Option F landed

- A.2c HLD §2.5 revised per pre-Gate finding: the silicon is SoC-600 / DPv3 / ADIv6 (OpenOCD proves it). probe-rs reads DPIDR wrong due to a missing dormant-wake / SWD-activation sequence. Not silicon variation — a probe-rs read-path bug. H1'' definitively ruled out; H1''' ruled down.
- A.2c HLD §10: Option A implementation SIGNED OFF with three Gate 3 outcome definitions (green / yellow / red) so the impl agent can self-supervise. 6–10 h budget.
- A.1 Option F impl agent returned with code-reviewer approval but the worktree branch `worktree-agent-a96bce73` had no commit (files modified, not committed). Applied the diff manually on main, built clean, landed as `29541b6`. Harness bumped 0.35.4 → 0.35.5.
- A.1 Phase 2 Option F complete. Track A.1 close-out pending the Option A wave (to confirm no regression on RP2040 path).
- A.2c Option A implementation agent dispatched next.

### 2026-04-22 A.2c Option A — Gate 3 RED, refactor landed as groundwork

A.2c Option A impl agent (`ae3912c133c376866`) delivered 3 commits on worktree branch `worktree-agent-ae3912c1`. Cherry-picked to main:
- `64563e4` — probe-rs: add AdiVersion enum and plumb from ArmDebugSequence trait
- `97800c8` — probe-rs: use AdiVersion (not DPIDR.version) for SELECT cache init and AP enumeration
- `e254d33` — probe-rs: declare ADIv6 for RP235x in vendor sequence

**Gate 3: RED.** From the agent's `/tmp/gate3-output.log`:
- `"RP235x debug sequence constructed; declaring ADIv6 for V2-AP addressing"` — our vendor sequence fires.
- `"Target declared ADIv6 addressing (vendor sequence override); SELECT cache initialised as DPv3 regardless of DPIDR.version"` — Option A path confirmed live.
- `"Debug Port version: DPv2 MinDP: Implemented (target adi_version: ADIv6)"` — probe-rs STILL reads DPIDR as DPv2.
- `"Error: Arm(WrongApType)"` — AP IDR read STILL fails.

This is unambiguously RED per HLD §10: refactor fires, addressing works, but the read-path gap (why probe-rs reads DPIDR as DPv2 when OpenOCD reads it as DPv3 on the same silicon) still blocks IDR reads. **H1''' (sequence-level blocker) is now the confirmed leading hypothesis.**

Refactor landed anyway per HLD §5/§7.6 reasoning: "the Option A refactor is the right first move regardless, providing groundwork for follow-on diagnosis." Also regression-clean — 2614 tests pass, `probe_diff_rp2350` 4000/4000, `probe_diff_rp2040` 127/127. Code-reviewer approved with nits only.

Track A sentinel arm: kept in place with FIXME. Still load-bearing against the `unreachable!()` panic.

**Next: Track A.2d — dormant-wake / SWD-activation investigation.** Goal: identify what specifically OpenOCD's `-adiv6` declaration triggers at SWJ-DP-switch time that probe-rs doesn't do, and patch the gap. Expected deliverable: a small addition to `Rp235x::debug_port_setup` that sends the ADIv6 dormant-state wake + alert + SWD-activation sequence unconditionally. If successful, combined with A.2c Option A, probe-rs should read DPIDR as DPv3 and AP IDR should succeed.

### 2026-04-22 A.2d Gate 4 results

**Outcome: RED.** Exit code `1`, `Error: Arm(WrongApType)`. Main at `6a478d8`. Debug-profile build, `RUST_LOG=probe_rs=trace`, probe `` → RP2354. Logs: `wrk_debug/gate4-a2d.stdout.log` (2 lines, banner only) and `wrk_debug/gate4-a2d.stderr.log` (273 lines).

**Criterion 1 (swj_sequence bursts on first attempt before `debug_port_connect`): PASS.** The A.2d patch fires exactly as designed. From stderr:
- L128 `INFO  Rp235x::debug_port_setup: ADIv6 dormant-wake path (matches OpenOCD -adiv6); dp=Default`
- L130 `TRACE swj sequence:111111111111111111111111111111111111111111111111111` (SWD line reset, 51 bits high)
- L133 `DEBUG RP235x: SWD → Dormant (0x33BBBBBA, 31 bits)`; L134 `swj sequence:0101110111011101110111011100110` (31-bit SWD→Dormant)
- L137 `TRACE Sending Selection Alert sequence`; L138 `swj sequence:11111111` (8-bit preamble)
- L141 `swj sequence:0100100111001111100100000100011010101001101101001010000101100001` (alert half 1, 64 bits)
- L144 `swj sequence:1001011111110101101110111100011101000101011100000011110110011000` (alert half 2, 64 bits)
- L147 `swj sequence:000001011000` (12-bit SWD activation `0x1A`)
- L150 `DEBUG debug_port_connect: SWD: Connecting to debug port with address Default` — confirms bursts precede the first connect.

**Criterion 2 (DPIDR = `0x4c013477`, DPv3): FAIL.** First post-wake DPIDR read at L163 returned `0x0bc12477` (MSB lost — transient), but the canonical re-read at L241 is the decisive one:
- L241 `Read DP register DPIDR, value=0xDPIDR { .0: 10212927, revision: 00000001, part_no: 00000002, min: true, version: 00000002, designer: 00000493, jep_cc: 00000009, jep_id: 00000013 }`
- L242 `INFO Debug Port version: DPv2 MinDP: Implemented (target adi_version: ADIv6)`

DPIDR still reads `0x10212927` — byte-identical to every A.2a/A.2b/A.2c capture. The dormant-wake burst has no effect on post-wake DPIDR identity.

**Criterion 3 (AP IDR at V2 `0x2000` = `0x34770008` or any MEM-AP): FAIL.** L258 `reading IDR: IDR { REVISION: 0, DESIGNER: JEP106Code({ cc: 0x00, id: 0x00 } => None), CLASS: Undefined, _RES0: 0, VARIANT: 0, TYPE: JtagComAp }` — IDR = `0x00000000` (same as Gate 3 A.2c).

**Criterion 4 (exit 0, no `WrongApType`): FAIL.** L273 `Error: Arm(WrongApType)`; exit=1.

Per HLD §8 "Red" row: DPIDR `0x10212927` ⇒ H_A wrong. Necessary prerequisite (swj bursts precede debug_port_connect on first attempt, matching OpenOCD's wire-level behaviour) is satisfied, but it is not sufficient on this silicon — hypothesis that the dormant→alert→SWD-activation sequence flips the DP into DPv3-identity mode is falsified against RP2354 Pico 2.

**Regression gates: SKIPPED** per instructions (run only if Gate 4 GREEN).

**Recommendation for lead: escalate per HLD §9.2 — capture USB-level CMSIS-DAP frames from OpenOCD and probe-rs attaching within minutes of each other, diff byte-for-byte.** The working OpenOCD attach captured in `wrk_debug/openocd-rp2354-attach2.stderr.log` reads DPIDR = `0x4c013477` (DPv3) on the same silicon, so *something* OpenOCD sends differs from what probe-rs sends now — even with the dormant+alert+activation bursts faithfully transmitted per the §8 criterion-1 trace. Candidates to diff: clock pinning (OpenOCD may `swj_clock` before the burst), chunking/packing of the 128-bit alert across CMSIS-DAP packets (our L141/L144 split into 2×64 over 2 requests; OpenOCD may pack differently), the line-reset length (our 51 bits vs OpenOCD's), ordering vs SWD line reset. File as A.2e with scope = "USB-frame differential capture + root-cause identification"; bypass Option II (flipping the default) — we already tested the equivalent (unconditional dormant wake) and it didn't move DPIDR, so flipping the default would just cargo-cult a no-op. Do NOT proceed to Commit 3/4 (Track A sentinel arm must stay; the unreachable!() path is still live).

**Blockers / surprises:**
- L163 showing `0x0bc12477` on the first post-wake DPIDR read is interesting — it's `0x10212927`'s complement-ish neighbour but with the top nibble `0` instead of `1`, suggesting either a sampling race on the first bit or probe-rs's line-reset was slightly late. The probe-rs retry-path at L241 got a clean read as DPv2; OpenOCD reportedly gets a clean read as DPv3 on the same hardware. The delta between `0x10212927` (what we see) and `0x4c013477` (what OpenOCD sees) is large — different `part_no`, `designer`, `revision` — so this is not a bit-flip, it's a genuinely different DP responding. Strong signal that RP2354 exposes two DPs (DPv2 + DPv3) selectable by some wire-level discriminator we're not driving. That discriminator is what A.2e needs to find.
- Build was clean (5s incremental, debug profile as HLD §8 mandates for TRACE retention); no concurrent-agent contention observed.
- `rp235x: send dormant-to-SWD wake unconditionally on attach (matches OpenOCD -adiv6)` (commit `6a478d8`) is functionally correct — the burst fires on attempt 1, matches OpenOCD's `-adiv6` shape in the coarse-grained log. The code itself is not at fault; the hypothesis it implements is what Gate 4 disproves.

### 2026-04-22 Wave close-out — checkpoint with Arthur

**Final state.** Three original follow-on tracks from yesterday's Oracle Fix Team wave:

| Track | Status | Evidence |
|---|---|---|
| **PR** (upstream probe-rs #3872) | **100% ready for Arthur to submit** | `wrk_docs/2026.04.22 - PR - probe-rs V2-on-DPv1 recovery.md` |
| **A.1** (RP2040 "ARM specific error") | **Closed as "not reproducing"; Option F defensive retry landed** | Phase 0a firmware `bcdDevice=0x0230` confirms denylist floor passed. Phase 0b diagnostic walker landed (`26a2b9a`). Phase 1 triage: 5/5 PASS runs. Option F retry landed (`29541b6`) as belt-and-braces. |
| **A.2** (RP2354 `Arm(WrongApType)`) | **Partial — groundwork landed, root cause not yet fixed** | A.2a trace confirmed H1 (probe-rs-side). A.2b Gate 1 ruled out mis-reporting-ADIv6 (BASEPTR0 probe). A.2c OpenOCD pre-Gate confirmed silicon IS DPv3 — read-path divergence. A.2c Option A refactor landed (`64563e4..e254d33`). A.2d dormant-wake override landed (`6b2edfe..6a478d8`). Gate 4 RED: DPIDR still reads DPv2. A.2e USB-frame capture required for root cause. |

**Commits landed this wave on main:**
- `26a2b9a` — probe_diff_rp2040: print error source chain on fatal exit (Phase 0b)
- `29541b6` — probe_diff_rp2040: bounded attach retry as defensive posture (Option F)
- `64563e4` — probe-rs: add AdiVersion enum and plumb from ArmDebugSequence trait (A.2c commit 1)
- `97800c8` — probe-rs: use AdiVersion for SELECT cache init and AP enumeration (A.2c commit 2)
- `e254d33` — probe-rs: declare ADIv6 for RP235x in vendor sequence (A.2c commit 3)
- `6b2edfe` — probe-rs: promote alert_sequence to pub(crate) for vendor reuse (A.2d commit 1)
- `6a478d8` — probe-rs: rp235x: send dormant-to-SWD wake unconditionally on attach (A.2d commit 2)
- Plus ~7 docs/journal commits.

All regression gates green: `probe_diff_rp2350` 4000/4000 PASS, `probe_diff_rp2040` 127/127 PASS, 2614+ unit tests pass.

**Lessons learned this wave:**
1. Devils-advocate agent caught two substantial errors (stale upstream citations on A.1; H3 pre-rejectable by inspection on A.2) before implementation burned hours on wrong plans. Worth its weight.
2. Phase 0 gates with hardware — OpenOCD pre-Gate, BASEPTR0 probe, Gate 3/4 — repeatedly inverted pre-trace priors. Data beats speculation.
3. Worktree isolation was unreliable: three different implementation agents landed commits directly on main despite `isolation="worktree"` (detected during cherry-pick when main already had the change). Not damaging but worth flagging.
4. Several agent summaries under-reported (one said "no files modified outside wrk_debug and journal" when files WERE modified). The `feedback_verify_agent_summaries` memory pays off: always `git diff` before trusting a summary.

**Remaining work (not this wave):**
- A.2e USB-frame capture (Wireshark + USBPcap): 4-8h; needs admin rights on the host. This is qualitatively different from what we've done so far — USB-protocol debugging rather than probe-rs internals. Arthur's call whether to proceed.
- Track A sentinel arm removal: blocked on A.2e resolving the DPIDR puzzle. Arm stays in place (with FIXME in the code from A.2c) until then.
- Upstream PR submission: Arthur submits the refined body at `wrk_docs/2026.04.22 - PR - probe-rs V2-on-DPv1 recovery.md` when ready.

Next step: dispatch final verification agent to audit the wave's landed work against the signed-off HLDs.

