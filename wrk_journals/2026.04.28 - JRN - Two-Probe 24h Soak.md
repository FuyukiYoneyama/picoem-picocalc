# Two-Probe 24h Soak — 2026-04-28

Arthur connected both Pico debug probes (RP2354 on `<RP2354 probe serial>`, RP2040 on `<RP2040 probe serial>`) and asked for a 24h fuzz session leveraging them. I'm driving this overnight; he is asleep.

## Plan

Two parallel oracles, one per probe:

| Probe | DUT       | Driver                     | Oracle                         | Why this oracle |
|-------|-----------|----------------------------|--------------------------------|----------------|
| `<RP2354 probe serial>` | RP2354 | `fuzz-runs/run-test-silicon.sh` (new) | `test_silicon --soak 24h` | Bundles 5 oracles (probe_diff + cycle + periph + dualcore + isr) under one shared probe session, with per-iteration shuffling, hourly heartbeat, and per-case 60s watchdog with drop-and-reattach. Strictly more coverage than `probe_diff_rp2350` alone. |
| `<RP2040 probe serial>` | RP2040 | `fuzz-runs/run-m0plus-probe.sh` (existing) | `probe_diff_rp2040 --fuzz 200 --seed $RANDOM` in a batch loop | Pure ISA — silicon is the only oracle that admits the M0+ Thumb-32 subset (`BL`/`MRS`/`MSR`/`DSB`/`DMB`/`ISB`); QEMU oracle filters all wide encodings out. 24h of seeds maximises hits in that gap. |

Decision: pure-ISA on the RP2040 side (per Arthur's instruction). Skip `silicon_isr_diff_rp2040` — CLAUDE.md flags it as expected-FAIL until RP2040 IRQ plumbing lands.

## Setup notes

- Binaries already built at 22:12 BST tonight (test_silicon.exe, probe_diff_rp2040.exe). Skipping rebuild — no stale processes holding the .exe locks.
- `probe-rs list` confirms both probes enumerated.
- Both drivers PID-qualify their binary copy (`fuzz-runs/<bin>.<pid>.exe`), so concurrent rebuilds during the run won't fight us.
- Shared `fuzz-runs/deadline` file coordinates both drivers — set once, both scripts read it at startup.

## New script: `fuzz-runs/run-test-silicon.sh`

Mirrors the structure of `run-probe.sh` but wraps a single `test_silicon --soak <remaining>s` invocation rather than a `--fuzz N` batch loop. The orchestrator handles its own case shuffling, watchdog, and probe reattach; the shell wrapper only adds defense-in-depth restart-until-deadline logic in case the orchestrator panics outright. 30s back-off between restart attempts.

## Timeline

- **23:07 BST** — Wrote `run-test-silicon.sh`, made executable.
- **23:08 BST** — Set deadline to 2026-04-29T23:08:24+01:00. Launched both probe drivers in background. Both immediately failed `rc=2` with `Could not determine a suitable packet size for this probe`.
- **23:09 BST** — Drivers cycling rc=2 → 30s back-off → rc=2 (3 attempts each). Hit a probe-rs `info` directly, hand-bypassing the wrappers — same error. Confirms USB-level wedge, not a wrapper bug.
- **23:10 BST** — Killed both probe drivers (PID 11640, 11663). The trap removed the PID-qualified binary copies cleanly; no orphan processes. Verified PnP state — all four `VID_2E8A&PID_000C` interfaces (probe USB Composite + CMSIS-DAP v2 + USB Serial COM6/7) report `Status: OK`. Two BOOTSEL-mode entries for the chips themselves (`PID_000F` RP2350 Boot, `PID_0003` RP2 Boot) appear with `Status: Unknown` — but those are independent of probe attach.
- **23:11 BST** — Retried `probe-rs info` on both probes after a 60s settling pause with no driver activity. Still wedged. PowerShell admin check: `Admin: False` — cannot disable/enable PnP without elevation. **Recovery requires Arthur's physical unplug+replug** of both probes when he wakes.
- **23:13 BST** — **Pivoted to host-side QEMU oracles** to keep the night productive. Wrote `run-riscv32.sh` (Hazard3 driver was missing). Launched all three: `run-m33.sh` (PID 12201), `run-m0plus.sh` (PID 12202), `run-riscv32.sh` (PID 12203). Distinct GDB ports (3333/3334/3335) so they don't collide.
- **23:14 BST** — All three oracles generating tests cleanly. M33: 1.19M cases per batch. M0+: 638K cases per batch. RISC-V: 30K cases per batch.
- **23:14 BST** — Wrote `probe-watch.sh` to poll probes every 15 min. **First version had a bug**: `probe-rs info` returns `rc=0` even when both JTAG and SWD attaches fail with the wedge error, so the watcher mistakenly launched the test_silicon driver. Caught immediately (first attempt failed rc=2, restart loop kicked in), killed the spurious driver and the v1 watcher.
- **23:15 BST** — Replaced the watcher's `probe_alive` with stderr-grep against the wedge fingerprint (`Could not determine a suitable packet size`, `Failed to open the debug probe`, `An error which is specific to the debug probe`) plus a positive-match for chip-info lines. Relaunched (PID 12379). First poll correctly identifies both probes as wedged.

## Current state

**Running:**
- `run-m33.sh` (PID 12201) — qemu_diff_m33 batch 1, 28K/1.19M tests, 0 failures
- `run-m0plus.sh` (PID 12202) — qemu_diff_m0plus batch 1, 28K/638K tests, 0 failures
- `run-riscv32.sh` (PID 12203) — qemu_diff_riscv32 batch 1, 14K/30K tests, 0 failures
- `probe-watch.sh` (PID 12379) — polling every 15min for probe recovery

**Stopped:**
- Both probe-driven oracles. Will auto-relaunch when WinUSB clears (probe-watch v2 logs "still wedged" or "responsive" each poll).

## What Arthur should do when he wakes

1. **Physically unplug both Pico debug probes** (USB cables). Wait 5 seconds. Replug.
2. After ~10 seconds, `probe-rs list` should still show both probes; the watcher should detect them on its next 15-min poll and auto-launch the silicon drivers.
3. If you don't want to wait up to 15min, kick `probe-watch.sh` off manually with `nohup ./fuzz-runs/probe-watch.sh ...` (it will detect probes immediately on the new run).

## Progress snapshots

### 23:45 BST (T+30 min)

| Oracle | Batches done (rc=0) | Current batch progress | Failures |
|--------|--------------------|------------------------|----------|
| qemu_diff_m33 | 0 (mid batch 1) | 353K / 1.19M (29.7%) | 0 |
| qemu_diff_m0plus | 0 (mid batch 1) | 366K / 638K (57.4%) | 0 |
| qemu_diff_riscv32 | 6 | mid batch 7 | 0 |

Aggregate ~720K cases against QEMU in 30 min = ~24K cases/min sustained. Probe-watch poll #2 (23:30) — both still wedged, as expected.

### 07:10 BST (T+8h)

Arthur back. Eight clean hours of QEMU coverage, zero failures across all three oracles, ~12.7M differential cases.

| Oracle | Complete batches (rc=0) | Current batch | Cases done | Failures |
|--------|-------------------------|---------------|------------|----------|
| qemu_diff_m33 | 4 | mid batch 5 (241K / 1.19M) | ~4.76M | 0 |
| qemu_diff_m0plus | 8 | mid batch 9 (275K / 638K) | ~5.10M | 0 |
| qemu_diff_riscv32 | 94 | mid batch 95 (10K / 30K) | ~2.82M | 0 |

Probe-watch: 32 polls completed (every 15 min), both probes still wedged on every poll. Zero responsive transitions — the wedge has not cleared on its own, as expected per the 2026-04-25 incident pattern.

**Decision points for Arthur:**
- Reseat both probes now → silicon drivers auto-launch on next 15-min poll. Best case: ~15h of two-probe silicon coverage starting in ≤15 min.
- Skip silicon entirely; let QEMU oracles run the remaining ~16h. They've been steady at ~26K cases/min and have plenty of headroom in the catalogue.
- Stop the run early if the goals from `2026.04.28 - JRN - Four-Track Test Infrastructure Push.md` need the host's CPU back.

### 23:08 BST (T+24h, deadline)

24-hour window closed. probe-watch exited cleanly on the deadline check.

### Drivers wrapped up (post-deadline tail-out)

Each driver finishes its in-flight batch before the while-loop deadline check fires, so the actual exit times are after the 23:08 deadline:

- RISC-V: deadline-exit at 23:09:01 (1 min tail).
- M0+: deadline-exit at 23:43:19 (35 min tail — one M0+ batch is ~58 min so the partial batch finished mid-stride).
- M33: deadline-exit at 00:49:38 next day (101 min tail — M33 batches average ~110 min, so this last one ran almost full length).

All three drivers exited cleanly with `deadline reached` markers, and the `trap 'rm -f $BIN' EXIT` removed every PID-qualified binary copy. No residual processes; no orphan binaries.

### Final coverage (drivers fully drained)

| Oracle | Complete batches (rc=0) | Approx cases | Failures |
|--------|-------------------------|--------------|----------|
| qemu_diff_m33 | 13 | ~15.5M | **0** |
| qemu_diff_m0plus | 25 | ~15.95M | **0** |
| qemu_diff_riscv32 | 273 | ~8.19M | **0** |
| **Total** |  | **~39.6M** | **0** |

**Probe-watch:** 95 polls completed (every 15 min over 24h), zero `responsive` transitions. Both probes stayed wedged for the entire window. The wedge has now persisted at minimum 24h with zero driver activity (drivers stopped at 23:09 BST 04-28); confirms the journal-noted recovery requirement of physical unplug+replug is the only known fix.

## What's been learned

- `probe-rs info` returns rc=0 even on probe-open failure — surprising, makes scripted health checks brittle. Documented in the watcher script's comments. Worth raising upstream or in our own runbook.
- The WinUSB wedge that struck both probes simultaneously on first attempt suggests the wedge predates our session — likely left over from a session that ended uncleanly earlier today (the four-track infrastructure push journal references "probes are running fuzz" but the latest probe.log mtime was 2026-04-26, so that journal entry was aspirational, not realised).

## What I'm watching for

- **RP2354 side**: any FAIL in soak output, hourly heartbeat counts, watchdog firing patterns. The ISR oracle had recent activity (per CLAUDE.md note about banked-SP fix on 2026-04-16); soak should exercise it heavily.
- **RP2040 side**: aggregate PASS/FAIL/SKIP/UndefOnSilicon counts per batch. UndefOnSilicon hits indicate filter-gap escapes — interesting but not necessarily real bugs.
- **Cross-cutting**: `rc=2` lines in either log → probe attach failed (USB endpoint busy). The drivers already back off 30s on rc=2.

## Risks I've accepted

- Two simultaneous probe sessions on one host. Both drivers use explicit `--probe VID:PID:SERIAL`, so no auto_attach contention. USB stack should handle it (the rig has done this before).
- The 24h window straddles into Arthur's morning. If something goes wrong I can't restart cleanly without him; design favours self-recovery.
