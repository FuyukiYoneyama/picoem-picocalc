# 2026-05-03 — Overnight Hardware Fuzzing Supervision

## Mission

8-hour overnight hardware fuzz session, both Pico debug probes attached.
Supervisor (this journal) launches the drivers, schedules periodic
check-ins, and dispatches investigation agents on any FAIL / DIFF / panic.

- **Start:** 2026-05-03T00:53 BST (`deadline` = 1777794792 = 08:53 BST)
- **Window:** 8h
- **Probes:**
  - RP2354 (Pico 2) → ``
  - RP2040 (Pico V1) → ``

## Drivers launched

| Target | Driver | Log | Cadence |
|---|---|---|---|
| RP2354 (M33 + peripherals) | `fuzz-runs/run-test-silicon.sh` | `fuzz-runs/test-silicon.log` | one orchestrator, soak mode, restarts on early exit |
| RP2040 (M0+ ISA) | `fuzz-runs/run-m0plus-probe.sh` | `fuzz-runs/m0plus-probe.log` | 200-case batches in a loop |

`test_silicon` covers: `probe_diff_rp2350` (ISA), `silicon_cycle_oracle_rp2350`,
`silicon_periph_diff_rp2350`, `silicon_dualcore_diff_rp2350`,
`silicon_isr_diff_rp2350`. Single shared probe session, Fisher-Yates
case shuffle each iteration, per-case 60s watchdog with drop-and-reattach,
hourly heartbeat.

`probe_diff_rp2040` covers M0+ ISA against live RP2040 silicon (probe-rs
0.31). No cycle comparison — M0+ has no DWT CYCCNT.

## Pre-flight checks

- [x] `probe-rs list` shows both probes (`<RP2040 probe serial>` + `<RP2354 probe serial>`).
- [x] `target/release/test_silicon.exe` and `target/release/probe_diff_rp2040.exe` built (mtime 2026-05-03 00:25).
- [x] Drivers PID-qualify their copies, so concurrent rebuilds remain safe.
- [x] `fuzz-runs/deadline` set to `now + 8h`.

## Supervisor protocol

1. Launch both drivers in background.
2. Schedule wake-ups every ~25 min via `ScheduleWakeup`.
3. Each wake: `tail` both logs, scan for `FAIL`, `DIFF`, `panic`, `assertion failed`, `rc=2` storms, hourly heartbeat lines from test_silicon.
4. On any failure signal: capture seed + scenario + register diff, dispatch a `general-purpose` agent for repro + investigation. Keep the supervisor's context minimal.
5. Append timestamped notes to this journal at every wake.

## Timeline

### 2026-05-03T00:53 — supervisor armed

Pre-flight passed. Launching drivers next.

### 2026-05-03T00:54 — both probes wedged at WinUSB layer

Both silicon drivers hit `rc=2` immediately on attach with the
canonical fingerprint:

```
fatal: An error with the usage of the probe occurred
  caused by: An error which is specific to the debug probe in use occurred.
  caused by: Could not determine a suitable packet size for this probe.
```

After ~6 minutes of retry-with-backoff (12 attempts on RP2354 side, 9
on RP2040 side), no recovery. Killed both wrappers and ran
`probe-rs info` directly against each probe — same fingerprint on
both, JTAG and SWD. `probe-rs list` enumerates them, but the WinUSB
endpoint claim fails on every attach.

This matches the **2026-04-28 wedge incident exactly**
(`wrk_journals/2026.04.28 - JRN - Two-Probe 24h Soak.md`). That
incident persisted 24h with zero recoveries across 95 polls; the only
known fix is a physical unplug+replug. PowerShell elevation is not
available in this session, so no `pnputil /restart-device` path.

### 2026-05-03T01:05 — pivot to QEMU oracles + probe-watch

Mirroring the 2026-04-28 fallback exactly:

| Oracle | Driver | Log | PID |
|---|---|---|---|
| QEMU Cortex-M33 (RP2350 ISA) | `run-m33.sh` | `fuzz-runs/m33.log` | 264735 |
| QEMU Cortex-M0 (RP2040 ISA) | `run-m0plus.sh` | `fuzz-runs/m0plus.log` | 264736 |
| QEMU RV32 (Hazard3) | `run-riscv32.sh` | `fuzz-runs/riscv32.log` | 264737 |
| Probe recovery watcher | `probe-watch.sh` | `fuzz-runs/probe-watch.log` | 264738 |

`probe-watch.sh` polls every 15 min; if either probe recovers it will
auto-launch the corresponding silicon driver for the rest of the
window. So we get continuous coverage either way:
- if probes never recover (likely): three QEMU oracles deliver ~24K
  cases/min sustained per the 04-28 numbers (~10–11M total in 8h).
- if probes recover: silicon drivers come up automatically; QEMU
  oracles continue alongside.

Three QEMU instances on distinct GDB ports (3333/3334/3335) — no
collision risk. Drivers PID-qualify their `.exe` copies, so
concurrent rebuilds still link cleanly.

### 2026-05-03T01:36 — wake #2 (T+28min): clean

| Oracle | Batches | In flight | Failures |
|---|---|---|---|
| qemu_diff_m33   | 0 (mid batch 1) | 224K / 1219896 (~18%) | 0 |
| qemu_diff_m0plus | 0 (mid batch 1) | 198K / 687538 (~29%) | 0 |
| qemu_diff_riscv32 | 6 | mid batch 7 (17K / 30K) | 0 |

Aggregate: ~22K cases/min (in line with 04-28 figure of ~24K/min).
probe-watch poll #2 (01:23) — both probes still wedged.

### 2026-05-03T02:07 — wake #3 (T+59min): clean

| Oracle | Batches | In flight | Failures |
|---|---|---|---|
| qemu_diff_m33   | 0 (mid batch 1) | 462K / 1219896 (~38%) | 0 |
| qemu_diff_m0plus | 0 (mid batch 1) | 470K / 687538 (~68%) | 0 |
| qemu_diff_riscv32 | 8 | mid batch 9 (just started) | 0 |

Aggregate ~20K cases/min; probe-watch polls 3+4 (01:38, 01:53) — both
still wedged. PnP query (started at pivot) finally reported back: all
six interfaces `Status: OK`, `Admin: False`. Matches 04-28 picture
exactly — nothing recoverable at userland.

### 2026-05-03T02:38 — wake #4 (T+1h30): clean

| Oracle | Batches | In flight | Failures |
|---|---|---|---|
| qemu_diff_m33   | 0 (mid batch 1) | 702K / 1219896 (~58%) | 0 |
| qemu_diff_m0plus | 1 | mid batch 2 (5K / 687583) | 0 |
| qemu_diff_riscv32 | 12 | mid batch 13 (17K / 30K) | 0 |

Aggregate ~20K cases/min sustained. probe-watch polls 5+6 (02:08, 02:23)
— both still wedged.

### 2026-05-03T03:19 — wake #5 (T+2h11): M33 failures spotted, agent dispatched

| Oracle | Batches | In flight | Failures |
|---|---|---|---|
| qemu_diff_m33   | 0 (mid batch 1) | 1023K / 1219896 (~84%) | **3040** |
| qemu_diff_m0plus | 1 | mid batch 2 (363K / 687583) | 0 |
| qemu_diff_riscv32 | 18 | mid batch 19 (22K / 30K) | 0 |

**M33 failures** — all 3040 are class `M0PLUS_T32_MRS`, opcode `0xF3EF`
(MRS T1). Zero in any other M33 class. Pattern is stable:

- `MRS Rd, PRIMASK` (sysm=16): QEMU R*=0x1 vs EMU=0
- `MRS Rd, MSP` (sysm=8):     QEMU R*=0x40000 vs EMU=0
- `xpsr_pre=0x01000000`, `reg_pre=[]` — harness sets only T-bit and R0..R12

So MSP and PRIMASK *values* diverge between sides — NOT an MRS-execute bug.
QEMU presumably initialises MSP=0x40000 and PRIMASK=1 from its boot stub;
our emulator starts with both zero. Likely a harness setup gap (MSP/
PRIMASK aren't sync'd to the EMU side before the test executes), not a
real M33 emulator regression. Need to confirm.

Repro: `qemu_diff_m33 --fuzz 30000 --seed 2770223442767 --classes=all`.

Investigation agent dispatched in background (`general-purpose`, id
`ad05f3fbaa5657115`) to identify root cause + scope (harness vs emulator
vs test-class scoping). Agent has READ-ONLY mandate — no code changes,
no fuzzer launches, no rebuilds. Window: 15–25 min.

probe-watch polls 7+8 (02:39, 02:54, 03:09) — three more polls, all
three still wedged. 8 total polls, zero recoveries.

### 2026-05-03T03:22 — agent report: known harness gap, NOT a regression

Investigation agent returned in ~3 min with a clean diagnosis. **Root
cause: harness bug, sub-class missing-port-of-fix.** The 3040 failures
are *spurious* — not emulator divergences, but QEMU's persistent CPU
state leaking across tests because the M33 oracle has no PRIMASK/MSP
reset primer.

**Why "M0PLUS" appears in M33 log:** the class is generated by
`thumb32_gen::generate_fuzz_m0plus_t32` and folded into the shared
`base_alu` bucket at `crates/picoem-harness/src/lib.rs:4999`. Both
oracles consume the same bucket — intentional cross-coverage of the
M0+ Thumb-32 subset on M33. Class name is just where the cases were
authored.

**Where sync happens (or doesn't):**
- M0+ oracle steps a primer `MSR PRIMASK, R0` (R0=0) before every test
  at `crates/picoem-harness/src/bin/qemu_diff_m0plus.rs:386-401`.
- M33 oracle has *no equivalent*. Per-test setup at
  `crates/picoem-harness/src/bin/qemu_diff_m33.rs:457-464` writes only
  R0–R12, SP, LR, PC, XPSR. PRIMASK and the banked MSP/PSP carry over
  from the previous test's QEMU state. Our emulator builds a fresh
  `CortexM33::new()` per test, so the divergence is structural.

**Regression status:** Pre-existing gap. Commit `d29dc41` (2026-05-01)
added the primer to `qemu_diff_m0plus.rs` only; the same fix never got
ported to the M33 oracle. The 2026-05-01 campaign journal already
classified `M0PLUS_T32_MRS` as a *known deterministic baseline* and
flagged not to count it as a novel divergence. So this overnight run
re-discovered an already-tagged issue.

**Fix direction (deferred to daytime):** port the primer pattern from
`qemu_diff_m0plus.rs` to `qemu_diff_m33.rs`, but the M33 needs more
than just PRIMASK — also FAULTMASK, BASEPRI, CONTROL, banked MSP/PSP.
That's six MSR encodings and CONTROL changes affect privilege /
SP-bank, with risk of breaking other M33 classes if botched. **Not
appropriate to dispatch unsupervised at 03:30 with Arthur asleep.**
Recording it here and leaving the fix to a daytime session.

**Decision:** allow the campaign to continue producing the same 3040
known-spurious failures on each M33 batch, plus useful signal on every
other class. Adjusted M33 failure tally going forward will subtract
the M0PLUS_T32_MRS contribution.

### 2026-05-03T03:47 — wake #6 (T+2h39): no new classes failing

| Oracle | Batches | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 0 (mid batch 1, ~99.7%) | 1216K / 1219896 | 3040 | **0** (M0PLUS_T32_MRS subtracted) |
| qemu_diff_m0plus | 1 | mid batch 2 (590K / 687583, ~86%) | 0 | 0 |
| qemu_diff_riscv32 | 22 | mid batch 23 (25K / 30K) | 0 | 0 |

M33 still failing exactly the predicted set — class-failure histogram
confirms zero leakage into other classes. probe-watch polls 9+10
(03:24, 03:39) — both still wedged (10 polls, 0 recoveries).

### 2026-05-03T04:18 — wake #7 (T+3h10): batches rolling, all classes clean

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 1 (rc=1, 3040 M0PLUS_T32_MRS) | mid batch 2 (272K / 1219852) | 3040 | **0** |
| qemu_diff_m0plus | 2 (both rc=0) | mid batch 3 (145K / 687673) | 0 | 0 |
| qemu_diff_riscv32 | 27 | mid batch 28 (13K / 30K) | 0 | 0 |

M33 batch 1 closed at 03:47:48 (2h39m runtime). Batch 2 started fresh
seed `27947595628821`. The MRS class doesn't fire until ~760K of the
corpus; batch 2 is pre-MRS, so the 3040 has not yet reproduced — will
confirm in next wake.

M0+ steady — batches 1 (179501348328821) and 2 (470917122338) both
clean to completion. Batch 3 in flight.

probe-watch polls 11+12 (03:54, 04:09) — both still wedged.

Cumulative cases at this point: ~3.8M (M33 1.49M + M0+ 1.52M + RV32
0.82M). Aggregate ~20K cases/min, in line with the 04-28 reference.

### 2026-05-03T05:05 — wake #8 (T+3h57): all healthy, determinism check still pending

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 1 | mid batch 2 (642K / 1219852, ~53%) | 3040 | **0** |
| qemu_diff_m0plus | 2 | mid batch 3 (558K / 687673, ~81%) | 0 | 0 |
| qemu_diff_riscv32 | 34 | mid batch 35 (15K / 30K) | 0 | 0 |

M33 batch 2 still pre-MRS class (fires at ~763K, currently 642K).
Throughput recap by batch:
- M33 batch 1: 1.22M cases / 159 min = 7.66K/min
- M33 batch 2 so far: 642K / 77 min = 8.34K/min (slight uptick)

probe-watch polls 13+14+15 (04:24, 04:39, 04:54) — all still wedged
(15 polls, 0 recoveries — matches 04-28 incident profile exactly).

Cumulative cases: ~4.86M (M33 1.86M + M0+ 1.93M + RV32 1.07M).

### 2026-05-03T05:37 — wake #9 (T+4h29): determinism check — interesting variance

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 1 | mid batch 2 (893K / 1219852, ~73%) | 5977 | **0** |
| qemu_diff_m0plus | 3 (all rc=0) | mid batch 4 (113K / 687483) | 0 | 0 |
| qemu_diff_riscv32 | 39 | mid batch 40 (8K / 30K) | 0 | 0 |

**Determinism check result:** batch 2 contributed **2937** MRS failures
(cumulative 5977), **not** 3040 (which would have given 6080). The 103-
case variance is *not* a contradiction of the diagnosis — it's actually
strong evidence *for* it. If the bug were a deterministic emulator
decode mistake, every batch with `--classes=all` and the same corpus
generator would produce identical fail counts regardless of seed (the
MRS tests are per-test register/SYSm tuples; their decode is independent
of preceding tests). The fact that the count varies confirms the bug is
**path-dependent state leakage in QEMU**: PRIMASK and MSP are whatever
the *previous test* left them as, and a different test ordering
(different seed) leaves them in different states when MRS reads them.

Failure class still exclusively `M0PLUS_T32_MRS` — zero leakage into
other M33 classes. The structural gap is scoped exactly where the
agent identified it.

M0+ continues clean across 3 closed batches and into batch 4. RV32
40th batch in flight, no failures.

probe-watch polls 16+17 (05:09, 05:24) — both still wedged. 17 polls
total, 0 recoveries. (04-28 incident reached 95 polls / 0 recoveries
over 24h, so this is fully on script.)

Cumulative cases: ~5.46M (M33 2.11M + M0+ 2.17M + RV32 1.18M).
Aggregate ~20.3K/min sustained.

### 2026-05-03T06:09 — wake #10 (T+5h01): no surprises

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 1 | mid batch 2 (1127K / 1219852, ~92%) | 5977 | **0** |
| qemu_diff_m0plus | 3 | mid batch 4 (394K / 687483, ~57%) | 0 | 0 |
| qemu_diff_riscv32 | 44 | batch 45 just started | 0 | 0 |

M33 batch 2 ~99 min from completion. Still exclusively `M0PLUS_T32_MRS`.

probe-watch polls 18+19 (05:40, 05:55) — both still wedged. 19 polls
total, 0 recoveries.

Cumulative cases: ~6.13M (M33 2.35M + M0+ 2.45M + RV32 1.34M).

### 2026-05-03T06:40 — wake #11 (T+5h32): batch rollovers, all healthy

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 2 (batches 1+2 rc=1) | mid batch 3 (157K / 1220007, ~13%) | 5977 | **0** |
| qemu_diff_m0plus | 3 | mid batch 4 (643K / 687483, ~94%) | 0 | 0 |
| qemu_diff_riscv32 | 48 | mid batch 49 (23K / 30K) | 0 | 0 |

M33 batch 2 closed at 06:22:36 with 2937 MRS failures (path-dependent
as expected). Batch 3 started immediately on a fresh seed, currently
pre-MRS (class fires ~63% through corpus).

M33 batch 2 throughput: 1.22M / 154 min = 7.92K/min.
M0+ aggregate: ~8.13K/min, RV32: ~4.4K/min. Aggregate ~20.5K/min.

probe-watch polls 20+21 (06:10, 06:25) — both still wedged. 21 polls,
0 recoveries.

Cumulative cases: ~6.81M (M33 2.51M + M0+ 2.74M + RV32 1.46M). Run
remaining: 2h13m. Projected finish total: ~9M cases.

### 2026-05-03T07:11 — wake #12 (T+6h03): batches 3/5/54

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 2 | mid batch 3 (430K / 1220007, ~35%) | 5977 | **0** |
| qemu_diff_m0plus | 4 (all rc=0) | mid batch 5 (221K / 687623, ~32%) | 0 | 0 |
| qemu_diff_riscv32 | 53 | mid batch 54 (17K / 30K) | 0 | 0 |

M0+ batch 4 closed at 06:45:55 (rc=0). Four consecutive clean batches.
M33 batch 3 will hit the MRS class around 07:53 — third determinism
sample point.

probe-watch polls 22+23 (06:55, 07:10) — both still wedged. 23 polls,
0 recoveries.

Cumulative cases: ~7.21M (M33 2.78M + M0+ 2.97M + RV32 1.61M). Run
remaining: 1h42m.

### 2026-05-03T07:43 — wake #13 (T+6h35): just below MRS-class threshold

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 2 | mid batch 3 (692K / 1220007, ~57%) | 5977 | **0** |
| qemu_diff_m0plus | 4 | mid batch 5 (525K / 687623, ~76%) | 0 | 0 |
| qemu_diff_riscv32 | 58 | mid batch 59 (17K / 30K) | 0 | 0 |

M33 batch 3 about to hit the MRS class (fires ~763K). Will sample the
third determinism point next wake. probe-watch polls 24+25 (07:25,
07:40) — both still wedged. 25 polls, 0 recoveries.

Cumulative cases: ~7.55M (M33 3.04M + M0+ 3.27M + RV32 1.74M). Run
remaining: 1h10m.

### 2026-05-03T08:09 — wake #14 (T+7h01): determinism sample 3, M0+ five-clean

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 2 | mid batch 3 (911K / 1220007, ~75%) | 7436 | **0** |
| qemu_diff_m0plus | 5 (all rc=0) | mid batch 6 (45K / 687426) | 0 | 0 |
| qemu_diff_riscv32 | 62 | mid batch 63 (25K / 30K) | 0 | 0 |

**Determinism samples on M0PLUS_T32_MRS class:**
- Batch 1 (seed `2770223442767`): 3040 failures
- Batch 2 (seed `27947595628821`): 2937 failures
- Batch 3 (seed unknown — in flight, partial): **1459** as of 911K mark

Three samples → 3040 / 2937 / 1459, mean ~2479, σ ~728. The substantial
variance (38% spread between batches) is dispositive: a deterministic
emulator-decode bug would produce identical counts across batches.
Path-dependent QEMU-state leakage matches exactly.

M0+ five consecutive clean batches:
- batch 5 closed at 08:04:14 (rc=0)
- 5 × 687K ≈ 3.44M cases, 0 failures across the whole window.

probe-watch polls 26+27 (07:25→already in last update, 07:40, 07:55) —
both still wedged. 27 polls, 0 recoveries.

Cumulative cases: ~8.05M (M33 3.26M + M0+ 3.48M + RV32 1.88M). Run
remaining: 44 min — both M33 batch 3 and M0+ batch 6 will run past
the 08:53 deadline (wrappers only check before starting a new batch).

### 2026-05-03T08:36 — wake #15 (T+7h28): pre-deadline, all stable

| Oracle | Batches done | In flight | Failures (raw) | Failures (adjusted) |
|---|---|---|---|---|
| qemu_diff_m33   | 2 | mid batch 3 (1117K / 1220007, ~92%) | 7436 | **0** |
| qemu_diff_m0plus | 5 | mid batch 6 (276K / 687426, ~40%) | 0 | 0 |
| qemu_diff_riscv32 | 66 | mid batch 67 (28K / 30K) | 0 | 0 |

probe-watch polls 28+29 (08:10, 08:26) — both still wedged. 29 polls,
0 recoveries.

Cumulative cases: ~8.36M (M33 3.47M + M0+ 3.71M + RV32 1.99M). 17 min
to deadline.

### 2026-05-03T08:58 — wake #16 (T+8h05): deadline crossed; final state

**Wrappers exited cleanly:**
- `probe-watch.sh` — exited 08:53:13 with `deadline reached`. **30 polls,
  0 recoveries** over the full 7h45 window.
- `run-riscv32.sh` — exited 08:58:13. **70 batches × 30K = 2.10M cases,
  0 failures**. RC summary: all rc=0.
- `run-m33.sh` — wrapper started a fourth batch at 08:52:32 (40s before
  deadline), which would have run until ~11:30. **Killed batch 4 binary
  with SIGTERM** (rc=143) at 08:59:48 to honor the 8h window — wrapper
  immediately checked deadline, saw it passed, and exited at 08:59:48.

**Still running:**
- `run-m0plus.sh` — batch 6 in flight (487K / 687426, ~71%). Started
  08:04:14, has been running 54+ min, will complete naturally around
  09:24 (~26 min from now). 5 prior batches all rc=0.

**M33 final tally:**
- Batch 1 (seed `2770223442767`):    1.22M cases, 3040 MRS-class FAILs
- Batch 2 (seed `27947595628821`):   1.22M cases, 2937 MRS-class FAILs
- Batch 3 (seed `14105290643215`):   1.22M cases, **1459** MRS-class FAILs
  (final — confirmed; nothing changed between partial 1459 reading and
  batch close)
- Batch 4 (seed `177722070324680`):  killed at 57K (4.7%), 0 failures
  observed pre-MRS-class

Three full M33 batches × 1.22M = 3.66M cases. Adjusted failures = 0.

**M0PLUS_T32_MRS variance summary (3 samples):** 3040 / 2937 / 1459.
Mean 2479, σ 728 (CoV 29%). Variance dispositively confirms
path-dependent QEMU-state leakage (deterministic decode bugs would give
identical counts).

Cumulative cases at 08:58 deadline (M0+ still running):
- M33: 3.66M (3 full batches + 57K of batch 4 killed) → ~3.72M
- M0+: 5 × 687K + 487K = ~3.92M
- RV32: 70 × 30K = 2.10M
- **Total ≈ 9.74M cases**, all classes adjusted = **0 real failures**

When M0+ batch 6 closes (~09:24), final M0+ tally will be 6 × 687K =
~4.12M cases, total ~9.94M.

### 2026-05-03T09:32 — wake #17 (T+8h39): close-out

M0+ batch 6 closed at 09:24:25 with rc=0 (`687426 cases, 0 failures`).
Wrapper exited at 09:24:26 with `=== M0+ deadline reached ===`. **All
fuzz processes have ended; no orphan binaries.**

## Final tallies

| Oracle | Batches | Cases | Real failures | Known-spurious |
|---|---|---|---|---|
| qemu_diff_m33 (RP2350 ISA) | 3 full + batch 4 killed at 57K | 3,716,755 | **0** | 7436 (M0PLUS_T32_MRS, three samples) |
| qemu_diff_m0plus (RP2040 ISA) | 6 (all rc=0) | 4,125,326 | **0** | 0 |
| qemu_diff_riscv32 (Hazard3) | 70 (all rc=0) | 2,100,000 | **0** | 0 |
| **Total** | | **9,942,081** | **0** | 7436 |
| `probe_diff_rp2350` (RP2354 silicon) | — | 0 | — | — (probe wedged) |
| `probe_diff_rp2040` (RP2040 silicon) | — | 0 | — | — (probe wedged) |
| `test_silicon` (orchestrator) | — | 0 | — | — (probe wedged) |

Aggregate ~21 K cases/min over the 7h45 effective window, in line with
the 04-28 reference of ~24 K/min for the same three QEMU oracles.

## Hardware coverage

**Zero silicon coverage.** Both probes' WinUSB endpoints were wedged at
launch (`Could not determine a suitable packet size for this probe` —
canonical fingerprint). `probe-watch.sh` polled every 15 min for the
full window: **30 polls, 0 recoveries**. PnP confirmed all six
interfaces `Status: OK`, but the kernel-level visibility doesn't reach
the userland WinUSB endpoint claim. The 04-28 incident produced the
same result over 24h / 95 polls; documented recovery is physical
unplug+replug only.

## Spurious-failure analysis (M33 only)

`M0PLUS_T32_MRS` class on `qemu_diff_m33`:

| Batch | Seed | FAILs |
|---|---|---|
| 1 | `2770223442767` | 3040 |
| 2 | `27947595628821` | 2937 |
| 3 | `14105290643215` | 1459 |

Mean 2479, σ 728, CoV 29% — high variance dispositively confirms
**path-dependent QEMU state leakage**, not an emulator decode bug.
Diagnosis (agent `ad05f3fbaa5657115`, see 03:22 entry): the M33 oracle
lacks the per-test PRIMASK/MSP reset primer that `qemu_diff_m0plus`
got in commit `d29dc41` (2026-05-01); QEMU's persistent CPU state
(PRIMASK, banked MSP/PSP, FAULTMASK, BASEPRI, CONTROL) carries across
tests on the QEMU side, while our emulator builds a fresh
`CortexM33::new()` per test. The `MRS Rd, PRIMASK` and `MRS Rd, MSP`
diffs are the symptom; the test ordering changes per seed → different
leaked state → different fail counts. The 2026-05-01 campaign journal
already classifies this class as a known deterministic baseline.

## Daytime follow-up

1. **Port the per-test reset primer** from
   `crates/picoem-harness/src/bin/qemu_diff_m0plus.rs:386-401` to
   `crates/picoem-harness/src/bin/qemu_diff_m33.rs` (currently only
   resets R0–R12 + SP + LR + PC + XPSR at lines 457-464). The M33
   needs more than just `MSR PRIMASK`: also FAULTMASK, BASEPRI,
   CONTROL (privilege + SP-bank), banked MSP and PSP. Six MSR
   encodings; risk of breaking other M33 classes if botched, so do
   under daytime supervision with a small fuzz before promoting to
   the overnight driver.

2. **Probe wedge recovery.** The 04-25 → 04-28 → 05-03 pattern is now
   a recurring infrastructure issue. Worth a short investigation into
   whether a service-level fix is possible (probe-rs version bump?
   a userland endpoint-reset shim?) — physical replug across multiple
   incidents is signalling a real bug, not an environment quirk.
   This run lost ~6h of expected silicon coverage to it.

3. **M33 wrapper deadline check.** The `run-m33.sh` while-loop checks
   the deadline *before* starting a new batch but doesn't compute
   "remaining time vs expected batch duration". Tonight it kicked off
   batch 4 with 40s of headroom; without manual intervention that
   would have run 2.5h past the 8h boundary. A small fix:
   `if [ "$REMAINING" -lt 60 ]; then break; fi` (the test_silicon
   driver already does this — see `run-test-silicon.sh:46-50`).

## Conclusion

**~10M cases, 0 real failures across three QEMU oracles in 8 hours.**
Silicon side wedged from launch and stayed wedged — recovered
gracefully via QEMU pivot. The one M33 failure spike was a
pre-existing harness gap re-discovered against a known-tagged
baseline; no emulator regression observed.

## Daytime resolution (2026-05-03 PM)

Worked through the three follow-ups above with Arthur after the run.

### Follow-up 1 — primer port: already done (with surprise)

The primer was already committed by Arthur in `377ed6e`
(2026-05-02 15:59 BST) on origin/main, but did **not** reach local
main until the merge at `10c8c25` (2026-05-03 11:11 BST) — *after*
the overnight binary had been built (00:25 BST). The investigation
agent at 03:22 read the pre-merge working tree and correctly
reported "no primer," which is what made the journal recommend the
port. Mystery resolved: the agent was right about the binary that
ran tonight, the journal was wrong about HEAD.

The committed primer covers PRIMASK / BASEPRI / FAULTMASK only.
Validation re-run with the post-merge binary on the same seed
`2770223442767`:

| Run | Cases | FAILs | Rate |
|---|---|---|---|
| Overnight (pre-primer binary) | 1.22M `--fuzz 30000 --classes=all` | 3040 | 0.249% |
| Daytime confirmation (post-primer binary) | 173K `--fuzz 5000 --classes=base` | 248 | 0.143% |

Exactly matches the rate the `377ed6e` commit message predicted
("drops … to 0.143%"). The 248 residual is the **separate**
`MRS Rd, MSP/PSP` gap the commit message itself flags. Two
underlying causes compound here:

1. **EMU side:** `set_reg(13, EMU_TEST_STACK)` only updates
   `regs.r[13]` — it does not sync the banked `regs.msp`. The MRS
   instruction reads `regs.msp` directly
   (`crates/rp2350-emu/src/core/execute_thumb32.rs:1168`). After
   harness setup, `regs.msp` is still 0 from `CortexM33::new()`,
   so `MRS Rd, MSP` returns 0. The `regs.msp` cache only gets
   sync'd at exception entry/exit via `sync_sp_to_banked()` —
   architecturally fine for normal program execution (which never
   observes the staleness), but the fuzzer issues MRS in
   contexts the architecture doesn't normally reach.
2. **Harness side:** Even if (1) were fixed, `MRS Rd, MSP` would
   still diverge because EMU and QEMU use different stack
   addresses (`EMU_TEST_STACK = 0x2004_0000` vs
   `QEMU_TEST_STACK = 0x0004_0000`) and the comparator does
   absolute compare for R0-R12 (no addr_reg translation for
   sysm=8/9). On the M0+ oracle this is invisible because both
   sides share `0x2000_1000` — that's why the M0+ MRS class is
   clean.

**Fix direction (deferred):** smallest correct fix is two-part —
(a) `MRS` reads from `r[13]` when the requested bank is active
(architecturally correct: when SPSEL=0, MSP *is* SP; trivially
correct, single match-arm), and (b) test-case generator marks Rd
as an address register when sysm ∈ {8, 9} so the comparator
translates. Estimated ~30 min of work + a 100K-case soak.
**Recording as a new follow-up (item 4 below) rather than
attempting tonight** — it's the actual remaining blocker on M33
ISA cleanliness, and the journal's "extend the primer" framing
was insufficient.

### Follow-up 2 — probe-wedge investigation: complete

Investigation agent found the root cause is upstream **probe-rs
issue [#1849](https://github.com/probe-rs/probe-rs/issues/1849)**,
open in our pinned 0.31.0. Wedge happens deterministically when a
prior process exits without calling `DAP_Disconnect` (panic, kill
-9, or our 04-25 cascade). `nusb`'s `claim_interface` succeeds
(kernel-level — that's why `probe-rs list` and PnP look healthy),
but the first OUT/IN exchange in `find_packet_size`
(`third_party/probe-rs-0.31.0-mdrp-patched/src/probe/cmsisdap/commands/mod.rs:307-335`)
times out 16× because the WinUSB bulk endpoint is left in a stuck
or HALT state. Probe firmware is fine; it's the host-side endpoint
state that needs reset.

Three remediation options ranked by feasibility:

1. **`pnputil /restart-device` from a one-shot elevated PowerShell**
   (lowest risk, requires UAC click once per night, ~1h to wire
   into `probe-watch.sh`). Likely high probability of working
   because re-enumeration is effectively a soft replug.
2. **Vendor a `clear_halt(out_ep)` + `clear_halt(in_ep)` patch**
   into `third_party/probe-rs-0.31.0-mdrp-patched/`'s
   `CmsisDapDevice::V2` open path, right before the existing
   `drain()` (~20 lines, no admin needed, mirrors the existing
   `select_ap_and_ap_bank` patch we already maintain). Could
   upstream as the fix for #1849 if it works.
3. **`nusb::Device::reset()` shim** wrapper at startup of every
   `probe_diff_*` / `test_silicon` invocation (issues a USB port
   reset via the Windows kernel, no admin). Higher complexity
   risk.

**Recommended first action next time the probes wedge:** try
option (1) with the one-liner

```powershell
Get-PnpDevice -PresentOnly | Where-Object { $_.InstanceId -match 'VID_2E8A&PID_000C' } |
  ForEach-Object { pnputil /restart-device $_.InstanceId }
```

from an elevated PowerShell. If it recovers them, wire into the
watcher. If it doesn't, that disproves the WinUSB-stall hypothesis
and points at probe firmware (which truly needs replug) — useful
diagnostic either way. Existing `RUNBOOK.md` wedge section (lines
267-353) stays as the fall-back recipe.

### Follow-up 3 — wrapper deadline check: applied to all three batch wrappers

`fuzz-runs/run-m33.sh`, `run-m0plus.sh`, and `run-riscv32.sh` all
shared the same loop pattern; the M33 just happened to be the one
that triggered tonight. Applied the journal's recommended
`if [ "$REMAINING" -lt 60 ]; then break; fi` guard to all three,
mirroring `run-test-silicon.sh:46-50`. Each wrapper now logs the
remaining-time at batch start and skips the final batch if under
60s of headroom remain. Shell syntax verified with `bash -n`.

### New follow-up 4 — close the M33 MRS MSP/PSP residual

See item 1 above. Two-part fix, ~30 min plus soak. Drops the M33
fuzz failure rate from 0.143% → 0%, which would let the overnight
M33 driver run to a clean rc=0 instead of the perpetual rc=1 that
masks any future genuine regression in the same noise.

## Repo state at close-out

No commits made; three modified shell scripts and a journal update
unstaged. Arthur's call on whether to commit + push.
