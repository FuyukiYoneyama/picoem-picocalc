# 2026.04.18 — JRN — Coverage Gap Fill V12 Supervisor

Supervisor journal for the V12 silicon-residual closure
(HLD: `wrk_docs/2026.04.18 - HLD - RP2350 Coverage Gap Fill V12.md`).

## Approach

Same agent-team pattern as V11 (implementer → reviewer →
apply-feedback → devil's-advocate → commit). V12 is a small
campaign — two in-tree items + one best-effort smoke fix.

Order per HLD §6: SYSINFO → POWMAN INTE → smoke best-effort.

Baseline: `mdrp2350` 1.13.0 (post-V11 Stage 5 commit `8112b17`).

## Stage log

### Stage 1 — SYSINFO CHIP_ID literal (done, commit `07545dc`)

- Implementer changed `bus/peripherals.rs::sysinfo_read` offset
  0x00 from `0x0000_524D` → `0x0004_0927`, updated the named
  unit test, bumped Cargo to 1.14.0.
- Reviewer/DA verdict GREEN; SHOULD-fix was a comment-accuracy
  tweak (V11's `0x524D` came from composing SDK field constants
  with an erroneous MANUFACTURER shift, not from a header
  literal — rephrased in place before commit).
- **In-flight discovery**: `cargo test --release` surfaced a V11
  Stage 4 regression — `powman_archsel_non_arm_write_fires_tripwire_once`
  asserted on a `trace!`-level capture, which
  `release_max_level_one=info` compiles out in `--release`. Test
  greened in debug and reddened in release. Fixed inline by
  dropping the capture assertion; the `warned_archsel` struct
  flag (the actual one-shot tripwire) still drives the test.
  Lesson: V11's warn→trace downgrade should have come with a test
  update. Added to the stage commit to land a clean tree.
- Cargo 1.13.0 → 1.14.0. Release lib tests: 1267 pass / 0 fail.

### Stage 2 — POWMAN INTE gating (done, commit `283920f`)

- Implementer changed `advance` to gate the NVIC return on
  `(self.inte & INT_TIMER_BIT) != 0` while keeping INTR.TIMER
  and TIMER.ALARM unconditional latches; changed `write32`
  signature to `#[must_use] -> u64` so writes that transition
  `(INTR & INTE) | INTF` 0→1 on TIMER (late INTE set, direct
  INTF force) propagate the level-sensitive raise; added a
  private `ints_timer_asserted` helper.
- Bus call sites consume the new return via `raise_irqs_u64`.
  Single bus write site, single advance site — no broader
  refactor needed.
- Silicon scenario `MAIN_IRQ_POWMAN` grew 32→40 halfwords:
  three new instructions at hw[9..11] write
  `POWMAN_INTE = 0x5AFE_0002` before NVIC_ISER1 enable; new
  literal-pool entries at hw[36..39]; all 8 LDR encodings
  recomputed (`Align(PC+4, 4) + imm8*4`). New regression test
  `test_main_powman_inte_literals_are_inte_addr_and_value`
  decodes the assembled image to lock-in the encoding.
- Reviewer/DA: GREEN with one SHOULD-fix — INTE/INTF were not
  password-stripped, leaving stored `0x5AFE_*` upper bits that
  would diverge from silicon on a firmware read. Applied inline:
  `value & 0xFFFF` in both write paths (same shape as TIMER /
  SET_TIME / ALARM_TIME password handling).
- Cargo 1.14.0 → 1.15.0. Release: 1270 passed / 0 failed
  (13 powman tests + 379 harness tests).

### Stage 3 — Silicon validation + audit + smoke best-effort

Run against live RP2354 (probe `<RP2354 probe serial>`).

| Scenario | Verdict | Notes |
|---|---|---|
| `sysinfo_readonly_fields` | PASS (after V12 Stage 3 in-stage fix) | First run still FAILed because Stage 1's `0x0004_0927` was wrong: PART value 4 occupies *bits [14:12]* not [16:14], so the contribution is `0x4 << 12 = 0x4000` not `0x4 << 16 = 0x40000`. Silicon literally returns `0x0000_4927`. Fixed in-stage; second run PASS. Then PLATFORM diverged next: silicon returns 0, our emulator returned 1 (V11 Stage 1's "ASIC bit 1 set" guess). Fixed PLATFORM → 0; third run PASS. |
| `tbman_platform_reads_silicon_value` | PASS (re-confirmed) | Untouched since V11. |
| `glitch_detector_arm_readback_tracks_ctrl` | PASS (re-confirmed) | Untouched since V11. |
| `isr_powman_match_timer_line_45` | PASS | V12 Stage 2's INTE-gating + scenario INTE write closes the V11 Stage 6 timeout. Handler runs; mailbox = `0xCAFE_BABE`. |

**§7 acceptance criteria** (V12 HLD):
1. ✅ `sysinfo_readonly_fields` PASSes on Arthur's RP2354.
2. ✅ `isr_powman_match_timer_line_45` PASSes on Arthur's RP2354.
3. ✅ All three new POWMAN INTE unit tests pass (in `powman.rs::tests`).
4. ✅ No regressions in V11-PASSed scenarios (TBMAN/GLITCH re-confirmed).
5. ✅ `cargo build --release` clean, `cargo test -p mdrp2350 --release` 1270 passed / 0 failed.
6. ✅ Coverage matrix updated (4 rows annotated `silicon = PASS`).

**Smoke binary best-effort** (HLD §3.3): added
`TIMER_USE_LPOSC_BIT` (bit 8) to the TIMER write per pico-sdk
`powman.h` field decode. Same fault remains
(`fatal: An ARM specific error occurred`) at the first
`read_word_32(DWT_CYCCNT)` after `core.run()`. Hypothesis is
now probe-rs run-mode-read on RP2354 — not POWMAN-specific,
since the same probe sequence works against silicon in the
silicon_periph_diff / silicon_isr_diff oracles (those use
`halted_*` reads under the hood). Per HLD §3.3 best-effort,
deferring the smoke binary fix to V13. Suggested V13 approach:
rework smoke to halt-read-resume around each sample point, or
poll POWMAN COUNT via probe-rs while keeping the core halted
(POWMAN COUNT advances independent of core run state).

**Lessons from V12**:
- *Iterative silicon discovery is the value of the oracle.*
  V12 Stage 1 fixed CHIP_ID and that exposed PLATFORM as the
  next residual — both got fixed in-stage. The V11 Stage 6
  diff stopped at the first divergence; subsequent oracle
  runs were needed to surface the next layer.
- *Bit-shift math discipline.* PART occupies bits [14:12];
  V12 Stage 1 mis-placed it at bits [18:16]. The silicon
  measurement is the only ground truth — header literals
  encode field constants, not register layouts. Always
  cross-check `(field_value << field_lsb)` produces the
  silicon-observed contribution.
- *Run-mode probe reads aren't free on RP2354.* Smoke
  binaries that use `core.run()` then immediately
  `read_word_32` may fault. Use halt-read-resume cadence
  instead. Add to runbook for V13+ smoke binaries.

## Deferred to V13

- Smoke `smoke_powman_pacing_rp2350.rs` halt-read-resume
  rework (HLD §3.3 best-effort failed — probe-rs run-mode
  read fault).
- BADPASSWD detection on POWMAN wrong-password writes
  (carried from V11 Stage 4 DA).
- POWMAN comparator `>=` vs `==` semantics (carried).
- POWMAN SET_TIME_* latched-commit semantics (carried).
- Peripheral reset via RESETS_RESET bit toggling (codebase-
  wide; carried from V11).
- POWMAN scenario CYCCNT-delta observable (carried).

