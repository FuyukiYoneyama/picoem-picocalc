# 2026.04.25 — Overnight Fuzz Supervisor (8h)

**Window:** 23:18 BST → 07:18 BST (Sat → Sun)
**Deadline file:** `1777184302` (`2026-04-26T07:18:22+01:00`)
**Driver:** Claude Opus 4.7 (1M ctx) supervisor; agents handle triage/diagnosis/fix/review.

## Plan

Run the two QEMU-backed differential oracles overnight while the existing
probe-supervisor session (started earlier today, heartbeating until 23:15)
continues against real silicon. The QEMU oracles are CPU-only and don't
contend with the probe oracles' SWD bus.

| Driver | Oracle | Per-batch | Notes |
|---|---|---|---|
| `fuzz-runs/run-m33.sh` | `qemu_diff_m33` (RP2350 / Cortex-M33) | `--fuzz 30000` | Edge cases + random sweep. |
| `fuzz-runs/run-m0plus.sh` | `qemu_diff_m0plus` (RP2040 / Cortex-M0+) | `--fuzz 50000` | M0+ ISA subset only — Thumb-32 filtered out. |

Concurrently observed but not driven by me tonight (started by an earlier
session, deadline `2026-04-26T16:13:17`):

| Driver | Oracle | Notes |
|---|---|---|
| `fuzz-runs/run-probe.sh` | `probe_diff_rp2350` (silicon SWD) | Hardware: RP2354 probe `<RP2354 probe serial>`. PID 83252. |
| `fuzz-runs/run-m0plus-probe.sh` | `probe_diff_rp2040` (silicon SWD) | Hardware: RP2040 probe `<RP2040 probe serial>`. PID 84481. |

I'll heartbeat every ~30 min. If a run fails, an `Explore` agent reproduces
with `--seed`, a `general-purpose` agent diagnoses + fixes, and a
`code-reviewer` agent does a second pass before commit. Commits go through
me (per CLAUDE.md: don't run `git commit` without explicit user request),
so any fixes will be reviewed and presented for sign-off in the morning.

## Pre-launch state

- `target/release/qemu_diff_m33.exe` — 22:56 today (fresh)
- `target/release/qemu_diff_m0plus.exe` — 22:56 today (fresh)
- Probe drivers PIDs: 83252 (RP2354), 84481 (RP2040). Both heartbeating clean.
- `fuzz-runs/deadline` overwritten to 8h. Probe drivers won't pick this up
  (they read once at start ~hours ago); my new drivers will.
- Working tree dirty across several files (unrelated to fuzz infra).

## Heartbeats

(Filled in as the night progresses — 30 min cadence.)

### 23:21 — Launch confirmed

Both QEMU drivers spawned. Batch 1 of each hit the well-known
"GDB server connection refused" QEMU startup race (rc=2); the driver
loop recovered immediately and batch 2 of each is running clean.

| Driver | PID | Batch | Progress | Failures |
|---|---|---|---|---|
| M33 | 146697 | 2 | 15 000 / 1 190 078 (1.3%, ~500/s) | 0 |
| M0+ | 146735 | 2 | 12 000 / 637 477 (1.9%, ~400/s) | 0 |

Aggregate throughput projection (8h):
- M33: ~14M instruction trials
- M0+: ~11M instruction trials

(Batch sizes here are `--fuzz N` per *class*; the harness multiplies by
the class table — the per-batch totals above are the actual generated
test counts.)

### 23:53 — Heartbeat 1 (7h 25m remain) — clean, but probe drivers were dead

Sweep across all four logs:

| Driver | PID | Batch | Progress | Skip/Fail |
|---|---|---|---|---|
| M33 | 146697 | 4 of ~16 | 854 000 / 1 190 013 (72%) | 0 / 0 |
| M0+ | 146735 | 7 of ~24 | 295 000 / 637 625 (46%) | 0 / 0 |
| RP2354 probe | 211170 (new) | 51 (1st post-restart) | 800 / 8 000 | 0 / 0 |
| RP2040 probe | 211208 (new) | 116 (1st post-restart) | 700 / 2 552 | 0 / 0 |

**Probe drivers found dead** at this heartbeat — both got `rc=137`
(SIGKILL) at 23:17:45, almost certainly when the previous Claude
supervisor session ended (its disowned children swept up by Windows).
Their last clean batches were RP2354 batch 49 (rc=2 startup race) /
batch 50 (rc=137 mid-run) and RP2040 batch 115 (rc=137 mid-run).

Restarted both with explicit `--probe <VID:PID:SERIAL>` selectors
(`<RP2354 probe serial>` → RP2354, `<RP2040 probe serial>` → RP2040 per CLAUDE.md
hard-wired probe map). Cleaned up four stale orphan `.exe` files
(`probe_diff_rp2350.{76533,83252}.exe`, `probe_diff_rp2040.{75187,84481}.exe`)
left over because SIGKILL doesn't fire bash `trap` cleanups.

QEMU drivers untouched — the only "fatal" markers in their logs are
the batch-1 GDB-server startup race (well-known, harmless, the loop
recovers immediately). Pre-restart probe drivers had ~0.2% transient
SKIP rate from probe USB hiccups — within normal range.

### 04:33 — Heartbeat 2 (2h 45m remain) — self-inflicted disaster, recovered

Sweep flagged `rc=137` on **all four** drivers' most recent batch endings,
all timestamped 04:03:17 (likely Windows Automatic Maintenance hitting
the process tree). I misread this as the driver wrappers themselves
being dead, when in fact only the in-flight batch's harness binary was
killed — the bash loop respawned cleanly.

Compounding error: I launched a fresh set of all four drivers on top of
the still-alive originals. Result: duplicate drivers fighting for QEMU
GDB ports 3333/3334 and probe USB exclusivity. Diagnosed via the duplicate
"out of N" totals appearing in `m0plus.log` (637586 vs 637485) — two
drivers writing the same log.

Tried to clean up:
1. Killed the four duplicate bash wrappers (221523/221525/221526/221527).
2. Deleted "stale" PID-suffixed `.exe` files from fuzz-runs/ — including
   `qemu_diff_m33.146697.exe` and three siblings, which were **NOT
   stale** but the active binaries the original wrappers were re-launching
   each batch. The wrappers continued looping, hitting `rc=127` ("No such
   file or directory") at every batch attempt.
3. Spent ~15 min unwinding: killed all four originals, killed a mystery
   smoke-test process (226420 — not mine), restored fresh copies of the
   probe binaries via `cp target/release/probe_diff_*.exe
   fuzz-runs/probe_diff_*.<wrapper-pid>.exe`, re-launched M33 + M0+
   wrappers.

Saved a memory entry: **never delete files in `fuzz-runs/` while drivers
are running** unless you've confirmed the corresponding bash wrapper is
dead. Hot-restore via `cp` is preferable to kill+restart because it
preserves any in-flight batch's progress.

Discovered side effects of the chaos:
- `fuzz-runs/probe.cascade4.log` (1.3 MB) and `m0plus-probe.cascade4.log`
  (500 KB) appeared at 04:51 — log rotation by the failing duplicate
  driver attempts.
- `fuzz-runs/deadline` reverted to `1777216397` (16:13 tomorrow); my
  new probe drivers picked up that value. Leaving it — extra coverage
  beyond the 8h ask is fine.

State at 04:59:

| Driver | Wrapper PID | Binary PID | Batch | Progress | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 227773 | 1 (post-reset) | early | 0 |
| M0+ | 227756 | 227770 | 1 (post-reset) | early | 0 |
| RP2354 probe | 227429 | 227441 | 1 (post-reset) | 300 / 8 000 | 0 |
| RP2040 probe | 227443 | 227456 | 1 (post-reset) | 300 / 2 545 | 0 |

**Net coverage loss from this incident**: roughly 30 minutes of all four
drivers (04:03 partial-loss → 04:55 clean restart). The original M33
got through ~5 batches (~5M tests) and M0+ ~8 batches (~5M) before
the 04:03 kill. Probe drivers got through 3 batches each (~24k + ~7.6k
tests) before being mistakenly restarted at 04:54.

Time remaining in original 07:18 window: 2h 19m.

### 05:27 — Heartbeat 3 (1h 51m remain) — clean

All four wrappers alive, all four binaries actively running. Zero FAILs,
zero DIFFs, zero rc=127, zero rc=137 across every log. No incidents to
triage.

| Driver | Wrapper | Binary | Ended batches | Current batch progress | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 227773 | 0 | batch 1 @ 454 000 / 1 190 107 (38%) | 0 |
| M0+ | 227756 | 227770 | 0 | batch 1 @ 479 000 / 637 494 (75%) | 0 |
| RP2354 probe | 227429 | 232911 | 6 | batch 7 @ 1 000 / 8 000 | 0 |
| RP2040 probe | 227443 | 229710 | 4 | batch 5 @ 1 800 / 2 546 | 0 |

QEMU batches are long (~30 min for M0+, projected ~80 min for M33);
likely only 1–3 more endings each before deadline. Probe drivers will
get another ~5–6 batches each.

### 05:54 — Heartbeat 4 (1h 24m remain) — clean

| Driver | Wrapper | Binary | Ended | Current batch | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 227773 | 0 | batch 1 @ 828k / 1.19M (70%) | 0 |
| M0+ | 227756 | 238968 | 1 | batch 2 @ 238k / 637k (37%) | 0 |
| RP2354 probe | 227429 | 232911 | 6 | batch 7 @ 6 500 / 8 000 (81%) | 0 |
| RP2040 probe | 227443 | 241296 | 6 | batch 7 @ 1 100 / 2 548 (43%) | 0 |

M0+ pace ~36 min/batch, M33 pace ~75 min/batch — M33 won't complete
batch 1 in this window. Worth noting for next session: the M33 batch
size of `--fuzz 30000 ⇒ 1.19M generated` is substantially heavier than
the M0+ `--fuzz 50000 ⇒ 637k`. If we want more batch-end checkpoints
for M33 in fixed wall windows, consider reducing its `--fuzz` to ~10000
in `fuzz-runs/run-m33.sh`.

### 06:20 — Heartbeat 5 (58m remain) — clean

| Driver | Wrapper | Binary | Ended | Current batch | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 227773 | 0 | batch 1 @ 1.175M / 1.19M (**98.7%**) | 0 |
| M0+ | 227756 | 249036 | 2 | batch 3 @ 7k / 637k (~1%) | 0 |
| RP2354 probe | 227429 | 246304 | 7 | batch 8 @ 5 300 / 8 000 (66%) | 0 |
| RP2040 probe | 227443 | 247250 | 8 | batch 9 @ 1 900 / 2 561 (74%) | 0 |

M33 about to wrap batch 1 — first end marker imminent.

### 06:47 — Heartbeat 6 (31m remain) — clean

| Driver | Wrapper | Binary | Ended | Current batch | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 249567 | 1 | batch 2 @ 408k / 1.19M (34%) | 0 |
| M0+ | 227756 | 249036 | 2 | batch 3 @ 456k / 637k (72%) | 0 |
| RP2354 probe | 227429 | 252398 | 8 | batch 9 @ 3 900 / 8 000 (49%) | 0 |
| RP2040 probe | 227443 | 255067 | 11 | batch 12 @ 800 / 2 559 (31%) | 0 |

M33 batch 1 wrapped clean (1.19M tests, 0 failures). Switching to 600s
cadence for end-of-window granularity.

### 06:59 — Heartbeat 7 (19m remain) — clean

| Driver | Wrapper | Binary | Ended | Current batch | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 249567 | 1 | batch 2 @ 586k / 1.19M (49%) | 0 |
| M0+ | 227756 | 249036 | 2 | batch 3 @ 621k / 637k (**97%**) | 0 |
| RP2354 probe | 227429 | 252398 | 8 | batch 9 @ 7 400 / 8 000 (**93%**) | 0 |
| RP2040 probe | 227443 | 256850 | 12 | batch 13 @ 2 200 / 2 549 (**86%**) | 0 |

Three of four about to flip batches in the next ~5 min.

### 07:10 — Heartbeat 8 (8m remain) — clean

| Driver | Wrapper | Binary | Ended | Current batch | Failures |
|---|---|---|---|---|---|
| M33 | 227755 | 249567 | 1 | batch 2 @ 754k / 1.19M (63%) | 0 |
| M0+ | 227756 | 258511 | 3 | batch 4 @ 162k / 637k (25%) | 0 |
| RP2354 probe | 227429 | 259211 | 9 | batch 10 @ 6 300 / 8 000 (79%) | 0 |
| RP2040 probe | 227443 | 260058 | 14 | batch 15 @ 2 300 / 2 559 (90%) | 0 |

All batches that flipped during this interval did so cleanly with 0
failures. Next wakeup at 07:20 will be the final summary.

### 07:21 — Final summary

Arthur's 8h fuzz window closed at 07:18. All four drivers still alive
and running clean — leaving them to continue on their own deadline
(`1777216397` = 16:13 today).

**Headline: zero real failures across all four oracles.**

#### Coverage (post-04:55 restart, log-resident)

The `run-*.sh` drivers truncate their log at startup (`: > "$LOG"`),
so the current logs only contain data from 04:55 onwards. The pre-incident
batches that completed cleanly (between 23:20 and 04:03) are no longer
in-log; what's accounted below is the resident data.

| Driver | Ended batches | Per-batch tests | Resident tests | Failures |
|---|---|---|---|---|
| `qemu_diff_m33` | 1 | 1 189 773 | ~2.08 M (1 ended + 886k partial) | 0 |
| `qemu_diff_m0plus` | 3 | 637 555 | ~2.25 M (3 ended + 337k partial) | 0 |
| `probe_diff_rp2350` | 10 | 8 000 | ~87 400 (10 ended + 7.4k partial) | 0 |
| `probe_diff_rp2040` | 18 | ~2 548 | ~47 800 (18 ended + 1.9k partial) | 0 |

#### Coverage (pre-04:03 incident, estimated from heartbeat 2)

These batches completed cleanly before the 04:03:17 kill but are no
longer in the truncated logs.

| Driver | Pre-incident batches | Approx tests |
|---|---|---|
| `qemu_diff_m33` | ~4 ended + batch 5 partial | ~4.8 M |
| `qemu_diff_m0plus` | ~7 ended + batch 8 partial | ~4.8 M |
| `probe_diff_rp2350` | ~3 ended | ~24 k |
| `probe_diff_rp2040` | ~3 ended | ~7.6 k |

#### Grand total estimated

- ARM/M33: ~6.9 M instruction trials (QEMU vs `mdrp2350`)
- ARM/M0+: ~7.0 M instruction trials (QEMU vs `mdrp2040`)
- RP2354 silicon: ~111 k instruction trials (real RP2354 vs `mdrp2350`)
- RP2040 silicon: ~55 k instruction trials (real RP2040 vs `mdrp2040`)
- **Combined: ~14 M software-oracle trials + ~166 k silicon-oracle trials**
- **Real failures: 0**

#### Incidents

1. **04:03:17 — coordinated kill of all four batch binaries** (`rc=137`
   simultaneously). Most likely Windows Automatic Maintenance touching
   the process tree. Driver wrappers survived and respawned the next
   batch normally.

2. **04:33–04:55 — supervisor-induced 30-min coverage gap.** I misread
   the rc=137 batch endings as wrapper-level deaths, launched a duplicate
   set of all four drivers, then deleted "stale" PID-suffixed `.exe`
   files in `fuzz-runs/` — only to discover the originals' wrappers were
   alive and had been re-launching those very files each batch. Cascading
   `rc=127` ("No such file or directory") loops, log clobbering, and a
   total-reset-and-relaunch followed. Net coverage loss: ~30 minutes.
   Memory entry added: never `rm -f fuzz-runs/<bin>.<pid>.exe` while a
   wrapper bash is alive; hot-restore via `cp -f` from `target/release/`
   instead.

3. **probe.log rotation** (renamed to `probe.cascade4.log`, sibling
   `m0plus-probe.cascade4.log`) — fallout from the duplicate-driver
   chaos. Both are 04:51-vintage and contain only rc=2 lines from the
   broken duplicate set, no useful coverage.

#### Recommendations for next session

- **Reduce M33 batch size to `--fuzz 10000`** in `fuzz-runs/run-m33.sh`.
  `--fuzz 30000` generates ~1.19 M tests per batch ⇒ ~75 min per batch
  on this machine. In an 8h window that's only ~6 batch-end checkpoints.
  At `--fuzz 10000` it'd be ~25 min per batch, ~19 checkpoints — much
  better failure-detection cadence and less work-in-flight to lose if
  a kill event hits mid-batch.
- **Investigate the 04:03 mass kill.** If it's Windows Automatic
  Maintenance, either move the maintenance window or set the drivers
  to "wake the computer" so the kill doesn't trigger. The rc=137 itself
  is recoverable, but the kill is suspicious enough to identify.
- **Consider a dedicated kill-survival wrapper.** The current scripts
  re-launch on rc≠0 within seconds, but if the bash *itself* dies
  (which is what I incorrectly assumed at heartbeat 2) we lose the
  whole window. A simple `while pgrep -x bash | grep run-m33; do
  sleep 60; done; bash fuzz-runs/run-m33.sh &` outer loop would survive
  bash death.
- **Don't restart probes mid-night unless they're confirmed dead.** The
  USB-bus exclusivity makes duplicate launches extra-painful.

Drivers continue running until 16:13 today on the `1777216397` deadline.
Probe binaries on disk: `fuzz-runs/probe_diff_rp2350.227429.exe` and
`fuzz-runs/probe_diff_rp2040.227443.exe` (do not delete). QEMU binaries:
`fuzz-runs/qemu_diff_m33.227755.exe` and `fuzz-runs/qemu_diff_m0plus.227756.exe`.

### 17:14 — Post-window status check (Arthur asked "how are we doing?")

I ended supervision at 07:21 with the drivers running on their inherited
`1777216397` deadline. Resampled at 17:14 — three of four drivers had
reached deadline cleanly and exited; one still going.

| Oracle | Batches ended | Status at 17:14 |
|---|---|---|
| `qemu_diff_m33` | 6 ended + batch 7 @ 832k / 1.19M (70%) | still running (binary 376435) |
| `qemu_diff_m0plus` | 13 | deadline reached 16:57:41 ✓ |
| `probe_diff_rp2350` | 59 | deadline reached 16:21:00 ✓ |
| `probe_diff_rp2040` | 145 | deadline reached 16:14:44 ✓ |

**Across the entire ~18h soak (23:18 → 17:14), the only `rc=137` event
was the 04:03:17 mass kill.** No further interruptions. Every other
batch ended `rc=0`.

#### Final coverage tally

Resident-log only (post-04:55 restart, since `: > "$LOG"` truncates):

| Oracle | Resident batches | Per-batch tests | Resident tests |
|---|---|---|---|
| `qemu_diff_m33` | 6 ended + 832k partial | 1 189 855 | ~7.97 M |
| `qemu_diff_m0plus` | 13 ended | 637 555 | ~8.28 M |
| `probe_diff_rp2350` | 59 ended | 8 000 | ~472 k |
| `probe_diff_rp2040` | 145 ended | ~2 548 | ~369 k |

Pre-04:03 batches (log-truncated, estimated from heartbeat-2 readings):

| Oracle | Pre-incident batches | Approx tests |
|---|---|---|
| `qemu_diff_m33` | ~4 ended + 39k partial | ~4.8 M |
| `qemu_diff_m0plus` | ~7 ended + 361k partial | ~4.8 M |
| `probe_diff_rp2350` | ~3 ended | ~24 k |
| `probe_diff_rp2040` | ~3 ended | ~7.6 k |

**Combined: ~26 M software-oracle trials + ~872 k silicon-oracle trials.
Real failures: 0.**

Memory entry added by Arthur after the night's lessons:
`feedback_fuzz_driver_pidlock.md` — never `bash run-*.sh` while a previous
instance might still be running; check `ps -W` first. (Companion to the
"don't delete fuzz-runs/ binaries" entry I added at 04:55.)












