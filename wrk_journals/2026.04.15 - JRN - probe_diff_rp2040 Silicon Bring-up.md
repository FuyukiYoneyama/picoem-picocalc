# Journal — `probe_diff_rp2040` Silicon Bring-up

**Date opened:** 2026-04-15
**Supervisor:** Claude (Opus 4.6)
**HLD:** `wrk_docs/2026.04.15 - HLD - probe_diff_rp2040 Silicon Bring-up.md`

Running this work as a supervisor: implementation, review, and apply-feedback stages are delegated to agents. This journal is the canonical source of truth for what happened, what decisions were made, and why.

---

## Stage 0 — HLD + journal start (2026-04-15)

### Context

Arthur connected a Pico V1 to a freshly-flashed CMSIS-DAP debugprobe and asked whether it was working. `probe-rs list` showed exactly one probe (``); `probe-rs info --chip RP2040` confirmed multidrop TARGETIDs `0x01002927` / `0x11002927` and CPUID `0x410cc601` → Cortex-M0+ r0p1. Silicon is live.

Key finding during HLD drafting: `probe_diff_rp2040.rs` is **not** the stub CLAUDE.md and tech_debt.md describe — it was fully implemented in commit `529886b` ("RP2040 Phase 0 Wave 3", 2026-04-15). Filter (`is_m0plus_silicon_safe`), probe attach, single-step, semantic compare — all wired. What it has never done is execute against real silicon.

So the HLD scope collapsed from "design" to "bring-up". Runner already exists; what's missing is:

1. Probe disambiguation (`--probe <selector>`) so two probes can coexist on one host.
2. A post-step PC sanity check, so any `is_m0plus_silicon_safe` filter gap surfaces as a distinct `UNDEF_ON_SILICON` diagnostic rather than a mystery register divergence.
3. Actually running the thing against silicon and triaging what breaks.
4. Overnight fuzz driver + doc updates.

Confirmed probe-rs 0.31 APIs before committing to the design:

- `DebugProbeSelector` with `FromStr` via `TryFrom<&str>` at `probe-rs-0.31/src/probe.rs:911/990/1031`.
- `Lister::open(&selector)` at `probe-rs-0.31/src/probe/list.rs:31`.
- `Probe::attach(target, permissions)` at `probe-rs-0.31/src/probe.rs:328`.
- **Confirmed auto_attach first-probe-only bug**: `probe-rs-0.31/src/session.rs:446–458` uses `Lister::new().list_all()` and opens `probes.first()` with no target-aware fallback. This is the root cause of the disambiguation problem.

### Decisions locked in

- **`--probe` is a CLI flag, not an env var and not auto-detect by TARGETID.** Explicit, scoped, no magic. Consistent with probe-rs's own CLI.
- **Post-step PC sanity check is diagnostic, not behavioural** — doesn't change pass/fail, just classifies the failure mode. Keeps silicon HardFault escapes from being mystifying.
- **Filter duplication with `qemu_diff_m0plus` acknowledged as debt, deferred.** Collapsing `is_m0plus_safe` + `is_m0plus_silicon_safe` into one filter with a mode enum is out of scope; the ~80 lines of duplication is tolerable.
- **Scope strictly `probe_diff_{rp2040,rp2350}`.** Other silicon binaries stay on `auto_attach` until their own bring-up waves land.

### Plan summary

Six stages (tracked in TaskList). Each stage delegated to an implementer agent, reviewed by `code-reviewer` + a devil's-advocate agent in parallel, fed back through `code-review-apply-feedback`, and committed. Final compare-against-plan agent pass at the end.

### Stage 0 actions

- HLD written to `wrk_docs/2026.04.15 - HLD - probe_diff_rp2040 Silicon Bring-up.md`.
- Journal opened (this file).
- Task list created (7 tasks, Stages 0-6).
- Committed as `201c997`.

---

## Stage 1 — `--probe` flag + UNDEF diagnostic (2026-04-15)

### Deliverables shipped

- `--probe <VID:PID:SERIAL>` CLI flag on both `probe_diff_rp2040.rs` and `probe_diff_rp2350.rs`. When supplied, attach via `Lister::new().open(&selector)?.attach(target, Permissions::default())?`; when absent, keep the existing `Session::auto_attach` path verbatim.
- `UndefOnSilicon { pc: u32 }` diagnostic on `probe_diff_rp2040.rs` only — classifies post-step PC into bootrom (`< 0x0000_4000`) as a distinct failure class, leaving the `[FAIL]`/`[SKIP]` classes for their existing meanings.
- 11 new unit tests across both binaries (arg-parse + PC classification + BL target regression test).
- `mdpicoem-harness` crate bumped `0.23.0 → 0.24.0` (minor — new feature).

### The devil's-advocate catch

First pass by the implementer keyed the UNDEF check off `pc == EMU_M0PLUS_TEST_SLOT + insn_len` (the BKPT sentinel address). The `code-reviewer` agent said READY_TO_COMMIT. The devil's-advocate agent caught a **real latent bug**: the filter admits BL, B, Bcond, BX/BLX, and POP-with-PC, none of which land at the BKPT. On silicon every admitted branch would have false-triggered `[UNDEF]` and caused `run_targeted` to exit 1 on the first run. The register-level `compare_probe` would have passed on those cases — silicon and emu both branch to the same target — so the UNDEF class was over-eager.

Fix: reframe the check. The HLD's actual intent was to catch silicon HardFault → VTOR=0 → dispatch into bootrom. Classify UNDEF only when silicon PC falls inside bootrom (`< 0x0000_4000` on RP2040), leaving every other post-step PC (BKPT sentinel, branch target, garbage) to the existing register comparison. Five-line fix, plus test-suite revision (removed 3 stale tests, added 5 new ones including a dedicated BL-target regression test).

Lesson: two reviewers disagreed. Trust the spec-reader, not the diff-reader. The HLD needs updating to record the corrected classifier semantics — do as part of Stage 4 alongside CLAUDE.md.

### Build + test state at commit

- `cargo test -p mdpicoem-harness --bins`: 25/25 bin suites green; 21/21 on probe_diff_rp2040, 4/4 on probe_diff_rp2350.
- `cargo build --release -p mdpicoem-harness --bin probe_diff_rp2040 --bin probe_diff_rp2350`: clean, zero warnings.

### What did not happen

- Filter `is_m0plus_silicon_safe` NOT modified (out of scope).
- Emulator NOT touched (out of scope).
- Other silicon binaries NOT given `--probe` (out of scope).
- No silicon run yet — that's Stage 2.

Committed as `552a4fe`.

---

## Stage 2 — silicon bring-up (2026-04-15)

### Result: CLEAN on the first run

- **Sanity check** — probe-rs confirms multidrop DPs with Part `0x1002`, IDs `0x1002927` + `0x11002927` and AmbaAhb3 MemoryAPs. Silicon is a live RP2040.
- **Targeted suite** — 392/392 pass. 23.5s elapsed. Zero UNDEFs, zero SKIPs, zero Mismatches.
- **Fuzz `--fuzz 1000 --seed 1`** — 12,734/12,734 pass. 893.7s elapsed. (The `--fuzz N` arg is *per class*; with 8 ALU classes + 4.7 memory classes this expands to ~12.7K tests.) Zero UNDEFs, zero SKIPs, zero Mismatches.

### Interpretation

Nothing broken. The runner was wired carefully in commit `529886b` (Wave 3) and the emulator has been thoroughly differentiated against QEMU; silicon confirmed the work. Three things stand out:

1. **The UNDEF diagnostic never fired.** `is_m0plus_silicon_safe` has no filter gap in the admitted set — every Thumb-16 and every admitted Thumb-32 case ran to completion without a HardFault. The diagnostic is well-designed but unused today; keep it, it'll earn its keep when the filter is extended.
2. **The `--probe` flag worked.** Explicit `--probe ` routed through the new `Lister::open().attach()` path; all 12K+ attaches succeeded. Stage 1 plumbing is sound.
3. **Emulator matches silicon byte-for-byte** across 13,126 single-step diffs. No EMU_BUG escalations needed; no ESCALATE path exercised.

### Expected vs. what happened

HLD §3 predicted iterative triage across reset-timing, bootrom interaction, and filter gaps. Zero of those showed up. Conservative estimate in §7 ("plan for a day, be pleased if it's less") overshoots by ~a factor of 10 for the triage path. The up-front investment in the Wave 3 port paid off.

### Artefacts

- `d:\tmp\probe_diff_rp2040_targeted.log`
- `d:\tmp\probe_diff_rp2040_fuzz1k.log`

No code changes in this stage. No commit.

---

## Stage 3 — overnight fuzz driver (2026-04-15)

### Delivered

`fuzz-runs/run-m0plus-probe.sh` — mirrors `fuzz-runs/run-probe.sh` (M33 driver) with:

- Binary `probe_diff_rp2040.exe` (PID-qualified copy at startup, EXIT-trapped cleanup).
- Log `fuzz-runs/m0plus-probe.log`.
- Same deadline-file protocol, same random-seed concatenation, same end-of-run summary.
- Optional positional arg `$1` for probe selector (`--probe <VID:PID:SERIAL>`). Empty when omitted; auto_attach picks the single visible probe. CLI flag, not env var.

### Devil's-advocate catch (supervisor miss)

My first prompt to the implementer asked for an `MDPICOEM_RP2040_PROBE_SERIAL` env var — contradicting HLD §2.3 ("No env var — per project policy") and Arthur's brief ("Minimize the use of environment variables"). Devil's advocate flagged it immediately. Fixed by moving the selector to a positional script argument. Net zero env vars in this stage.

Lesson for supervisor: when writing implementer prompts, re-read the HLD's own constraints before dictating. A prompt that contradicts the HLD is a policy violation dressed up as an instruction.

### Validation

- `bash -n` syntax clean.
- `grep MDPICOEM_ fuzz-runs/` → zero matches (clean removal).
- Other fuzz drivers untouched.

Committed as `84df0a5`.

---

## Stage 4 — docs catch up to reality (2026-04-15)

### Delivered

- **CLAUDE.md**:
  - Testing Topology §5 — rewrote the "stub" bullet to describe the actual runner: `is_m0plus_silicon_safe` filter admits the M0+ Thumb-32 subset that QEMU rejects, no `--cycles` (M0+ has no DWT CYCCNT), `UndefOnSilicon` bootrom-range diagnostic, `--probe` selector for multi-probe hosts.
  - Non-Obvious Conventions "Hardware harness needs real silicon" bullet — added `probe_diff_rp2040` Pico V1 prereq; removed "stub" language.
  - Non-Obvious Conventions — added a new "Probe selector" bullet explaining `auto_attach`'s first-probe-only behaviour and pointing operators at `--probe <VID:PID:SERIAL>` + `probe-rs list`.
  - Workspace Layout §harness — dropped stale `(stub)` annotation off the `probe_diff_rp2040` mention.
- **tech_debt.md**: removed the now-resolved "`probe_diff_rp2040` is a stub" entry (heading + body + trailing blank).

### What did not change

- No lab-rig probe serials hardcoded. Operators read their own from `probe-rs list`.
- No changes outside the four bullets + the one `tech_debt.md` entry. Other docs and sections untouched.

Committed as `e51ed0e`.

---

## Stage 5 — soak started, overlaps Stage 6 (2026-04-15)

Exit-criterion run: `--fuzz 10000 --seed 42` → 127,468 fresh tests (~2.5 hours at observed ~14 tests/sec throughput).

Deviation from HLD §3's "`--fuzz 100000`": at 1.27M tests that would be ~25 hours, not a single-session number. The supervisor chose `--fuzz 10000` as a 10× expansion of Stage 2's run with a fresh seed, still well over the "100K tests" bar the HLD §3.1 names as its actual exit criterion.

Status at stage-switch: 2,400/127,468 tests complete, 0 failures, trajectory clean. Arthur elected to start Stage 6 in parallel with the fuzz still running, using the partial-but-clean trajectory as sufficient reassurance and letting the full soak finish in the background. Soak result will be folded back into this stage before it closes.

**The "soak in background" didn't survive.** The Stage 5 runner agent launched the fuzz via `cmd | tee log | tail -N`; when that outer agent exited, SIGPIPE from the closing `tail` killed the `tee`, which killed the fuzz. Log froze at 2,700 tests. No process running afterwards. Lesson: never pipe a long-running process through `tee | tail` from a short-lived agent — or if you must, use `nohup` / shell `disown` / a standalone driver.

Stage 5 is being re-opened on 2026-04-16 with the right tool: the overnight driver `fuzz-runs/run-m0plus-probe.sh` shipped in Stage 3, which does exactly this job (PID-qualified binary copy, 8-hour deadline default, standalone logging, no fragile pipes).

---

## Stage 6 — verification findings (2026-04-15 → 2026-04-16)

### Verifier agent

Declared **100% COMPLETE** on the deliverables, with one PARTIAL (exit-criterion §3.1 not literally met — `--fuzz 10000` substituted for `--fuzz 100000`). Every HLD section that mandated a deliverable was shown landed, with file:line evidence. Zero scope bleed. Version bump correct. No env vars.

Minor doc delta noted: HLD §3.2 still described the rejected equality-based classifier (`pc == slot + insn_len`), not the landed bootrom-range classifier (`pc < 0x0000_4000`). Fixed below.

### Devil's advocate

Caught a real miss: `README.md:125` still says "`probe_diff_rp2040` is currently a stub — the lab rig carries no RP2040 silicon". Stage 4 updated CLAUDE.md + tech_debt.md but missed README.md. Fixed below.

Otherwise verdicts: UNDEF diagnostic NOT structurally inert (confirmed by reading `classify_post_step_pc`); `--probe` runtime-error paths under-tested but not blocking; BL regression test present; "no filter-gap concern on M33" SOLID (M33 runner has no filter at all — uses `generate_all()` unfiltered); no scope creep.

### Fixes landed at Stage 6

- **`README.md:125`** — replaced stub paragraph with accurate Pico V1 / RP2040 prereq and `--probe` disambiguation pointer.
- **HLD §3.2** — rewrote the post-step PC check paragraph to describe the bootrom-range classifier that actually shipped, with an explicit "do NOT equality-check" caveat so a future reader doesn't re-introduce the first-design bug.

---

## Stage 5 — real soak (2026-04-16)

Kicking off `fuzz-runs/run-m0plus-probe.sh ` with an 8-hour deadline. This time via the standalone driver that's designed to survive parent shell exit.

Monitoring strategy: periodic log tails. Fix anything that surfaces.

---
