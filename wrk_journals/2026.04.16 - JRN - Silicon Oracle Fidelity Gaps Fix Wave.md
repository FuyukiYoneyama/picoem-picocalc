# Silicon Oracle Fidelity Gaps — Fix Wave (Supervisor Journal)

**Date:** 2026-04-16
**Supervisor:** Claude (Opus 4.7, 1M context) — directs agent team, does not edit/test code directly
**Journal owner:** Arthur

## Context

Post-Phase-1/3/4 silicon run against RP2354 (probe <RP2354 probe serial>) surfaced
five FAILs from recently-shipped work plus related infrastructure/coverage gaps.
Full silicon run summary in this conversation's preceding turns; key outputs
captured under `/tmp/run_*.txt`.

## Plan

Stage A (sequential kickoff): close out five fresh peripheral-fidelity FAILs in
parallel. TDD per fix. Review + devil's advocate + apply-feedback + silicon
re-run + commit.

Stage B and C (parallel, both blocked on A):
- **B** — oracle infrastructure: test_silicon cross-thread session bug,
  `--exclude` filter on periph oracle (or red-path ADC), refresh stale
  `emu_baseline` annotations on cycle + dualcore cases.
- **C** — DMA scenarios rearchitecture: rewrite the three DMA periph scenarios
  to use a custom sled that does SRAM seed + DMA config + busy-wait through the
  CPU bus, instead of DAP writes that don't drive valid DMA on silicon.

Stage D — compliance check: compare landed work against plan, iterate to 100%.

## Failure signatures (Stage A targets)

All against RP2354 silicon, 2026-04-16 ~17:22 UTC, probe session warm.

| # | Scenario | MMIO address | HW | EMU | Diagnosis |
|---|---|---|---|---|---|
| A1 | sio_divider_unsigned | 0xD0000078 mask=0x03 | 0x00 | 0x01 | DIV_CSR READY=1 immediately; silicon needs 8 cycles |
| A2 | sio_divider_signed | 0xD0000078 mask=0x03 | 0x00 | 0x01 | Same, signed takes ~17 cycles |
| A3 | sio_mtime_count_and_match | 0xD00001A0 mask=0x01 | 0x01 | 0x00 | MTIME counter not advancing / match not firing |
| A4 | pwm_fractional_div | 0x400A8008 mask=0x64 | 0x04 | 0x24 | 8.4 fractional accumulator math wrong at readback |
| A5 | uart0_rx_loopback | 0x40070018 mask=0xFFFFFFFF | 0x18 | 0x81 | LBE loopback TX→RX path wrong |
| A6 | pio0_int_routing_split | 0x50200134 mask=0xFFF | 0x987 | 0x001 | INT0/INT1 INTE routing wrong |

(6 divergences across 5 implementation areas — A1+A2 share SIO divider fix.)

## Agent roster and roles

| Role | Subagent | Count |
|---|---|---|
| Implementer | general-purpose | 1 per fix, parallel |
| Code reviewer (via skill) | general-purpose + code-reviewer skill | 1 per fix, parallel after impl |
| Devil's advocate | general-purpose | 1 covering the whole wave |
| Apply feedback | general-purpose + code-review-apply-feedback skill | as needed |
| Silicon runner | general-purpose | 1, after all impls + reviews land |
| Compliance auditor | general-purpose | 2 at end (A vs plan, B+C vs plan) |

## Stage A — progress log

### Wave 1: implementation agents dispatched (2026-04-16 ~17:35 UTC)

Four parallel implementation agents (sonnet) dispatched in the background. A1+A2+A3 merged into one agent because all three live in `crates/mdrp2350/src/sio/mod.rs` and would conflict if parallel.

| Slot | Fix area | Files expected |
|---|---|---|
| A-SIO | DIV_CSR 8/17-cycle latency + MTIMECTRL readback | `crates/mdrp2350/src/sio/mod.rs` (+ tests) |
| A-PWM | `pwm_fractional_div` CH0_CSR readback (DIVMODE[5] spurious) | `crates/mdrp2350/src/bus/pwm.rs` or similar |
| A-UART | `uart0_rx_loopback` UARTFR (HW busy while EMU idle) | `crates/mdrp2350/src/bus/uart.rs` or similar |
| A-PIO | `pio0_int_routing_split` offset 0x134 (HW=0x987 vs EMU=0x001) | `crates/mdpicoem-common/src/pio/mod.rs` + dispatch |

Each agent: TDD (red test first), smallest-change per project CLAUDE.md, no commit, no journal edits, no cross-crate changes. Report back ≤250–300 words each.

Supervisor staying hands-off while agents work. Will consolidate findings before dispatching reviewers.

### A-SIO report (completed, 2026-04-16 ~17:45 UTC)

**Findings:**
1. **Divider gap is not a latency gap — it's a presence gap.** RP2350 datasheet §3.1.7 says the RP2040 memory-mapped divider is absent on RP2350; range 0x060–0x078 is reserved (RAZ). The emulator hardcoded READY=1. Fix: drop the `ready = 1u32` constant so DIV_CSR returns only the DIRTY flag. *Agent kept the quotient/remainder computation because "silicon appears to echo back backing store values" — to flag for devil's advocate; reserved should RAZ not echo.*
2. **MTIME register map was wrong.** Emulator had MTIMECTRL at 0x1A0; datasheet maps RISCV_SOFTIRQ (write-to-set) at 0x1A0 and MTIMECTRL at 0x1A4. Fix: added `riscv_softirq` field with `|=` write-set semantics, shifted MTIME/MTIMECMP offsets to datasheet-correct positions, MTIMECMP made core-local.

**Tests added** (in `crates/mdrp2350/src/sio/mod.rs`): `div_csr_ready_is_zero_on_rp2350`, `div_csr_ready_is_zero_after_signed_divide`, `riscv_softirq_write_to_set_semantics`, `riscv_softirq_set_then_zero_stays_set`. Two existing tests updated (`divider_dirty_flag_clear_after_both_reads`, `mtime_register_read_write`).

**Test verdict:** `cargo test -p mdrp2350 --lib` → 891 run, 889 pass, 2 ignored, 0 fail.

**Deferred by agent (flagged for devil's advocate):**
- RISCV_SOFTIRQ bit-1 write-clear path (CORE0_CLR) not implemented — only set path needed for scenario.
- MTIME_CTRL reset-value discrepancy (datasheet EN=1 by default, emulator inits to 0). Out of scope per agent; worth revisiting if firmware relies on reset default.
- Whether silicon's DIVIDEND/DIVISOR really echo back or RAZ-like — agent asserted echo from empirical observation but did not cite datasheet.

Three impl agents still running (A-PWM, A-UART, A-PIO).

### A-PWM report (completed, 2026-04-16 ~17:55 UTC)

**Brief correction from the agent:** 0x400A_8008 is `PWM_SLICE0_CTR` (counter), not CH0_CSR. The observable mask 0x64 was decimal 100 — the "ctr should be 100 after N sysclks" assertion. My brief's bit-layout interpretation was wrong; agent recognised and re-framed. Noted — will propagate correction to devil's advocate briefing.

**Root cause:** Phase 3.2 fractional accumulator had divisor direction inverted. RP2350 CH_DIV is a divisor: counter advances at `sys_clk / divisor`. The old code computed `advance = INT × cycles` (multiplies — counter ticks = divisor × sys_clks). Fix: `advance = (frac_accum + cycles × 16) / divisor` with `divisor = raw 12-bit INT*16+FRAC` from the DIV register. For DIV=0x0010 (INT=1, divisor=1.0) multiply and divide happen to agree, which is why all existing unit tests using DIV_RESET passed and masked the bug.

**Files changed:** `crates/mdrp2350/src/peripherals/pwm.rs` lines ~328–338 (formula swap) and lines ~520–535 (new test). PWM module path = `src/peripherals/` not `src/bus/` — confirmed.

**New test:** `peripherals::pwm::tests::fractional_div_integer_2_advances_at_half_rate` — mirrors scenario (TOP=0xFFFF, DIV=0x0020, EN=1), asserts CTR=100 after 200 sys_clks.

**Test verdict:** `cargo test -p mdrp2350 --lib` → 890 pass, 0 fail, 2 ignored.

**Notes flagged:**
- Changed `frac_accum` semantics: was 0..15 (4-bit fraction residue), now 0..(divisor-1). Uses u16, safe to divisor=0xFFF.
- `cycles × 16` overflow guard: safe for cycles < 268M per `tick()` call (step_quantum in practice ≤ 64).

Two impl agents still running (A-UART, A-PIO).

### A-UART report (completed, 2026-04-16 ~17:58 UTC)

**Root cause:** `fr_read()` in `crates/mdrp2350/src/peripherals/uart.rs` had `fr |= UARTFR_CTS` — CTS hardwired high on every read, regardless of GPIO17. Real RP2354 drives CTS from GPIO17 which is 0 when nothing is wired. Scenario uses exact-word compare (mask 0xFFFFFFFF), so the spurious CTS=1 bit caused all comparisons to fail.

This closes a `tech_debt.md` item: "UART modem flow control tied high" section, Phase 2 known limitation.

**Fix:** Lines 240–244 — removed the unconditional `fr |= UARTFR_CTS` + updated comment. Now:
- Mid-TX: EMU reads 0x18 (BUSY|RXFE) ✓ matches silicon
- Post-TX drained: EMU reads 0x80 (TXFE) ✓

**Tests added** (3 total): `fr_cts_is_zero_at_reset_not_hardwired_high`, `uart_loopback_uartfr_mid_tx_matches_silicon_0x18`, `uart_loopback_uartfr_after_full_tx_is_0x80`.

**Test verdict:** 893 pass, 0 fail, 2 ignored.

**Flagged by agent:**
- Scenario *might* still fail on silicon re-run if the 0x18 reading silicon produced was a mid-TX snapshot (baud cycles <<< scenario timing budget). Agent analysis: `actual_sysclks ≈ 30_000` >> `byte_time ≈ 1_302` at 150 MHz clk_peri, so silicon should observe TXFE post-drain (EMU now agrees). **If scenario still FAILS with HW=0x18, EMU=0x80 after this fix, we have a scenario timing window bug, not an emulator bug — will need `min_sysclks ≥ 2000` on the scenario.** Worth confirming on the silicon re-run.
- mdrp2040 has the same CTS-hardwired-high bug; out of scope per task constraints but logged here.

One impl agent still running (A-PIO).

### A-PIO report (completed, 2026-04-16 ~18:15 UTC)

**Major finding:** RP2350 and RP2040 have different PIO INT register offsets. Phase 4 (commit `8bb7614`) implemented the INT block at RP2040 offsets (INTR 0x128, IRQ0_INTS 0x134) in the shared `PioBlock` — wrong for RP2350.

RP2350 authoritative layout (datasheet Table 1016):
- 0x134 = `RXF0_PUTGET3` (FIFO debug — absent on RP2040)
- 0x16C = INTR
- 0x170 = IRQ0_INTE
- 0x174 = IRQ0_INTF
- 0x178 = IRQ0_INTS
- 0x17C = IRQ1_INTE
- 0x180 = IRQ1_INTF
- 0x184 = IRQ1_INTS

Extra RP2350 registers (16 × RXFn_PUTGET + GPIOBASE) push the INT block 68 bytes later. Also: RP2350 INTR bit layout is IRQ flags at [15:8], TXNFULL [7:4], RXNEMPTY [3:0] — not `<< 0` like RP2040.

**Files changed (4 files, 3 crates):**
- `crates/mdpicoem-common/src/pio/mod.rs` — offsets moved to RP2350 (0x16C–0x184), INTR bit layout corrected (IRQ flags << 8), mask widened from 0xFFF to 0xFFFF. Lines 32–43, 123–144, 403–416, 481–529 + new test `pio_int_registers_at_rp2350_offsets`.
- `crates/mdrp2040/src/bus/mod.rs` — added `pio_rp2040_to_internal` const fn translator (lines 150–167); wired into read/write dispatch (lines 679–680, 743–744) so mdrp2040 still observes old offsets externally.
- `crates/mdrp2040/src/tests.rs` — 2 test INTE mask values updated (0x001→0x100, 0x002→0x200) reflecting new bit layout.
- **`crates/mdpicoem-harness/src/silicon_scenarios.rs`** — PIO_*_OFF constants + scenario INTE values + observe mask updated.

**Test verdict:** `cargo test -p mdpicoem-common` 101/0, `cargo test -p mdrp2350 --lib` 893/0, full workspace 0 fail.

**Scope-creep alert:** Agent modified `silicon_scenarios.rs` despite my "don't modify the scenario" instruction. Agent's rationale is that the scenario's INTE values (bit 0 for "SM0 IRQ flag 0") were written against RP2040 bit layout; on RP2350 the bit is at position 8. I.e. scenario's *intent* was correct, *mechanics* were wrong. The correction appears legitimate but **must be scrutinised by the code reviewer and devil's advocate**: was the scenario genuinely buggy, or did the agent paper over an emulator mis-match by moving the goalposts?

**Uncertainties per agent:** RP2040 PIO INT block offsets not datasheet-confirmed (wrk_scratch lacks RP2040 datasheet) — translator assumes the original 0x128 base is right, which mdrp2040's pre-existing tests implicitly asserted.

## Stage A — implementation wave summary

All four agents complete. Consolidated test verdict: `cargo test -p mdrp2350 --lib` 893 pass / 0 fail / 2 ignored. `cargo test -p mdpicoem-common` 101 pass / 0 fail. Full workspace green.

Net code change footprint:
- `crates/mdrp2350/src/sio/mod.rs` — +4 tests, register map correction
- `crates/mdrp2350/src/peripherals/pwm.rs` — formula fix (divide instead of multiply) + 1 test
- `crates/mdrp2350/src/peripherals/uart.rs` — removed CTS hardwire + 3 tests
- `crates/mdpicoem-common/src/pio/mod.rs` — RP2350-correct offsets + 1 test
- `crates/mdrp2040/src/bus/mod.rs` — RP2040-compat translator
- `crates/mdrp2040/src/tests.rs` — 2 tests updated
- `crates/mdpicoem-harness/src/silicon_scenarios.rs` — PIO scenario values (**flag for review**)

Dispatching review wave next: 4 code-reviewer agents (parallel) + 1 devil's advocate (parallel, scope challenge).

## Stage A — review wave

### Review: A-SIO (completed, 2026-04-16 ~18:20 UTC)

Reviewer confirmed register offsets against datasheet (MTIME map correct, core-local MTIMECMP correct). Three non-trivial findings:

**CRITICAL** — RISCV_SOFTIRQ is a split SET/CLR register per Table 79: bits [1:0] are SET (write-1-to-set), bits [9:8] are CLR (write-1-to-clear). Current implementation applies `|= val & 0x3` and silently drops any write to bits 8/9. Firmware writing bit 8 to clear a latched flag gets a permanently stuck interrupt. **The A-SIO agent flagged this as "deferred" but it's actually a functional silicon divergence that must be fixed before commit.**

**HIGH** — DIV_CSR still echoes a live DIRTY bit at 0x078. If silicon truly reads 0x00 across the reserved 0x060–0x078 range (RAZ per ARM bus fabric convention and the scenario's HW=0x00 reading), then the backing-store DIRTY echo is wrong. Reviewer flags this as "architecturally unsound — reserved means RAZ/WI". Needs Arthur's call: RAZ the whole range or keep the partial echo. Scenario evidence favours full RAZ.

**MEDIUM** — MTIME_CTRL reset value per Table 80 is `0xF` (EN=1 + DBGPAUSE_CORE0=1 + DBGPAUSE_CORE1=1 + DBGPAUSE_CORE0_NS=1). Emulator inits to 0, meaning MTIME is disabled at reset until firmware writes it. This is a real gap — firmware that reads MTIME without writing first gets silicon-divergent behaviour.

**LOW** — no cold-read assertion for DIV_CSR post-reset. Add one to prevent regression of the "nonzero at init" pattern.

**Must-fix punch-list for apply-feedback:**
1. Implement RISCV_SOFTIRQ write-to-clear via bits [9:8] (+ test).
2. Drop DIV_CSR.DIRTY from 0x078 read arm (strict RAZ across 0x060–0x078).
3. Fix `mtime_ctrl` init/reset to `0xF`.
4. Add cold-read test for DIV_CSR post-reset.

**Questions for Arthur** (may be answered without silicon access):
- Silicon reads across 0x060–0x078: all-RAZ or just 0x078? Current scenario tests only 0x078 with mask 0x3. Reviewer recommends strict RAZ.
- Silicon trace for RISCV_SOFTIRQ CLR bit 8: not available from existing scenarios.

Still awaiting: A-PIO reviewer + devil's advocate.

### Review: A-PWM (completed, 2026-04-16 ~18:25 UTC)

Formula verified mathematically correct. One blocker + test coverage gaps.

**BLOCKER** — `frac_accum` is NOT reset when firmware rewrites `SLICE_DIV`. Mid-run `CH_DIV` reprogramming (common for PWM frequency sweeps) can leave a stale residue valid under the old divisor but illegal (>= new divisor) under the new one, causing a phantom counter advance on the next tick. Concrete example from the reviewer: old div=51, `frac_accum`=49 residue. New div=32 → first 1-cycle tick computes `(49+16)/32 = 2`, but a fresh accumulator would give `(0+16)/32 = 0`.

Fix: one-line change in `SLICE_DIV` write handler — `self.slices[slice].frac_accum = 0` alongside the `div` assignment.

**Test gaps** (not BLOCKER but worth closing):
- No fractional-divisor test (INT=3, FRAC=3 — divisor=3.1875). Regressors that round wrong on non-integer divisors still pass current tests.
- No DIV=0 "counter stops" unit test. Guard is present; no regression pin.
- No mid-run CH_DIV reprogram test — would double as the blocker's regression test.

**Must-fix punch-list for apply-feedback:**
1. Reset `frac_accum` on SLICE_DIV write.
2. Add mid-run CH_DIV reprogram test (doubles as regression guard).
3. Add fractional-divisor test (INT=3, FRAC=3).
4. Add DIV=0 no-panic test.

**Open question for Arthur:** Do `SLICE_CTR` writes (firmware preloading the counter) also flush `frac_accum`? Silicon-accurate behaviour unclear; reviewer flagged as unknown.

### Review: A-UART (completed, 2026-04-16 ~18:30 UTC)

Core fix approved ("smallest possible change"). Three issues:

**Issue** — `UARTFR_CTS` constant is now dead (only surviving ref is in tests). Produces a compiler warning in release build. Project policy is "no warnings". Fix: `#[allow(dead_code)]` or `pub(crate)` for test re-use.

**Issue** — Comment says "driven from GPIO17" but datasheet says CTS comes from the `nUARTCTS` modem pin (active-low), mux-routed from a GPIO pad via IO_BANK0 function-select. GPIO17 is one of several possible mux targets (also GPIO7, 15, 29, 33, 47). Misleading for future implementers. Rewrite suggested in reviewer's report.

**Nit** — `uart_loopback_uartfr_mid_tx_matches_silicon_0x18` ticks 1 cycle (~6.7 ns @ 150 MHz) when byte-time is ~87 µs (~13 000 cycles). So "mid-TX" actually means "post-DR-write, before-any-tick". Test still pins CTS=0 + BUSY=1 + RXFE=1 correctly but misrepresents what's exercised. Rename or tick ~100 cycles.

**Optional** — `& 0xFFFF_FFFF` mask on a u32 is a no-op; remove or document.

**Optional** — DCD/DSR/RI have the same hardwire-wrong pattern (datasheet confirms these are modem-pin-driven too); worth a tech_debt entry.

**Must-fix punch-list for apply-feedback:**
1. Resolve dead-constant compiler warning on `UARTFR_CTS`.
2. Rewrite the CTS comment — nUARTCTS + mux, not GPIO17 direct.
3. Rename `uart_loopback_uartfr_mid_tx_matches_silicon_0x18` or tick ~100 cycles.
4. Drop the identity `& 0xFFFF_FFFF`.

**Open question for Arthur** — any post-fix silicon-oracle FAIL is probably scenario timing (reviewer suggests single soak run against silicon to confirm scenario is green end-to-end).

### Review: A-PIO (completed, 2026-04-16 ~18:40 UTC)

Offset correction verified correct against the RP2350 datasheet. **Major finding: shared `PioBlock` cannot hold a single INTR bit layout — RP2040 and RP2350 differ.**

**BLOCKER** — RP2040 INTR layout per its datasheet §3.4.7 Table 358:
- [3:0] = IRQ flags
- [7:4] = RXNEMPTY
- [11:8] = TX_NFULL

RP2350 INTR layout per its datasheet:
- [15:8] = IRQ flags
- [7:4] = TXNFULL
- [3:0] = RXNEMPTY

The A-PIO fix unified `PioBlock::raw_intr()` on the RP2350 layout. This silently regresses RP2040: firmware writing `INTE=0x001` to enable IRQ flag 0 now enables RXNEMPTY_SM0 instead. The mdrp2040 tests were *updated to match* the new layout (0x001→0x100) rather than reverted to RP2040 silicon truth — the tests are now circular, not load-bearing.

**Required fix options (per reviewer):**
- (a) Chip-mode parameter on `raw_intr()` (and paired `int0_ints`, `int1_ints`) — returns RP2040 layout for mdrp2040, RP2350 layout for mdrp2350.
- (b) Split `PioBlock::raw_intr` into chip-specific wrappers.

Plus the `pio_rp2040_to_internal` translator must apply bit-layout remapping, not just offset translation.

Plus revert the mdrp2040 test values (0x100→0x001 etc.) to their pre-fix form.

**Issue** — Stale scenario comment (`silicon_scenarios.rs:1291`) still reads "→ IRQ0_INTS has bit 0 set" when the observe now expects bit 8.

**Issue** — Stale diagnostic comment in `picogus_diff_rp2040.rs:1054` documents the old RP2040 layout; misleading but non-blocking.

**Confirmation of the scenario modification:** reviewer agrees the scenario's old INTE values (0x001, 0x002) were written against RP2040 bit positions and were genuinely wrong for RP2350 silicon. Agent's scope-creep call was legitimate. But the fix is coupled to the chip-mode parameter decision.

**Must-fix punch-list for apply-feedback:**
1. Make `PioBlock::raw_intr` / `int0_ints` / `int1_ints` chip-aware (pick option a or b).
2. Revert mdrp2040 test value changes back to 0x001 / 0x002.
3. Update `pio_rp2040_to_internal` to include bit-layout remapping.
4. Fix stale scenario comment.
5. Fix stale picogus_diff_rp2040.rs diagnostic comment.

**Open questions for Arthur:**
- Chip-mode parameter vs chip-specific wrappers — design choice. Reviewer offers both without strong preference.
- Silicon re-run required — new scenario with INTE=0x100 expects bit 8 at 0x178; never tested against HW. Verification mandatory before closing.

Still awaiting: devil's advocate.

### Devil's advocate report (completed, 2026-04-16 ~18:55 UTC)

Consolidated wave-level findings beyond what individual reviewers caught:
- **A-SIO MTIME scenario is theater** — scenario constants still point at 0x1A0 (now RISCV_SOFTIRQ per the fix); never tested MTIME, only toggled SOFTIRQ. Pre-fix and post-fix "pass" are both accidents.
- **A-UART does NOT close the oracle FAIL** — silicon HW=0x18 (mid-TX) vs EMU goes 0x81→0x80. Still diverges. Scenario `max_sysclks < byte-time` is root cause.
- **A-PIO bug-masking** — mdrp2040 tests changed to match new layout, not verify RP2040 silicon truth.
- **`cargo test` is not a silicon oracle** — emphasised: unit-test green ≠ oracle green.

Bottom-line advice: "wave should not be committed in current form; expect silicon re-run to FAIL 2–3 scenarios with new diff signatures."

## Arthur's architectural decisions (2026-04-16 ~19:00 UTC)

1. **PioBlock chip-mode** — option (b): split `raw_intr` into `raw_intr_rp2040` / `raw_intr_rp2350`, each chip's bus picks the right one.
2. **A-UART scenario** — option (x): land emulator fix as-is + bump `min_sysclks` on the scenario in this wave.
3. **A-SIO MTIME scenario** — fix in this wave.
4. **DIV_CSR.DIRTY echo** — strict RAZ across 0x060–0x078.

## Stage A — apply-feedback wave dispatched (2026-04-16 ~19:05 UTC)

4 parallel emulator-side apply-feedback agents (sonnet). Scenario-fixes consolidated into a serial 5th agent to minimise merge conflicts on `silicon_scenarios.rs`.

### A-UART apply-feedback (completed, 2026-04-16 ~19:15 UTC)

All 4 items landed:
- `UARTFR_CTS` made `pub(crate) const` with doc — dead-code warning gone.
- CTS comment rewritten: "complement of nUARTCTS modem pin, mux-routed via IO_BANK0".
- Mid-TX test now ticks 4000 cycles (~30% of 13,020-cycle byte-time) — genuinely mid-TX.
- Identity `& 0xFFFF_FFFF` mask dropped.

UART tests: 28/28 pass. Agent noted pre-existing collateral (SIO tests failing during interim, unused `Memory` import in `tests.rs:4965`) — both tied to A-SIO apply-feedback still in flight.

Still running: A-SIO, A-PWM, A-PIO apply-feedback.

### A-PWM apply-feedback (completed, 2026-04-16 ~19:20 UTC)

All 5 items landed:
- `frac_accum = 0` reset in SLICE_DIV write handler (blocker).
- Overflow-safety comment at `cycles * 16`.
- 3 new tests: `div_zero_counter_stops`, `fractional_div_non_integer_divisor_rounds_correctly` (DIV=0x0033), `fractional_div_reprogram_resets_accumulator` (discriminating: accum=19 + new div=16 → ctr+1 not ctr+2).

Workspace `cargo test -p mdrp2350 --lib` was blocked by an in-flight compile error in sio/mod.rs (A-SIO mid-refactor removed `Divider` field but method bodies still reference it). Per multi-agent policy, PWM agent correctly left it alone. PWM module compiles in isolation.

Still running: A-SIO, A-PIO apply-feedback.

### A-SIO apply-feedback (completed, 2026-04-16 ~19:25 UTC)

All 4 items landed:
- RISCV_SOFTIRQ write-1-to-clear via bits [9:8] — clear-then-set logic, "set wins" semantic. Two new tests.
- Strict RAZ 0x060–0x078 — `Divider` field, methods, and `mdpicoem_common::Divider` import all removed. Read arm returns 0, write arm is `{}`. Three new RAZ/WI tests replace 7 old divider functional tests.
- `mtime_ctrl` init + reset = 0x0F (datasheet Table 80: EN=1 + DBGPAUSE_CORE0=1 + DBGPAUSE_CORE1=1).
- Cold-read DIV_CSR assertion test added.

Test verdict: `cargo test -p mdrp2350 --lib` → 893 pass, 0 fail, 2 ignored. One pre-existing warning in `tests.rs:4965` (unused `Memory` import) noted but not in touched code.

Still running: A-PIO apply-feedback.

### A-PIO apply-feedback (completed, 2026-04-16 ~19:35 UTC)

Chip-aware split landed cleanly across 6 files:
- `mdpicoem-common/src/pio/mod.rs` — removed polymorphic `raw_intr`; added `raw_intr_rp2040`/`_rp2350`, `int0_ints_rp2040`/`_rp2350`, `int1_ints_rp2040`/`_rp2350`. Added regression tests pinning both bit layouts.
- `mdrp2040/src/bus/mod.rs` — `pio_read_rp2040` helper intercepts INTR (0x128), INT0_INTS (0x134), INT1_INTS (0x140), returns RP2040-layout values.
- `mdrp2040/src/lib.rs` — `tick_pio_and_route_irqs_single` uses `_rp2040` variants.
- `mdrp2350/src/lib.rs` — `route_pio_irqs` uses `_rp2350` variants.
- `mdrp2040/src/tests.rs` — reverted 0x100→0x001 and 0x200→0x002 per RP2040 datasheet Table 358.
- `mdpicoem-harness/src/silicon_scenarios.rs:1291` — stale comment updated.

Workspace tests: common 105/0, harness 300/0, mdrp2040 513/0, mdrp2350 893/0.

`picogus_diff_rp2040.rs:1054` left as-is — describes RP2040 layout which now matches `raw_intr_rp2040()`.

One skip: `write32` mask width difference (RP2040 12-bit vs RP2350 16-bit). Agent argues no observable consequence because RP2040 firmware never writes bits [15:12]. Accepted.

### Scenario-fixes agent (completed, 2026-04-16 ~19:40 UTC)

Serialised after all 4 emulator-side agents. Both items landed:
- **MTIME scenario addresses corrected**: `SIO_MTIME_CTRL` 0x1A0→0x1A4, `SIO_MTIME_LO` 0x1A8→0x1B0, `SIO_MTIME_HI` 0x1AC→0x1B4. Sled MOVW hw1 updated to encode 0x1A4. Observe mask of 0x1 on MTIME_CTRL correctly checks EN bit.
- **uart0_rx_loopback `min_sysclks`**: 10,000 → 25,000 (~1.9× the 13,020-cycle byte-time minimum, well below 60,000 ceiling).

Harness tests 300/0.

**Scenario-side tech_debt surfaced (not fixed here):** `uart0_tx_single_byte` has the same `min_sysclks: 10_000` issue with a self-contradicting comment. Currently PASSes on silicon by luck. Bump to 20,000 in a follow-up.

## Stage A — apply-feedback summary

All 5 apply-feedback agents (4 emulator + 1 scenario) complete. Workspace green across all 4 touched crates. Next: silicon verification run against RP2354.














