---
Title: RISC-V Oracle Campaign — Phase 2 Follow-ups
Date: 2026-04-18
Owner: Arthur (team lead) + Claude agent team (5 parallel tracks)
Status: In progress — HLD phase
Base commit: fe13038 (journal accuracy corrections post-audit)
---

## Goal

Close the five follow-ups documented in the prior campaign journal:

1. **Pre-existing `edge_cases_rv32a` unit-test failure** — atomic address `0x80000400` out of reservable window `0x20000000..0x20082000`.
2. **PMP fuzz class** — blocked on emulator `pmpcfg*`/`pmpaddr*` CSR model.
3. **Silicon-fuzz BLOCKER (scenario state-leakage)** — A.2.3 family; `pwm_wrap_irq`, `pio0_nop_loop`, `pio0_int_routing_split` fail because orchestrator resets DUT between iterations, not between scenarios.
4. **`uart0_rx_loopback` two-valued HW signature** — HW={0x5A, 0xAD} vs EMU=0x42 under soak-shuffle; possibly second state-leakage bug, possibly timing jitter.
5. **Fuzz-filter UX** — `--fuzz N --class X` generates N mixed then filters ≈5%; misleading to users.

## Process

1. **HLD phase (parallel)**: five `Plan` agents each produce an HLD. Path: `wrk_docs/2026.04.18 - HLD - <topic> V1.md`. Scale depth to scope — short fixes get short HLDs. No code yet.
2. **Team lead review**: I read each HLD, iterate with the agent via SendMessage until signed off. Any HLD I reject gets a new version (V2, V3…).
3. **Implementation phase**: once ALL HLDs signed off, launch implementation agents. Serialize pairs that touch the same files; parallelize the rest.
4. **Review + apply**: each implementation gets a code-reviewer pass, then apply-feedback pass, then commit.
5. **Final audit**: independent completeness check.

## Conflict map (pre-HLD — refine after HLDs land)

| Track | Primary files |
|---|---|
| #1 rv32a | `crates/mdpicoem-harness/src/riscv_gen.rs` |
| #2 PMP | `crates/mdpicoem-harness/src/riscv_gen.rs` + `crates/mdrp2350/src/core_riscv/*` |
| #3 silicon state-leakage | `crates/mdpicoem-harness/src/silicon_scenarios.rs` |
| #4 uart0_rx_loopback | `crates/mdpicoem-harness/src/silicon_scenarios.rs` (investigation may widen) |
| #5 fuzz filter UX | `crates/mdpicoem-harness/src/bin/test_rp2350_qemu_diff_riscv32.rs` |

Likely conflicts: #1↔#2 (same file, same neighbourhood), #3↔#4 (same file), #2↔#5 (class enum + dispatch).

## Iteration log

- **2026-04-18 T0**: Journal created. Spawning 5 HLD agents in parallel.
- **2026-04-18 T1**: All five HLDs returned. Signed off with notes:
  - **HLD-1** (rv32a): approved as-is. Root cause traced via git log (`049cab1` assertion born at old `0x2000_0300`, `d9a66a7` moved SCRATCH_BASE to `0x8000_0400` but didn't update assertion). Fix: canon_oracle_addr alias in the assertion (~3 lines).
  - **HLD-2** (PMP): approved for **phase-1 only** (1 entry, no L-bit). Phase-2/3 explicitly out of scope for this campaign. Added Risk 7 requiring implementation agent to empirically verify W=1/R=0 WARL behaviour on QEMU. Weight-table rebalance flagged for implementer.
  - **HLD-3** (silicon state-leakage): approved with **scope EXPANDED to fold HLD-4 in**. Option A (per-scenario explicit reset) recommended; Options B and C rejected. Fix 10 scenarios (3 confirmed FAIL + 4 latent PIO + 2 UART symmetric).
  - **HLD-4** (uart0_rx_loopback): saved as redirect to HLD-3. Smoking-gun evidence (`0x5A` = `S_UART0_TX_SINGLE_BYTE` payload at `silicon_scenarios.rs:855`) confirmed same pattern. No separate implementation agent.
  - **HLD-5** (fuzz filter UX): approved as-is. Option A (pre-dispatch on --class) — every class already has a dedicated `gen_fuzz_*` with matching signature, so ~10-line change.
- **2026-04-18 T2**: Implementation phase starting. **Wave 1 parallel**: Impl-1 (rv32a), Impl-3 (scenarios), Impl-5 (fuzz UX) — no file overlap. **Wave 2 serial**: Impl-2 (PMP) after Wave 1 closes (conflicts with 1 + 5 on riscv_gen.rs + harness binary).
- **2026-04-18 T3**: **Impl-1 done** — 3-line test fix, all 4 gates green (rv32a test passes, 376/0/1 harness unit tests, 1243/0/6 mdrp2350). Notes a minor HLD acceptance-criteria miscount (376→376 not 377, agent was right).
- **2026-04-18 T4**: **Impl-3 done** — 89+8 LoC, all 10 scenarios prefixed, A.2.2 preserved (UART E2E test still reaches UARTFR=0x80/UARTDR=0x42), added missing `RESET_UART0 = 1<<26` constant. Gates: 1243/0/6 mdrp2350, 376/0/1 harness, silicon binary builds clean. Silicon soak must be run by Arthur (needs RP2354 probe). Spawned Reviewers 1+3 and Impl-5 in parallel.
- **2026-04-18 T5**: **Reviewer-1 APPROVED** clean — zero findings. Committed Impl-1 as `1a6baab`.
- **2026-04-18 T6**: **Reviewer-3 APPROVED** clean — all 10 scenarios verified, A.2.2 preserved, PIO+IO_BANK0+PADS_BANK0 release tuples retained in latent scenarios. Committed Impl-3 as `25449db`.
- **2026-04-18 T7**: **Impl-5 done** — run_fuzz dispatch rewritten, 11/11 generator names matched exactly, mixed-mode invariance confirmed byte-identical (first 5 tc.name strings same pre/post). Closed match (no wildcard) makes future RiscvClass additions a compile error.
- **2026-04-18 T8**: **Reviewer-5 APPROVED** clean. One pre-existing minor finding (reproduce hint prints `--class all` for no-class runs) flagged as out-of-scope follow-up. Committed Impl-5 as `41f318c`.
- **2026-04-18 T9**: **Wave 1 complete** (3 commits: 1a6baab, 25449db, 41f318c). **Starting Wave 2**: Impl-2 (PMP phase-1). This is the larger task (~350 LOC across csr.rs, regs.rs, riscv_gen.rs, harness binary). Wave 2 runs solo — it conflicts with every Wave 1 target.
- **2026-04-18 T10**: **Impl-2 returned with correct stop-at-the-wall behavior** — emulator CSR model + 7 unit tests + 13 edge cases landed clean, but 5k fuzz smoke surfaced two HLD-unpredicted divergences:
  - QEMU rv32 virt applies non-zero `pmp_gran` by default (HLD §3.3 wrong).
  - QEMU L-bit sticky-trap across cases (HLD §5.1 Risk 1 firing without per-test reset ritual — phase-2 scope).
- **2026-04-18 T11**: **Scope-reduction decision**: keep all code (CSR model unblocks bootrom PMP writes, which IS the follow-up goal), set `RiscvClass::Pmp` weight to 0 bp so mixed runs aren't contaminated. `--class pmp` CLI still works for phase-2 bring-up. HLD V2 addendum documents both divergences with explicit §3.3 correction.
- **2026-04-18 T12**: **Impl-2 scope-reduction validation**: all gates green.
  - mdrp2350: 1250/0/6 (+7 PMP). Harness: 377/0/1. Weights sum green.
  - Edge: 220/220. Mixed 2k seed 42: 2000/2000. Mixed 10k seed 2703168085: 10000/10000 (no PMP contamination).
  - `--class pmp --fuzz 100 --seed 42` smoke: 100/100 PASS on phase-1 restricted pool (validates CSR model + WARL arms).
  - Reviewer approved clean. **Committed as da56c50.**
- **2026-04-18 T13**: **Implementation phase complete** (4 commits: 1a6baab, 25449db, 41f318c, da56c50). Spawning final audit agent.
- **2026-04-18 T14**: **Final audit: COMPLETE**. All 12 gates green (build, rv32a fix, 7 PMP unit tests, 220/220 edges, 2000/2000 @ seed 42, 10000/10000 @ seed 2703168085, fuzz UX reports 100 cases for `--class zifencei --fuzz 100`, PMP CLI 100/100, mdrp2350 1251/0/6, harness 377/0/1, 4 commits present, 5 HLDs on disk, journal up-to-date). All 12 adversarial checks clean (rv32a invariant preserved, PMP scope-descope documented, 10/10 scenarios prefixed, UART A.2.2 preserved, seed-determinism invariance holds, no debug litter, silicon-soak deferral flagged to Arthur, no cross-agent pollution in any campaign commit).

## Campaign close-out

**Commits (4):**
| SHA | Track | Summary |
|---|---|---|
| `1a6baab` | Follow-up 1 | rv32a test assertion fix (+7/-2, test-only) |
| `25449db` | Follow-up 3+4 | Silicon scenario state-resets, 10 scenarios (+333/-1, HLD-4 folded in) |
| `41f318c` | Follow-up 5 | Fuzz UX pre-dispatch on `--class` (+150/-5) |
| `da56c50` | Follow-up 2 | PMP phase-1 CSR model + scope-reduced fuzz (+787/-15) |

**Agent team (15 agents across two phases):**
- Phase 1 (HLD design, parallel): 5 Plan agents → 5 HLDs (1 redirect, 2 signed off unchanged, 2 signed off with clarifications) → implementation kickoff.
- Phase 2 (implementation + review + apply, parallel where possible):
  - Wave 1 parallel: Impl-1 + Impl-3 + Impl-5 (no file overlap).
  - Per-impl review (3 reviewers) + apply (not needed — no apply agent spawned because reviews came back clean).
  - Wave 2 solo: Impl-2 (PMP — wider blast radius).
  - Impl-2 stopped at the wall when fuzz smoke surfaced HLD-unpredicted divergences (correct TDD behaviour). Scope-reduction decision routed back through a follow-up agent. Validation + review combined into one pass.
  - Final audit agent.

**Open follow-ups (for future campaigns):**
- **Silicon soak verification** — Arthur to run `test_rp2350_silicon --soak 30m --seed 5831149830256 --filter "pwm|pio0|uart0"` on probe `<RP2354 probe serial>`. Expected `total_fail = 0`. Campaign acceptance gate.
- **PMP phase-2**: L-bit + reset ritual + `NUM_ENTRIES` expansion + QEMU `pmp_gran` divergence handling (symmetric low-bit mask OR CPU-option change).
- **PMP phase-3**: fetch/load/store fault delivery. Requires U-mode support.
- **Datasheet §3.7.9 entry-count resolution** (needed before phase-2 `NUM_ENTRIES` setting).
- **Reproduce-hint bug** (found during Reviewer-5): prints `--class all` for no-class runs, which isn't a valid CLI token. Minor, pre-existing.
- **HLD §3.3 correction** (pmp_gran divergence): documented in HLD V2 addendum for future phase-2 author.
- **Narrow-audit / PWM / probe-diff concurrent work** in the dirty tree: untouched; those are other agents' in-flight work.

**Campaign verdict: SHIP.** All five follow-ups closed with proper TDD (HLD → review → impl → review → apply → commit → audit).
