# 2026.04.17 — JRN — OneROM Stress Harness Implementation

## Mission

Implement the OneROM Stress Harness per `wrk_docs/2026.04.17 - HLD - OneROM
Stress Harness.md`. Supervisor journal — I coordinate agents for
implementation, review, apply-feedback, and devil's-advocate pressure; I
don't test or debug directly.

## Plan (stages)

1. **Stage 0 — pre-flight.** Probe the 1541 fixture: confirm `lift_shadow_from_flash`
   works, verify ROM set count, inspect pin invariant. If the CPU-mode sync
   PC range or the patcher offset don't transfer cleanly from `test-sdrr-0`
   to `1541`, surface that now before writing the binary.
2. **Stage 1 — library TDD.** `onerom_stress` module: sweep generator,
   histogram aggregator, report formatter. Three pure-function unit tests.
3. **Stage 2 — PIO stress binary.** Wire `run_case` loop. First full-sweep run
   on 1541 fixture. Inspect for latency sawtooth + first-failure triage.
4. **Stage 3 — CPU stress binary + 1541-cpu fixture.** Produce
   `onerom-fire-24-a-rp2350-1541-cpu.bin`. Verify CPU-mode sync PC range
   (risk 1). Wire the CPU `run_case` loop.
5. **Stage 4 — devil's-advocate review.** Independent agent reviews both
   binaries + library, challenging design and correctness. Gaps feed back
   into stage 2/3 as needed.
6. **Stage 5 — plan-vs-implementation confirmation.** Independent agent
   compares final state against the HLD, reports completeness.

Commit after each stage (not in bulk). Each stage has its own code-reviewer
pass + apply-feedback loop before commit.

## Guardrails

- Use agents for legwork. Protect context. Remain aware of what each agent
  is doing; redirect quickly if scope creeps.
- TDD: histogram + sweep generator first, with failing tests.
- Devil's-advocate agent challenges at stage 4 with fresh eyes; not
  rubber-stamp.
- Hardcoded fixture paths; no env vars, no feature flags (per user).
- Don't overengineer — if a simpler approach satisfies the HLD, prefer it
  even if the HLD was more elaborate.
- Never touch `third_party/`. The 1541 fixture is pass-through.
- Commit attribution: upstream `0x4D44`.

## Log

### 2026-04-17 — kick-off

- HLD complete, pressure-tested, risks added.
- Stage 0 probe delegated next.

### 2026-04-17 — Stage 0 close (clean)

All six HLD assumptions hold for the 1541 fixture:
- `lift_shadow_from_flash_pub(flash, 0)` returns 64 KB with 253 unique non-zero bytes — real content.
- 4 rom_sets, 65536 bytes each; `data_ptr`s at 0x1001_0000..0x1004_0000.
- A11=A12=1 invariant valid (cs2/cs3 = GPIO12/15).
- CPU serve-loop PC range 0x1000_0926..=0x1000_0930 is **byte-identical** between the two fixtures (firmware code below 0xc000 matches).
- Patcher offset 0xc328 is 64 bytes of 0xff in the 1541 fixture too — reusable.
- `ADDR_PINS = [7,6,5,4,3,2,1,0,10,11,14,15,12]` identical.

Open HLD risks 1 and 2 **closed** by this probe. Risks 3 (PIO residue sawtooth at 2048 scale) and 4 (per-case report noise) remain implementation-time concerns.

### 2026-04-17 — Stage 1 kick-off

Delegating `onerom_stress.rs` library with TDD.

### 2026-04-17 — Stage 1 close (`69805c4`)

- Library landed: `generate_sweep_cases()`, `Histogram`, `compute_histogram`, `format_report`, `expected_byte_for` helper.
- 4 unit tests green (`histogram_of_empty_is_zeroed`, `histogram_percentiles_nearest_rank`, `histogram_percentiles_edge_cases`, `generate_sweep_cases_covers_full_range`).
- Full mdpicoem-harness lib suite 337 → 341.
- Code-review apply-feedback loop landed 7 deltas: dropped unused `shadow` param, extracted `FAST_NS/STANDARD_NS` constants, guarded `cycles_to_ns`, matched neighbour mean order-of-ops, strengthened percentile + sweep tests, added `#[must_use]` on `expected_byte_for`.

### 2026-04-17 — Stage 2 (PIO binary)

Transient build-break during this stage: concurrent engineer added an uncommitted `pub mod riscv_gen;` to `lib.rs` pointing at a missing file. Handled by stashing just that file's diff long enough to build + run my binary, then un-stashing so their state is preserved on disk.

Implementer produced `onerom_stress_pio_rp2350.rs` (168 lines). First run on 1541.bin rom_set 0:

```
cases    : 2048
pass     : 2048
fail     :    0
latency (ns):  min 126 / p50 226 / mean 226 / p95 240 / p99 240 / max 253
ROM speed class: slow (mean 226 ns)
```

No sawtooth. Residue-buildup risk (HLD Risk 3) did not materialise at 2048-case scale — the drain-between-cases logic from the 15-case sweep scales cleanly.

Code-reviewer pass: no Required items. Two nits applied inline (clarifying comment on the `let _ = oracle.run_case(...)` discard; reviewer's `test = false` suggestion intentionally skipped to match the neighbour oracle binary's convention).

### 2026-04-17 — Stage 2 commit (`a482d09`)

Committed the PIO binary + Cargo.toml bin entry + journal update. Intentionally did NOT commit:
- `Cargo.lock` (concurrent engineer's touch)
- `lib.rs` (concurrent `pub mod riscv_gen;` line)
- `crates/mdrp2350/src/peripherals/uart.rs`, `silicon_scenarios.rs`, `tech_debt.md` (all concurrent engineer's WIP)

### 2026-04-17 — Stage 3 CPU binary + fixture

Fixture `onerom-fire-24-a-rp2350-1541-cpu.bin` produced via the metadata patcher (327 680 bytes, same size as the PIO fixture). New stress binary `onerom_stress_cpu_rp2350.rs` mirrors Stage 2, using `CpuServingOracle::run_case` and mapping `CpuVerdict → Verdict` for the shared histogram/report path.

**First run result** against 1541 rom_set 0:

```
cases    : 2048
pass     :  140
fail     : 1908
latency (ns) on passes: min 93 / p50 266 / mean 260 / p95 266 / p99 266 / max 266
```

All 1908 failures are `NoResolve` (mapped from `CpuVerdict::DataPinsNotDriven` — OEN never went to 0xFF within the 400-cycle `PER_CASE_TIMEOUT`). Latency on the 140 passing cases is clustered at 40 cycles — the `ZERO_BYTE_TRUST_TIMEOUT_CPU` fallback. Passes concentrate at zero-byte shadow offsets.

**Contrast with 15-case CPU oracle (previously): 15/15 PASS, min latency 9 cycles, bimodal (fast transition path vs 40-cycle zero-timeout).** Large-scale sweep doesn't hit the fast transition path for most cases — the CPU stays in the wait-loop (OEN low) across consecutive cases.

Triage delegated next.

### 2026-04-17 — Stage 3 triage outcome

Agent traced the 1541 CPU firmware's wait-loop to `0x1000_09A4..=0x1000_09B0` and dumped its working registers. Key finding: **the 1541 fixture's CPU firmware reads CS from GPIO0**, not GPIO13. Register state at case entry: `R4=SIO_GPIO_IN, R8=R9=0x00000001` → wait loop only exits when bit 0 of `LDRH R0,[R4]` is high.

Our harness drives CS via `GPIO_CS1=13` (set high for gap, low for stim). GPIO0 is only incidentally high in the stim pattern when `addr_bits & 0x80 != 0` (because `ADDR_PINS[7] = 0`). Empirically:

```
bit7=1 (GPIO0 high during stim): pass=140, fail=884
bit7=0 (GPIO0 low during stim):  pass=0,   fail=1021
```

Zero passes when GPIO0 is low, confirming the firmware uses GPIO0 as its select.

Root cause is firmware-specific pin configuration baked into the 1541 CPU-mode metadata. The fix belongs in `CpuServingOracle` (parameterise over a `PinProfile` read from fixture metadata), which is out of scope — would risk regressing the 15-case CPU oracle the wave is explicitly not allowed to destabilise.

**Resolution**: land the stress CPU binary as-is with an explicit `Known limitation` header comment pointing at the library constraint, log tech_debt entry, and document as a latent regression target that will flip to 2048/2048 once the pin-profile parameterisation lands. Proceed to Stage 4.

### 2026-04-17 — Stage 4 devil's-advocate

Independent reviewer raised three substantive concerns:

1. **PIO latency "distribution" is misleading.** Only 5 unique cycle values across 2048 cases — `p95=p99=240 ns` isn't a tail, it's "the higher of two steady-state buckets". Reader could misquote "226±14 ns" as calibrated timing. **Applied:** added `unique_cycles` field to `Histogram`, surfaced in report as `unique cycle values: N`, with a warning note when N ≤ 3. New TDD test covers the three shape cases (all-same, step, continuous). Post-fix PIO run shows `unique cycle values: 5` — still narrow but honestly reported.

2. **CPU 140 passes are a double-coincidence, not real coverage.** Every "pass" requires both (a) stim pattern setting GPIO0 high AND (b) expected shadow byte = 0x00. No case currently verifies actual serve-path byte delivery. **Applied:** expanded both the CPU binary's header comment and the tech_debt entry to call this out explicitly — pass count should be read as "0 of 2048 cases are real serve-path verifications" until the pin-profile fix lands.

3. **Is the CPU diagnosis complete?** Reviewer questioned whether tripwire/envelope/timeout might also contribute. After their own instrumentation, rejected — pin-map mismatch is the single factor.

Other flagged items (library type coupling, concerns about test adequacy) were nits — log-and-ship acceptable.

Library test count: 5 (was 4). Harness lib suite 341 → 342 green.

Stage 4 complete.

### 2026-04-17 — Stage 5 plan-vs-impl close

Independent verifier audited all 10 HLD sections against the four commits. Verdict: **COMPLETE**.

Every HLD item is met; the test count expansion (3 → 5) is tracked to specific review-driven defects (boundary bugs + Stage 4 `unique_cycles` field), not scope creep. All four risks the HLD named were properly handled (Risk 1 surfaced at a different PC address but was resolved via a local constant in the CPU binary, not oracle mutation; Risk 3 PIO sawtooth did not materialise).

Verifier corrected my Cargo.toml version confusion — HEAD ships at 0.30.0 (per HLD). The 0.31.0 I was seeing in earlier system-reminders was a concurrent engineer's uncommitted bump sitting in my working tree.

Final state:
- Four commits on main: `69805c4` (lib), `a482d09` (PIO bin), `3b9aeed` (CPU bin + fixture), `0008235` (Stage 4 honesty fixes).
- `onerom_stress_pio_rp2350` reports 2048/2048 PASS on 1541.bin with transparent `unique cycle values: 5` note.
- `onerom_stress_cpu_rp2350` retained as a latent regression target with loud documentation of the pin-map library gap (tech_debt.md + binary header + commit message).
- Library: 5 TDD tests, all green. Harness lib suite: 365 tests green.
- One follow-up queued: parameterise `CpuServingOracle` over a `PinProfile` read from fixture metadata (~1 day), after which the CPU stress binary flips to full 2048/2048 without any binary-side change.

Mission complete.
