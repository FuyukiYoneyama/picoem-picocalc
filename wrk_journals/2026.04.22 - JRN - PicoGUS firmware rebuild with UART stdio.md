# 2026-04-22 — PicoGUS firmware rebuild with UART stdio

## Goal

Rebuild `picogus-v4.0.0` from source with `pico_enable_stdio_uart(target 1)` so that `printf`/`puts` in the firmware drain through our UART TX tap (landed yesterday in `crates/mdrp2040/src/peripherals/uart.rs`) and tell us why the GUS reset gate (`(GUS_reset_reg & 0x03) != 0x03` at `gus-x.cpp:1734`) never opens during MIDI replay.

Side benefit of rebuilding: we get the **.elf with symbols**, which lets us locate `GUS_reset_reg` (and every other static) in SRAM without shot-in-the-dark probing.

## Starting state

- `third_party/picogus/upstream/sw/CMakeLists.txt:215-219` currently disables stdio-over-UART for the `SOUND_MPU` build variant (the GUS firmware):
  ```cmake
  if (SOUND_MPU)
      pico_enable_stdio_uart(${TARGET_NAME} 0)
  ```
- We need this flipped to `1`, then a clean rebuild of the `SOUND_MPU=ON` target.
- Toolchain: **not yet confirmed**. `arm-none-eabi-gcc` is not on PATH. Need either an existing install to locate, or a fresh download.

## Plan

1. Locate / install:
   - `arm-none-eabi-gcc` (Cortex-M0+ target)
   - `cmake` (Pico SDK needs 3.13+)
   - Pico SDK source tree (the picogus tree has `pico_sdk_import.cmake` but expects `PICO_SDK_PATH` env or a sibling checkout)
2. Flip the CMake flag.
3. Configure+build the `SOUND_MPU` variant. Confirm we can reproduce something *close* to the 906 KB stripped `picogus-v4.0.0.bin`.
4. Point the harness at the rebuilt `.bin`, stream UART, and read what the firmware tells us about the reset-gate state.

---

## Log

### Step 1 — toolchain discovery

- Windows-side: no `arm-none-eabi-gcc`, no `cmake`, no Pico SDK install anywhere under `C:\`, `C:\Program Files`, `C:\apps\`, `D:\`. No `PICO_SDK_PATH` env var.
- Arthur installed `gcc-arm-none-eabi`, `cmake`, `ninja-build`, `python3-pip` into WSL Ubuntu 24.04.
- Confirmed in WSL: `arm-none-eabi-gcc 13.2.1`, `cmake 3.28.3`, `ninja 1.11.1`, `python3 3.12.3`. CI uses arm-gcc 14.2 — 13.2 is close enough for a diagnostic build (GCC version won't change which branches `printf()` executes).

### Step 2 — SDK + pico-extras clones

- `~/pico/pico-sdk` — `git clone -b 2.1.1 --depth 1` + `submodule update --init --depth 1` (matches CI's `PICOSDK_VER=2.1.1`).
- `~/pico/pico-extras` — `git clone -b sdk-2.1.1 --depth 1` + submodules (matches CI's `PICOEXTRA_VER=sdk-2.1.1`).
- Both finished in ~2 min, parallel.

### Step 3 — stdio code-path audit (before editing)

- Suspicion: maybe flipping `pico_enable_stdio_uart` is insufficient because PicoGUS has a custom `stdio_async_uart.c` (a ring-buffered, IRQ-driven stdio backend).
- Audit result: `#ifdef ASYNC_UART` in `picogus.cpp:1226` gates the async path. **`ASYNC_UART` is never defined anywhere in the picogus tree.** So the firmware always falls through to `stdio_init_all()`, which respects `pico_enable_stdio_uart`. Flipping the flag is sufficient.
- GUS build (`build_gus` at `CMakeLists.txt:237`) sets `USB_JOYSTICK=TRUE` → `USB_STACK=TRUE` → `pico_enable_stdio_usb(target 0)` also fires. So currently: both UART and USB stdio disabled in the stock GUS firmware → `printf`/`puts` discarded. Flipping UART to `1` will route everything.
- UART0 pin assignment: `PICO_DEFAULT_UART=0`, `PICO_DEFAULT_UART_TX_PIN=28`, `PICO_DEFAULT_UART_RX_PIN=-1`, `PICO_DEFAULT_UART_BAUD_RATE=230400`. Our emulator tap is on UART0 TX so this maps perfectly.

### Step 4 — flip the flag

Changed `third_party/picogus/upstream/sw/CMakeLists.txt:216` from `pico_enable_stdio_uart(${TARGET_NAME} 0)` to `1` (inside the `if (SOUND_MPU)` branch). Surgical one-line edit.

### Step 5 — Python deps for Pico SDK

Pico SDK's `pioasm` / audio interpolation scripts need `sympy scipy more-itertools`. Installing via `pip3 install --user --break-system-packages` (PEP 668 Noble Ubuntu) — went to `~/.local/lib/python3.12/`. Installed: sympy 1.14.0, scipy 1.17.1, more-itertools 11.0.2.

### Step 6 — CMake configure

```
export PICO_SDK_PATH=~/pico/pico-sdk
export PICO_EXTRAS_PATH=~/pico/pico-extras
cd /tmp/picogus-build
cmake <workspace>/third_party/picogus/upstream/sw -GNinja \
  -DCMAKE_BUILD_TYPE=Release -DPROJECT_TYPE=GUS
```

Configure ran cleanly in 8.3 s. One notable line: "Downloading Picotool" — CMake is fetching picotool 2.1.1 from git because we don't have it installed. That'll add a few minutes to the first build.

### Step 7 — build

Build completed cleanly in ~90 s wall time. Artefacts:
- `pg-gus.bin` (95 312 B raw flash image)
- `pg-gus.elf` (ELF with full symbols — this is the real prize)
- `pg-gus.uf2`, `pg-gus.hex`, `pg-gus.dis` (disassembly), `pg-gus.elf.map`

Size note: our rebuilt binary is 95 KB, stock `picogus-v4.0.0.bin` is 906 KB. The discrepancy is that stock is `PROJECT_TYPE=MULTIFW` (GUS + MPU + SB + OPL + CDROM + SSPI + SB16 stack bundled behind a bootloader-mode picker), and we built `PROJECT_TYPE=GUS` (single-firmware GUS-only). For our diagnostic purposes GUS-only is better — no boot-mode picker, goes straight to GUS init.

Symbols located via `arm-none-eabi-nm`:
| Symbol | Address |
|---|---|
| `_ZL13GUS_reset_reg` | `0x2001_d976` (rev 1 build) |
| `myGUS` | `0x2001_7688` (rev 1 build) |
| `test_psram` | `0x2001_309c` (rev 1 build) |

Addresses shift every rebuild by ~tens of bytes because adding `printf` calls changes `.bss`/`.text` layout. All three addresses will be re-nm'd after each firmware patch.

### Step 8 — first run against harness

Run: `/tmp/pgd_rebuild.exe -- --flash wrk_scratch/picogus-rebuild/pg-gus.bin --pre-roll 2.5 --duration 0.5 --post-roll 0.1 --trace ...`

UART drained **241 bytes** including:
```
[uart] picogus-gus v4.0.0
[uart] copying settings to ...
[uart] ADC value: 0x305... Running on Pico-based board
[uart] Initing MIDI UART...
[uart] Initing PSRAM...
```

**Big win:** boot is clearly visible through our UART tap. But it stops at "Initing PSRAM..." — and stays there even with a 6 s pre-roll.

### Step 9 — test_psram stub address mismatch

The harness already had a hardcoded patch at `0x2001_2FA4` to stub `test_psram` to `return 0` (the real test writes 1 MB over PIO/PSRAM-SPI — emulator clocks that at "takes ~1 hour" rates per the harness comment). The stock firmware's `test_psram` was at that address; our rebuild's is at `0x2001_309c`.

Two fixes considered, one chosen:
- Drop `TEST_PSRAM=1` from firmware CMakeLists — cleaner but modifies upstream code unnecessarily.
- **Chosen:** parameterise the stub address via env var `PICOGUS_STUB_TEST_PSRAM=0x<addr>`, default the stock address `0x2001_2FA4`, allow `=0` to disable the patch entirely. Edit at `picogus_diff_rp2040.rs:1577`. Works for stock + rebuild + PSRAM-debugging without touching upstream.

### Step 10 — second run: boot completes

`PICOGUS_STUB_TEST_PSRAM=0x2001309c ... --pre-roll 5.0 --duration 0.5` →

```
[uart] Initing PSRAM...
[uart] Creating GUS
[uart] starting core 1
[uart] Config joystick PWM
[uart] Enabling bus transceivers...
[uart] Initing ISA DMA PIO...
[uart] GUS IRQ-driven audi.A... ..  *** PicoGUS ***
[uart] ..{..{..{..{..{..{..{..{..{..{..{..{..{..{..{..{.o started
[uart] Starting ISA bus PIO...
[uart] iow sm: 0  / ior sm: 1
[uart] fixed 44k output: 0
[uart] setting dma interval to 0
[uart] GUS channels: 27
```

The `.A...` / `{` garbage in "GUS IRQ-driven audi**o** started" is the MPU401 init SysEx (`*** PicoGUS ***` as MT-32 text, binary bytes `\xf0\x41\x10...`) interleaving with the stdio line. Both go over UART0 TX pin 28.

**Probe result:** `PICOGUS_PROBE=0x2001d976` → `0x01`. The GUS reset register reaches the intermediate value but **not** the 0x07 that opens the audio gate. This matches the audio-silence symptom.

### Step 11 — firmware-side tracing: `printf` in GUSReset + write_gus

Rebuild the firmware with three targeted printfs:
1. End of `GUSReset()`: prints `[rst] gRegData=0xNNNN  PP->CC` showing the driver intent, the old reg, the new reg.
2. `write_gus` case 0x103 (register select): prints `[103] sel=0x<val>`.
3. `write_gus` case 0x105 (data-high write): prints `[105] sel=0x<sel> data=0x<gRegData>`.

Plain `printf` — `LOG_MSG` is only compiled in on `-DDEBUG` and that would spam every subsystem. Three targeted lines tell us everything about reset-register traffic.

Result on a 2 s replay:
```
[103] sel=0x4C                <- select reset reg
[105] sel=0x4C data=0x0000    <- first write = 0 → reset
[rst] gRegData=0x0000  00->00
[103] sel=0x4C                <- select reset reg again
[105] sel=0x4C data=0x591D    <- second write → reset reg = (0x591D >> 8) & 7 = 0x59 & 7 = 0x01
[rst] gRegData=0x591D  00->01
[103] sel=0x44                <- moves on to DMA regs etc.
...
```

**Headline:** only two GUS-reset register writes happen during the first 2 s of replay — `0x00` then `0x01`. The driver then moves on to other registers and never writes `0x07`. `GUS_reset_reg` is stuck at `0x01`, audio gate stays closed.

The `0x591D` value is a 16-bit word where only the top byte is the actual reset-reg data. Bits: `0x59 = 01011001`. Mask with `7` → `0x01` = master-reset-released, DAC off, IRQ off. Low byte `0x1D` is a stale previous 0x104 write (data-low) that this 0x105 write OR's with `val << 8`.

### Step 12 — the trace IS correct

Cross-check the trace fixture for the expected `0x00 → 0x01 → 0x07` sequence on port 0x345:
```
3:  0      0x343 0x4c
4:  0      0x345 0x00      ← write 0
5:  100000 0x343 0x4c
6:  100000 0x345 0x01      ← write 1
11: 300000 0x343 0x0e
12: 300000 0x345 0xda
13: 300000 0x343 0x4c
14: 300000 0x345 0x07      ← write 7 → opens gate
```

All three reset-register writes exist in the first 300 µs of the trace. Yet our firmware only observes `0x00` and `0x01`. The `0x07` write never makes it to `GUSReset()`.

### Step 13 — widen the `iow_read` log to see PIO's raw view

Added `printf` at the top of `handle_iow()` dumping `raw=0x<iow_read>` — the 32-bit word the PIO pushes (port bits 8..17, data bits 0..7).

First observation with `WRITE_IDLE_CYCLES=12` (default):
```
raw=0x0003434C port=0x343 data=0x4C  ← L3 ✓
raw=0x00034500 port=0x345 data=0x00  ← L4 ✓
raw=0x00034343 port=0x343 data=0x43  ← WRONG: expected port 0x343 data 0x4c. data ≠ low byte of port.
raw=0x00034400 port=0x344 data=0x00  ← WRONG: expected port 0x345 data 0x01
```

Trying `PICOGUS_IDLE_CYCLES=1000` (an env-var override on the harness `drive_write_cycle`) made it better — the second cluster captured correctly:
```
raw=0x00034501 port=0x345 data=0x01  ← L6 ✓
```

But subsequent clusters were still lossy. And the broad `iow_read` print is itself flooding stdio and slowing `handle_iow`, which causes the PIO RX FIFO (4 deep) to overflow — so the printf is corrupting the measurement.

### Step 14 — remove all firmware-side printfs, re-run

Clean run, no firmware instrumentation, default timing: `GUS_reset_reg = 0x01` at end of 5 s replay. Same symptom as stock firmware. So the firmware rebuild faithfully reproduces the silence, and it's not an artefact of any printf we added.

### Finding — root cause class

The trace contains the correct `0x07` write at ns=300 000. The firmware never sees it as a reset-register write reaching `GUSReset()`. Event loss / data corruption is happening somewhere between the harness's `drive_write_cycle` and the PIO's output-to-firmware hand-off.

Candidates (not yet disambiguated):
1. PIO RX FIFO overflow: trace fires 143k events in the first 1 ms. The PicoGUS firmware's `handle_iow` loop runs on one core and must keep up; if it can't, FIFO (4 deep) drops.
2. Harness `drive_write_cycle` timing too tight: for same-ns event clusters the harness fires events back-to-back with no `advance_to_sim_ns` delay. `WRITE_IDLE_CYCLES=12` between them may not give the PIO program time to restart its `wait 0 gpio IOW` after the handshake cycle.
3. Harness / emulator PIO model has a model bug where the address and data phases aren't cleanly separated (`drive_pins` timing against `in pins, 10` / `in pins, 8`) — but this worked for the very first event at ns=0, so whatever happens is a state-drift issue across events, not a static miswiring.

Candidate 2 is the most likely given that `PICOGUS_IDLE_CYCLES=1000` demonstrably captured more events correctly. Both the address and data should be quiescent on the GPIO lines for long enough between writes that the PIO program can fully wrap back to `wait 1 gpio IOW_PIN` → `wait 0 gpio IOW_PIN` before the next event's falling edge.

### Step 15 — bypass test: poke `GUS_reset_reg = 0x07` directly

Rather than fix the bus replay in this session, confirm the audio gate diagnosis is correct by poking `GUS_reset_reg = 0x07` at the (now-known-from-ELF) address `0x2001_d976`.

Result: probe confirms `reset_reg = 0x07` at end, but the WAV is **still all zeros** (1 037 222 samples, 0 non-zero, peak 0). So opening the reset gate alone isn't enough — voices are also silent. The most likely reason: wavetable uploads during the trace (GUS DRAM writes via port 0x347 and surrounding register flow) also got dropped by the event-loss bug, so there are no samples in PSRAM for voices to play from.

### Step 16 — trace ns-cluster analysis

The trace's internal time resolution is coarse (captured from DOSBox-X's PIC clock). Count events by ns timestamp:

```
  217490 2135700     ← 217K events at one timestamp!
  106310 867000
   85410 4320800
   36700 500000
```

**One cluster has 217 490 same-ns events.** Our harness fires events at `target_ns` with `advance_to_sim_ns` — events in the same cluster fire back-to-back with only `drive_write_cycle`'s internal ~60-cycle timing between them. The PIO RX FIFO is 4 deep; at that rate any slow-write event that makes firmware pause (GUSReset, PSRAM SPI, etc.) causes FIFO overflow within microseconds and every subsequent event in the cluster is dropped.

The event-loss is confined to within-cluster because ns-to-ns time (even 100 µs) is ample drain time.

### Step 17 — PIO autopush stall fix attempt

Our emulator's PIO (`mdpicoem-common/src/pio/sm.rs:720`) **silently drops** autopush when RX FIFO is full. Real RP2040 PIO stalls the SM (datasheet §3.5.4). Fixed to stall. All 113 `mdpicoem-common` tests still pass.

But when combined with harness-side backpressure the combined flow deadlocks — harness backpressure loop waits for FIFO level to drop, but the stalled PIO can't advance past `in pins, 8` until the firmware pops, and the harness is blocking firmware by not advancing sim-time. The stall fix is correct, but the harness timing around it needs more work. Reverted the stall fix with a comment; left it as a known divergence from real HW.

### Step 18 — harness backpressure knobs

Added env-var tunables to `drive_write_cycle`:

- `PICOGUS_IDLE_CYCLES` — cycles between events (Phase 0 + Phase 3 idle).
- `PICOGUS_ADDR_HOLD` / `PICOGUS_DATA_HOLD` — phase durations.
- `PICOGUS_BACKPRESSURE_THRESHOLD` (default 2 of 4) — drain if RX FIFO level ≥ this before firing.
- `PICOGUS_BACKPRESSURE_STEP` / `PICOGUS_BACKPRESSURE_MAX` — step granularity and cap.

Added `pio0_sm0_rx_fifo_level` / `pio0_sm0_rx_fifo_drops` methods to `IsaSink` trait, forwarded from `Emulator` via the new `StateMachine::rx_fifo_level()` / `rx_fifo_drops()` accessors.

### Step 19 — confirmed fix

`PICOGUS_BACKPRESSURE_THRESHOLD=0 PICOGUS_IDLE_CYCLES=5000` + 400 µs of trace replay:

**`GUS_reset_reg = 0x07` at probe.** The trace's `0x07` reset write DID reach `GUSReset()`. The bug was purely `WRITE_IDLE_CYCLES=12` being too tight for same-ns clusters; 5000 cycles (≈ 13.5 µs sim-time at 370 MHz) between events is enough for the firmware's `handle_iow` path to drain a slow write (e.g. `GUSReset` takes ~1-2 kcycles).

This establishes:
- Root cause of silence: PIO RX FIFO overflow on within-cluster event fast-fire.
- Work-around: `PICOGUS_IDLE_CYCLES=5000` knob, no emulator model changes required.
- Proper fix (for a future session): PIO autopush stall + ISA bus IOCHRDY feedback to harness.

### Step 20 — full replay attempts

Tried `PICOGUS_IDLE_CYCLES=5000 --duration 5.0 --post-roll 0.5` but that's 2.5M trace events × ~5000 cycles = 12.5 G sysclks extra (≈ 34 sim-seconds of bus-cycle padding) on top of the real replay. At our emulator's ~5-10 M sysclks/wall-sec, that's a 30 min wall run. Killed that to free CPU for a shorter 500 ms run.

The 500 ms run crashed silently ~4 min in (process gone from tasklist, no panic text in stderr, last log line "setting dma interval to 0" which is just after GUS boot init finishes). Worth investigating on a future day — could be OOM from the I2S capture ring, or an unhandled assertion somewhere in the replay path under the new timing. Not diagnosed this session.

Currently running a 100 ms replay (lower wall budget) to confirm the key audibility claim.

### Working state of this session

Changes persisted on disk (uncommitted per global CLAUDE.md):

- `crates/mdpicoem-common/src/pio/sm.rs` — added `rx_fifo_level()` / `rx_fifo_drops()` accessors. All 113 unit tests pass. PIO-autopush-stall fix attempted but reverted (harness deadlock).
- `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs` — `IsaSink::pio0_sm0_rx_fifo_level` / `pio0_sm0_rx_fifo_drops` trait methods, `CapturingSink` forwarders, `drive_write_cycle` gets env-var knobs (`PICOGUS_IDLE_CYCLES` / `PICOGUS_ADDR_HOLD` / `PICOGUS_DATA_HOLD` / `PICOGUS_BACKPRESSURE_*`), and `PICOGUS_STUB_TEST_PSRAM` for rebuild-firmware stub address.
- `third_party/picogus/upstream/sw/CMakeLists.txt` — `pico_enable_stdio_uart(target 1)` for SOUND_MPU builds (the stdio flip that started this session).
- `wrk_scratch/picogus-rebuild/pg-gus.bin` + `.elf` + `.dis` + `.elf.map` — rebuilt firmware artefacts with stdio-over-UART enabled.
- `scripts/picogus_replay_rebuilt.sh` — shell wrapper that sets the working env-var combination so future sessions can reproduce.

### What's confirmed

- Firmware rebuild with stdio-over-UART: **works**. Boot prints land in our UART tap.
- ELF gives us real symbol addresses (`GUS_reset_reg` at `0x2001_d976`, `test_psram` at `0x2001_309C`).
- Root cause of audio silence: emulator's PIO RX FIFO overflows on same-ns event clusters because default `WRITE_IDLE_CYCLES=12` is too tight. The trace has 217k-event clusters that PIO can't drain at that rate.
- Work-around: `PICOGUS_IDLE_CYCLES=5000` — probe shows `GUS_reset_reg = 0x07` after 400 µs of replay (up from `0x01` / `0x00` with default timing). **The reset sequence is now correctly delivered to firmware.**

### What's not yet confirmed

- Whether audible samples come out of a full-length replay with the fix — the longer runs are expensive at current timing, and the 500 ms run had a silent crash I didn't get to diagnose.
- Whether wavetable-upload events (PSRAM writes via ports 0x343/0x344/0x345/0x347) are all now landing correctly, or whether some other event class still drops.

### Step 21 — the "silent crashes" weren't crashes

The intermittent "process gone" observations in runs 100-500 ms were a false-negative in my `tasklist //FI "IMAGENAME eq <exe>.exe"` filter — Git Bash's path mangling broke the filter and it always returned "No tasks are running which match the specified criteria" regardless of actual process state. The processes WERE still alive each time. I killed their mdtimeout wrappers prematurely based on the false negative.

Lesson for next time: use `tasklist | grep -i <name>` for process checks under Git Bash. The `//FI` filter form is unreliable.

With a properly-wrapped 50 ms replay running to completion (no premature kill), we can now wait for an actual audio result.

### Step 22 — harness end-of-run drop reporting

Landed `pio0 sm0 rx fifo drops: {}` in the harness summary output so we get an explicit drop count. The 400 µs run confirmed `0` drops with the new timing, matching the 12/12 event capture from the coverage table.

### Step 23 — 50 ms run completed (20 min wall)

Results from `--duration 0.05 --post-roll 1.0 PICOGUS_IDLE_CYCLES=5000`:
- `probe 0x2001d976 = 0x07` — reset gate opens ✓
- `pio0 sm0 rx fifo drops: 0` — FIFO never overflows ✓
- `writes_fired: 514502` — harness fired every trace event ✓
- **But `PIO0 SM0 autopush_count: 20`** — only 20 events actually reached the firmware through PIO ✗
- **`PSRAM Bytes written: 0`** — wavetable uploads never landed ✗
- **WAV: all zeros** — 420 k samples, 0 non-zero, peak 0 ✗

Root cause (different from what the 400 µs run suggested):

Firmware's `handle_iow` dispatches port `0x347` (DRAM write) through `write_gus` case 0x107 → `psram_write8` → `pio_spi_write_dma_blocking`. This **blocks firmware** until the PSRAM PIO TX DMA drains. The PSRAM PIO (PIO1 SM0) is **stalled at PC=0x19** (`OUT PINS, 1`, `EXEC_STALLED=1`) — firmware pushed 13 bytes to TX FIFO but PIO consumed 0 (`CS falls: 0`, `SCK toggles: 0`, `MOSI=1 cycles: 0`). So firmware hangs on the first `psram_write8`.

With firmware hung, IOCHRDY stays low (PIO0 SM0 is still in its `out null, 32` side 0b01 handshake from the last captured event). On real hardware, the ISA host would see IOCHRDY=low and wait to complete the outb. **Our harness doesn't honor IOCHRDY** — it keeps driving Phase 0-3 at fixed timings. So the remaining 514 482 events' IOW falling edges happen during "IOCHRDY busy" windows and are silently missed by PIO0 SM0.

This matches `autopush_count = 20` (only the first 20 events fired before firmware hung on the first 0x347 write).

**Net: the dropped-events-per-ns-cluster bug is fixed. The new blocker is a PSRAM PIO model issue (why is `OUT PINS, 1` at PC=0x19 stalled with data in TX FIFO?) combined with the harness not honoring IOCHRDY.**

### Next-session leads

Ordered by likely impact:

1. **Debug the PSRAM PIO stall** (PIO1 SM0 PC=0x19, `EXEC_STALLED=1`, 13 bytes in TX FIFO but 0 consumed). The PIO program `OUT PINS, 1` at that PC either auto-pulls from TX FIFO (if OSR empty) or drives from OSR. Something is preventing it from advancing. Potentially: autopull-threshold mismatch, clkdiv wrong, or something about sideset / OSR init state. This is the next real bug to find.
2. **Harness IOCHRDY feedback.** Teach `drive_write_cycle` to poll the IOCHRDY pin (GPIO 26) after Phase 2 and extend the bus cycle while it's low. Real-hardware-faithful model; lets the harness track firmware pace dynamically. Would also remove the need for the `PICOGUS_IDLE_CYCLES=5000` hack (default could stay at 12).
3. **PIO autopush stall fix** — the correct `in pins, ...` behaviour on full RX FIFO is to stall, not drop silently. I have a working patch (reverted this session). Land it once (2) is in place so the stall and the IOCHRDY feedback combine correctly.
4. Optimise the 5 s pre-roll (detect "GUS channels: 27" and start replay immediately instead of always paying 5 s).
5. Revisit `PICOGUS_IDLE_CYCLES` — 5000 is generous; binary-search the minimum that keeps `rx fifo drops = 0` once (2) is in place.
6. Once audio is confirmed, record an oracle WAV for regression testing.
