# 2026.04.17 — JRN — Coverage Gap Fill V11 Supervisor

Supervisor journal for the Phase 1 Bucket A coverage gap fill
(HLD: `wrk_docs/2026.04.17 - HLD - RP2350 Coverage Gap Fill V11.md`).

## Approach

Per-item cadence (HLD §8): implement → code-review → apply-feedback →
devil's-advocate → commit. Agent teams drive each step; supervisor
(this document) provides direction, not hands-on implementation.

Order per HLD: SYSINFO → TBMAN → GLITCH_DETECTOR → POWMAN.

Baseline: `mdrp2350` Cargo 1.9.1 (HLD §11 cited 1.8.3; intervening
tracks bumped it). Each item takes a Cargo minor bump: 1.9.1 → 1.10.0
(SYSINFO) → 1.11.0 (TBMAN) → 1.12.0 (GLITCH_DETECTOR) → 1.13.0
(POWMAN).

## Key files briefing

- `crates/mdrp2350/src/bus/peripherals.rs:48` — `sysinfo_read` target.
- `crates/mdrp2350/src/peripherals/inert.rs` — TBMAN, GLITCH_DETECTOR
  (HashMap storage today at lines 45 / 128).
- `crates/mdrp2350/src/peripherals/powman.rs` — POWMAN module (365 lines).
- `crates/mdrp2350/src/bus/mod.rs` — RESETS constants, `reset_bit_for_base`
  at line 213, `raise_irqs_u64` for NVIC pend.
- `crates/mdpicoem-harness/src/silicon_scenarios.rs` — SCENARIOS const;
  ALIAS_CLR at `silicon_scenarios.rs:26`.
- `crates/mdpicoem-harness/src/isr_scenarios.rs` — SCENARIOS const;
  VTOR write pattern at lines 1511-1534 (per HLD §3.2).

## Stage log

### Stage 1 — SYSINFO (done)

- Implementer landed CHIP_ID + PLATFORM + GITREF + scenario. Used
  un-cited guesses (CHIP_ID=0x2004_0927, PLATFORM=0x02) against the
  pre-change values.
- Code-reviewer flagged two MUST-fix items: PLATFORM flip lacked a
  source; GITREF=0 with full mask guaranteed silicon fail on first
  run.
- Apply-feedback #1 took the reviewer's "Safest" path: reverted
  PLATFORM to 0x01, masked GITREF, kept CHIP_ID as guessed.
- Devil's-advocate then (rightly) pushed harder on CHIP_ID and
  called the zero-masked GITREF an anti-pattern.
- Apply-feedback #2 did the work the first round should have done:
  WebFetched pico-sdk `sysinfo.h` + `platform.c`, derived
  `CHIP_ID = (PART_RP4 << 12) | (MANUFACTURER_RPI << 1) | STOP_BIT`
  = `0x0000_524D`, and deleted the GITREF observe entry entirely.
  CHIP_ID silicon mask set to `0x0FFF_FFFF` (REV nibble deferred
  to pre-flight).

**Landed**:
- `sysinfo_read` returns 4 offsets (CHIP_ID, PACKAGE_SEL, PLATFORM,
  GITREF) with authoritative pico-sdk-sourced values.
- Unit test `sysinfo_reads_hardcoded_readonly_fields`.
- Silicon scenario `sysinfo_readonly_fields` (3 observed words;
  GITREF deferred).
- Cargo 1.9.1 → 1.10.0.
- `cargo test -p mdrp2350` = 1249 passed. Harness builds clean.

**Carried forward to Stage 5 pre-flight**:
- PLATFORM bit layout (current 0x01 is historical LLD-draft value;
  SDK header says reset=0x00 but ASIC is likely fused to 0x02).
- CHIP_ID REVISION nibble (currently 0; mask `0x0FFF_FFFF`).
- GITREF_RP2350 value (not observed yet).

**Lesson**: web-sourcing should happen in the implementer pass, not
as salvage after DA. Fold WebFetch into future Stage briefs.

### Stage 2 — TBMAN (done)

- Implementer applied the Stage 1 lesson: WebFetched pico-sdk
  `tbman.h` up front, derived `TBMAN_PLATFORM_RESET = 0x0000_0001`
  from the documented `_u(0x00000001)` macro, cited the URL inline.
  Matches HLD §3.4 "assumption 0b01" exactly.
- Initial implementation overrode `Tbman::read32` (same pattern as
  `GlitchDetector::read32`) with HashMap fall-through for other
  offsets.
- Code reviewer: no MUST-fixes; one SHOULD-fix (HashMap fall-through
  is tech-debt-shaped — TBMAN has exactly one architectural
  register per the SDK header).
- Apply-feedback: collapsed `Tbman` to a unit struct, strict
  `read32` returns PLATFORM only at 0x00 and 0 elsewhere, `write32`
  no-op. `TBMAN_PLATFORM_OFFSET` made `pub` and imported by the
  harness (cleaner precedent than Stage 1's duplicated constants).
  `tbman_roundtrip` deleted (meaningless post-strictness).
- Devil's-advocate flagged scope-expansion items: write-fault
  semantics, alias-region read behavior, unmapped-offset reads are
  all emulator guesses not covered by the single-word oracle.
  These are genuine but **out of HLD §3.4 scope** (which specifies
  "one-shot read of `TBMAN_BASE + 0x00` at reset"). Deferred to
  Stage 5 pre-flight / V12.
- Process polish: pinned pico-sdk URLs in both `inert.rs` and
  `silicon_scenarios.rs` to commit SHA
  `a1438dff1d38bd9c65dbd693f0e5db4b9ae91779` (DA [PROCESS] item).

**Landed**:
- `Tbman` unit struct with strict PLATFORM-only read override.
- Unit test `tbman_platform_reads_silicon_value` (bus-level) +
  `tbman_platform_reads_silicon_reset` (module-level, with no-leak
  write-then-read assertion).
- Silicon scenario `tbman_platform_reads_silicon_value`.
- Cargo 1.10.0 → 1.11.0.
- `cargo test -p mdrp2350` = 1250 passed; catalogue invariants green.

**Deferred (V12 / Stage 5 pre-flight capture)**:
- TBMAN write-to-RO-register fault semantics.
- TBMAN alias-region (0x1000/0x2000/0x3000) read behavior.
- TBMAN unmapped-offset read behavior (emulator currently returns 0).

**Lesson**: establishing `pub` constants once (TBMAN_PLATFORM_OFFSET)
and importing at the scenario site is cleaner than duplicating
offsets between emulator and harness. Future stages should follow
this pattern; Stage 1 SYSINFO constants should be migrated in a
catch-up polish later.

### Stage 3 — GLITCH_DETECTOR (done)

- Implementer's WebFetch revealed a real HLD shorthand vs silicon
  divergence: there is no separate `CTRL` / `STATUS` — one
  16-bit `ARM` RW register at 0x00 with sentinel values
  (`ARM_VALUE_NO = 0x5bad`, anything else = force-arm). "ARM
  readback tracks CTRL" reduces to plain RW + `0x5bad` reset seed.
- Also surfaced: TRIG_STATUS is actually at offset **0x10**, not
  0x08 as the pre-Stage-3 code assumed. Cascade effect: stale W1C
  mask entry in `bus/w1c.rs` pointed at `0x4015_8008` which is
  SENSITIVITY (plain RW — the bogus W1C mask would corrupt narrow
  writes to SENSITIVITY). Also `tests_narrow.rs` had the same
  stale constant.
- Code-reviewer caught the offset cascade as SHOULD-fix.
  Apply-feedback replaced the stale `0x4015_8008 => 0xFFFF_FFFF`
  catalogue entry with `0x4015_8010 => 0x0000_000F` (pico-sdk
  TRIG_STATUS_BITS) and deleted the stale SENSITIVITY entry.
  Updated `bus/mod.rs` doc comment. Added post-write `ARM_MASK`
  to prevent upper-16-bit pollution.
- Devil's-advocate caught two real issues:
  - Warm-reset modelling gap: `GlitchDetector` had no `reset()`
    method, inconsistent with every other peripheral. Fix: added
    `reset()` (re-seeds `ARM_RESET`) and wired into
    `Emulator::reset()`. Unit test
    `glitch_detector_arm_resets_to_arm_value_no_on_warm_reset`.
  - Silicon scenario tautology: writing `ARM_VALUE_YES = 0x0000`
    and observing 0x0000 couldn't discriminate a working write
    from a silently-sunk one. Fix: write `0x0000_1234` instead
    (distinctive, not sentinel, not reset, fits ARM_BITS).

**Landed**:
- `GlitchDetector` HashMap seeded with `ARM_RESET = 0x5bad`.
  Plain RW with `ARM_MASK` applied. `reset()` method.
- Bus W1C catalogue corrected (0x4015_8010 instead of
  0x4015_8008).
- `pub` offsets + sentinels exported from `inert.rs`; harness
  imports.
- Unit tests: `glitch_detector_arm_readback_tracks_ctrl` +
  `glitch_detector_arm_resets_to_arm_value_no_on_warm_reset` +
  5 module-level tests.
- Silicon scenario `glitch_detector_arm_readback_tracks_ctrl`
  writes 0x1234 discriminator.
- Cargo 1.11.0 → 1.12.0.
- `cargo test -p mdrp2350` = 1254 passed; catalogue invariants
  green; workspace release build clean.

**Lesson**: HLD register-name shorthand can hide structural
divergence from silicon. Implementers MUST WebFetch the SDK
header first to sanity-check register layout before implementing
to an HLD spec. Stage 3 caught this; it is now a reproducible
rule.

### Stage 4 — POWMAN COUNT + MATCH (done)

The biggest stage. Implementer's WebFetch correctly mapped HLD
logical names to silicon registers and surfaced that the previous
"educated guess" POWMAN module had `AON_COUNT_LO=0x08` colliding
with silicon `VREG_STS=0x08` (firmware writing to our placeholder
would have hit silicon's VREG status).

**Silicon-correct register layout adopted from pico-sdk
(commit `a1438dff`)**:
- `READ_TIME_LOWER/UPPER` at 0x74/0x70 (RO COUNT view).
- `ALARM_TIME_15TO0..63TO48` at 0x84..0x78 (RW MATCH lanes).
- `TIMER` at 0x88 (RUN bit 1, ALARM_ENAB bit 4, ALARM W1C bit 6).
- `INTR/INTE/INTF/INTS` at 0xE0/E4/E8/EC (TIMER is bit 1).

**Code review caught two MUST-fix items**:
- Handler LDR encoding bug: `HANDLER_POWMAN_SENTINEL[1] = 0x4902`
  resolved (via PC-align rounding for non-word-aligned handler
  hw[1]) to load the sentinel, not the mailbox. Fix: opcode
  `0x4903` (`ldr r1, [pc, #12]`). Added regression test asserting
  loaded word values, not just target alignment.
- POWMAN password protocol: writes to ALARM_TIME / TIMER without
  `0x5AFE` in upper-16 are silently dropped on silicon. Fix: bake
  password into silicon scenario literals; add password-strip
  (Option A) to emulator write paths so emulator accepts the
  prefixed values consistently.

**Devil's-advocate caught two more blockers**:
- Pre-flight binary `smoke_powman_pacing_rp2350` was skipped
  per supervisor brief but HLD §8 explicitly mandates it. Fix:
  binary written (compile-only — Arthur runs it on silicon).
  Implements the HLD-specified `(CYCCNT, COUNT)` sample pairs +
  derived sys_clks/tick + register dump.
- ARCHSEL @ 0x20 collision risk: silicon offset 0x20 is NOT
  ARCHSEL on RP2350; the emulator-only tripwire would spurious-warn
  on real firmware. Fix: downgraded `warn!` to `trace!`; renamed
  the test to `powman_archsel_non_arm_write_fires_tripwire_once`
  to reflect tripwire-on-flag semantics, not warn-level emission.

**Other applied fixes**:
- Comment drift: scenario comments said runner pre-seeds mailbox
  to `0xDEAD_BEEF`; actually pre-seeds to 0. Comments corrected.
- Dead PRIMASK=1 in `powman_match_pends_nvic_line_45` removed
  (test path doesn't step a core).
- Unused `MARKER_ADDR` pre-seed in `powman_match_enters_emulator_handler`
  removed.
- `powman.reset()` wired into `Emulator::reset()` (Stage 3
  GLITCH_DETECTOR precedent); regression test added.
- `>=` alarm fidelity comment + RUN-transition accumulator
  comment.
- TIMER write-mask fix: low-16 mask before `TIMER_RW_MASK` (which
  covers bits 19:16) to avoid leaking upper-16 password bits.

**Landed**:
- POWMAN module rewritten with pico-sdk-accurate offsets, COUNT
  advance from `tick_peripherals`, alarm fires NVIC 45 (one-shot),
  password-strip, ARCHSEL tripwire (trace-level), reset() wired.
- New `RESET_POWMAN = 17` constant gates POWMAN MMIO; not in
  `RESETS_POST_BOOTROM`.
- Pre-flight binary `smoke_powman_pacing_rp2350` (Arthur runs).
- Unit tests: 4 HLD-named + 9 module-level + reset test +
  handler literals test = 16 total POWMAN tests.
- Silicon scenario `isr_powman_match_timer_line_45` with
  password-prefixed literals (max_sysclks=5500 budget).
- Cargo 1.12.0 → 1.13.0.

**Deferred (DA tech-debt items, captured below for Stage 5 / V12)**:
- BADPASSWD silent-accept (no warn on wrong-password writes).
- `>=` vs `==` alarm comparator (firmware setting MATCH < COUNT
  with ALARM_ENAB would fire-immediately on emulator vs
  never-fire on silicon).
- Peripheral reset via RESETS_RESET bit toggling (codebase-wide
  gap — POWMAN is consistent with other peripherals).
- SET_TIME_* buffer semantics (emulator commits each lane
  immediately; silicon may latch only on the final write).
- POWMAN scenario lacks CYCCNT-delta observable to diagnose
  LPOSC-vs-XOSC pre-flight surprise.

**Lesson**: pre-flight binaries are scaffolding for Arthur, not
optional. Future stage briefs MUST write the pre-flight binary
unless the HLD specifically waives it.

## Stage 6 — Silicon validation (2026-04-18)

Ran the V11 oracles against the live RP2354 (probe
`<RP2354 probe serial>`) via `test_rp2350_silicon_periph_diff` and
`test_rp2350_silicon_isr_diff`.

| V11 scenario | Verdict | First divergence |
|---|---|---|
| `tbman_platform_reads_silicon_value` | PASS | — |
| `glitch_detector_arm_readback_tracks_ctrl` | PASS | — |
| `sysinfo_readonly_fields` | FAIL | MMIO 0x4000_0000 mask=0x0FFF_FFFF: HW=0x0000_4927 EMU=0x0000_524D |
| `isr_powman_match_timer_line_45` | TIMEOUT | PC stuck at 0x2000_2198 (handler never fired on silicon) |

**SYSINFO finding**. Silicon CHIP_ID = `0x0004_0927` (PART = 4
RP2354, MANUFACTURER = 0x927 Raspberry Pi, REVISION masked
out). Stage 1's `0x0000_524D` came from the pico-sdk header
literal — not the runtime silicon constant. The bytes
`'R','M'` for "Raspberry Microcontroller" are a metadata
mnemonic, not the CHIP_ID register value. Fix is one-line:
`bus/peripherals.rs::sysinfo_read` should return `0x0004_0927`
on offset 0x00. Apply pre-existing 0x0FFF_FFFF mask convention
in scenario stays correct (REV nibble varies by die).

**POWMAN finding**. Silicon ALARM never fires the NVIC line
even with `TIMER.RUN | TIMER.ALARM_ENAB` set. Likely missing:
`POWMAN_INTE.TIMER` enable (offset 0xE4) — POWMAN has its own
peripheral-level interrupt mask that gates propagation to
NVIC line 45, in addition to NVIC ISER1 bit 13. The emulator
unit test passes because `peripherals/powman.rs::advance` calls
`raise_irqs_u64(1u64 << 45)` unconditionally on match — it
doesn't model INTE. Two things to do (V12, separate change):
1. Scenario: write `POWMAN_INTE = 0x5AFE_0001` (or whichever
   bit corresponds to TIMER) before NVIC enable.
2. Emulator: gate the NVIC raise on `INTE.TIMER` and add
   storage for INTE/INTR.

**Pre-existing non-V11 failures (not regressions)**:
`isr_pendsv_cold`, `isr_tail_chain_pendsv_systick`,
`isr_ext_irq_timer0_cold`, `isr_ext_irq_masked_pending` all
fail with `cyccnt_delta` 2-5 cycles slow on emulator. Pre-V11
exception-entry cycle accounting tuning, untouched by this
campaign.

**Smoke binary failure**. `smoke_powman_pacing_rp2350.exe` hits
`fatal: An ARM specific error occurred` at the first read after
releasing POWMAN from reset. Likely related to the same INTE /
peripheral-init gap — POWMAN appears to need clock-source setup
before TIMER reads return live values. Deferred to V12 alongside
the scenario fix.

**§9 acceptance criteria status post-silicon**:
1. ✅ All 4 items landed in tree.
2. ✅ Named unit tests pass.
3. ⚠️ Silicon: 2/4 PASS (TBMAN, GLITCH_DETECTOR);
   1/4 needs CHIP_ID literal fix (SYSINFO);
   1/4 needs INTE-gating fix (POWMAN ISR).
4. ✅ Workspace tests + release build clean (Stage 5 audit).
5. ✅ Coverage matrix updated.

The TBMAN + GLITCH_DETECTOR scenarios closed clean. SYSINFO
+ POWMAN are real-silicon-discovered residuals — V11
implementation work was correct in shape, but the literal
data and the INTE gating were extrapolated from headers
rather than measured. Recommend a V12 follow-up HLD for the
two residuals; both fixes are small (one literal change for
SYSINFO; INTE storage + scenario update for POWMAN).

