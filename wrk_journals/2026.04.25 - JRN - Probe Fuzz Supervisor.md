# 2026-04-25 — Probe Fuzz Supervisor

Supervisor: Claude (claude-opus-4-7), continuing yesterday's overnight
QEMU campaign with an 8-hour real-silicon probe campaign. Both probes
attached and enumerated by `probe-rs list`.

## Configuration

| Field | Value |
|---|---|
| Start (local) | 2026-04-25T08:13:17+01:00 |
| Deadline (epoch) | 1777101197 |
| Deadline (local) | 2026-04-25T16:13:17+01:00 |
| Window | 28 800 s (8h) |
| RP2354 driver | `fuzz-runs/run-probe.sh ` |
| RP2040 driver | `fuzz-runs/run-m0plus-probe.sh ` |
| Per-batch fuzz arg | `--fuzz 200` (≈ 8 000 RP2354 cases / ≈ 2 500 RP2040 cases per batch) |
| RP2354 log | `fuzz-runs/probe.log` |
| RP2040 log | `fuzz-runs/m0plus-probe.log` |

## Probe → DUT mapping (CLAUDE.md hard-wired table)

| Serial | Selector | DUT |
|---|---|---|
| `<RP2354 probe serial>` | `` | RP2354 (Pico 2) |
| `<RP2040 probe serial>` | `` | RP2040 (Pico V1) |

## Pre-launch fix

`fuzz-runs/run-probe.sh` previously hardcoded the no-`--probe` invocation,
so with two probes attached `probe-rs auto_attach` would pick the first
one regardless of target type and fail roughly half the attaches
(documented landmine in CLAUDE.md). Patched to take an optional
`$1 = VID:PID:SERIAL` selector exactly like `run-m0plus-probe.sh` already
does. Diff:

```
+# Optional argument $1: probe selector (VID:PID:SERIAL) for when two probes
+# are attached. When omitted, auto_attach picks the only visible probe.
+PROBE_ARGS=()
+if [ -n "$1" ]; then
+  PROBE_ARGS=(--probe "$1")
+fi
...
-    --fuzz 200 --seed "$SEED" >> "$LOG" 2>&1
+    --fuzz 200 --seed "$SEED" "${PROBE_ARGS[@]}" >> "$LOG" 2>&1
```

Backwards-compatible: omitting `$1` still works for the single-probe case.

## Throughput baseline (from last clean overnight runs)

| Oracle | Cases/batch | Time/batch | 8h projection |
|---|---|---|---|
| RP2354 (`probe_diff_rp2350`) | ~8 000 | ~10 min | ~50 batches, ~400k cases |
| RP2040 (`probe_diff_rp2040`) | ~2 500 | ~3 min | ~160 batches, ~400k cases |

## Timeline

### 08:13 — Both drivers up, attached to correct DUTs

- RP2354 driver started 08:13:42, batch 1 seed `17318575322531`, generated
  8 000 tests (6 200 ALU + 1 800 memory). Banner: `probe_diff_rp2350: RP2354
  hardware differential test runner`. Probe-rs emitted a benign WARN about
  matching `rp2350` via wildcard against the RP235x family — already noted
  in past logs, harmless.
- RP2040 driver started 08:13:44, batch 1 seed `40092718716979`, generated
  2 544 M0+-compatible tests (1 600 ALU + 944 memory). Banner:
  `probe_diff_rp2040: RP2040 hardware differential test runner`.
- Selector mapping confirmed working: each driver matched the correct
  banner for its target DUT, so the patched `--probe` arg routes attaches
  correctly.
- Failure monitors armed (greps `[FAIL]|MISMATCH|UndefOnSilicon|panicked|FATAL|Failed to attach|deadline reached|rc≠0`).
- 30-min heartbeat monitor armed, this time counting only `start=` markers
  to avoid the 2× over-count from yesterday.

Quiet until next event.

### 08:44 — Heartbeat 1 (31 min in, 7h 29m remain) — clean

- RP2354 batch 4 @ 4 400/8 000 (55%), 0 failures. Batches 1–3 done in
  31 min ≈ 10.3 min/batch — bang on baseline.
- RP2040 batch 10 @ 2 100/2 556 (82%), 0 failures. Batches 1–9 done in
  31 min ≈ 3.4 min/batch — also baseline.
- Heartbeat batch counter looks correct (no 2× inflation this time).

### 08:51 — USB blip on RP2354 probe (NOT a fuzz failure)

Failure-monitor fired with rc=2 events on RP2354 batches 6, 7, 8, 9 — all
within the same second. Investigated: this was a transient USB hiccup,
not an emulator divergence.

**Sequence:**
1. RP2354 batch 5 (seed `32746981415827`) was running the T32_LDM class
   when WinUSB returned `submit failed: The device does not recognize
   the command. (os error 22)`.
2. `probe_diff_rp2350` degraded gracefully — every case from #189 onward
   got `[SKIP]` with `probe-rs error: An ARM specific error occurred.`
   instead of `[FAIL]`. Batch 5 summary: `Passed: 1885, Failed: 0,
   Skipped: 6115` (~76% of the batch skipped).
3. Because the binary exited rc=0 (failures=0, skips don't count as
   failures), the driver immediately launched batches 6 → 9. Each
   exited rc=2 within <1s with `fatal: The debug probe could not be
   created.` — the probe was wedged on the host USB stack.
4. By batch 10 (seed `25274180577485`) the probe had recovered,
   attached cleanly, and is now running normally (200/8000 with 0
   failures observed).

**Impact:**
- ~6 100 RP2354 cases skipped (one batch's worth of T32_LDM coverage).
- No actual emulator divergences seen — every `[SKIP]` was a USB
  transport error, not an ISA mismatch.
- RP2040 probe driver unaffected throughout (batch 13 in progress).
- Both probes still enumerate normally via `probe-rs list`.

**Surface for later (not actioning tonight):**
- The driver re-runs a wedged probe 4× before the probe self-heals,
  burning ~4 seeds. A short back-off on rc=2 (e.g. `sleep 30`) would
  make this less noisy.
- Better still: have `probe_diff_rp2350` exit non-zero when degraded
  to all-SKIP mode so the driver can distinguish "0 failures because
  everything passed" from "0 failures because the probe died". That
  would let a smarter driver pause and re-enumerate the probe.
- Both are tech-debt work, not in scope for tonight. Continuing the
  run.

No agent dispatch — this isn't an emulator/code bug.

### 09:14 — Heartbeat 2 (1h 01m in, 6h 59m remain) — clean post-recovery

- RP2354 batch 12 @ 6 400/8 000 (80%), 0 failures. Batches 10, 11 done
  cleanly post-USB-blip; probe is healthy again.
- RP2040 batch 20 @ 1 900/2 550 (75%), 0 failures. Steady at baseline rate.
- No further USB instability since 08:51.

### 09:44 — Heartbeat 3 (1h 31m in, 6h 29m remain) — clean

- RP2354 batch 15 @ 7 500/8 000 (94%), 0 failures. Batches 13–14 done in
  the window. Steady ~10 min/batch.
- RP2040 batch 30 @ 200/2 555 (8%), 0 failures. Batches 21–29 done in
  the window. Steady ~3 min/batch.

### 10:14 — Heartbeat 4 (2h 01m in, 5h 59m remain) — clean

- RP2354 batch 19 @ 5 000/8 000 (63%), 0 failures. ~3.5 batches in this
  window — slightly faster than baseline.
- RP2040 batch 40 @ 1 400/2 552 (55%), 0 failures. 10 batches in window.
- USB blip has not recurred since 08:51.

### 10:44 — Heartbeat 5 (2h 31m in, 5h 29m remain) — clean

- RP2354 batch 22 @ 7 700/8 000 (96%), 0 failures.
- RP2040 batch 50 @ 1 800/2 548 (71%), 0 failures.
- Steady, no incidents.

### 11:14 — Heartbeat 6 (3h 01m in, 4h 59m remain) — clean, halfway

- RP2354 batch 26 @ 5 200/8 000 (65%), 0 failures.
- RP2040 batch 60 @ 2 100/2 532 (83%), 0 failures.
- Halfway through the window. Both at baseline rates.

### 11:44 — Heartbeat 7 (3h 31m in, 4h 28m remain) — clean

- RP2354 batch 29 @ 7 700/8 000 (96%), 0 failures.
- RP2040 batch 70 @ 2 200/2 554 (86%), 0 failures.

### 12:14 — Heartbeat 8 (4h 01m in, 3h 59m remain) — clean

- RP2354 batch 33 @ 5 000/8 000 (63%), 0 failures.
- RP2040 batch 80 @ 2 400/2 550 (94%), 0 failures.

### 12:44 — Heartbeat 9 (4h 31m in, 3h 28m remain) — clean

- RP2354 batch 36 @ 7 600/8 000 (95%), 0 failures.
- RP2040 batch 91 @ 300/2 544 (12%), 0 failures.

### 13:14 — Heartbeat 10 (5h 01m in, 2h 59m remain) — clean

- RP2354 batch 40 @ 4 900/8 000 (61%), 0 failures.
- RP2040 batch 101 @ 900/2 551 (35%), 0 failures.

### 13:44 — Heartbeat 11 (5h 31m in, 2h 29m remain) — clean

- RP2354 batch 43 @ 7 500/8 000 (94%), 0 failures.
- RP2040 batch 111 @ 1 600/2 548 (63%), 0 failures.

### 14:14 — Heartbeat 12 (6h 01m in, 1h 59m remain) — clean

- RP2354 batch 47 @ 4 200/8 000 (53%), 0 failures.
- RP2040 batch 121 @ 1 500/2 548 (59%), 0 failures.

### 14:44 — Heartbeat 13 (6h 31m in, 1h 28m remain) — clean

- RP2354 batch 50 @ 6 900/8 000 (86%), 0 failures.
- RP2040 batch 131 @ 700/2 562 (27%), 0 failures.

### 15:14 — Heartbeat 14 (7h 01m in, 58m remain) — clean but slower

- RP2354 batch 52 @ 1 100/8 000, 0 failures. ~2 batches in window
  (typical 3) — slower.
- RP2040 batch 135 @ 200/2 537, 0 failures. ~4 batches in window
  (typical 10) — noticeably slower.
- No failure signal (no `[FAIL]`, no `[SKIP]`, no rc=2). Likely host-load
  or USB-hub contention; both probes share the same hub. Not actioning.

### 15:44 — Heartbeat 15 (7h 31m in, 29m remain) — clean, recovered

- RP2354 batch 54 @ 7 100/8 000 (89%), 0 failures.
- RP2040 batch 142 @ 2 200/2 558 (86%), 0 failures.
- Throughput recovered to near baseline this window. Whatever slowed the
  14:44 window has eased. Both will let current batch finish then exit.

### 15:48 — Run extended +24h (Arthur's request)

Arthur asked to add another 24 hours. Drivers cache `$DEADLINE` at startup,
so updating `fuzz-runs/deadline` mid-run doesn't reach the running bash
loops. Plan:

1. Wrote new deadline file: `1777216397` = `2026-04-26T16:13:17+01:00`.
2. Original drivers will continue to exit at 16:13 today (cached value).
3. On exit, immediately relaunch both with the same probe selectors.
   The fresh shells read the new deadline file at startup → 24h forward.
4. Heartbeat monitor reads the deadline file each loop, so it
   automatically tracks the new deadline — no restart needed for it.

Total wall-clock plan: 8h initial + 24h extension = 32h, ending
**2026-04-26T16:13 local**. Tasks #9, #10 added for the relaunches.

### 16:14 — Heartbeat 16 (8h 01m in, 24h remain) — clean

- RP2354 batch 56 @ 3 900/8 000 (49%), 0 failures. ~5 min to finish.
- RP2040 batch 147 @ 900/2 543 (35%), 0 failures. ~2 min to finish.
- Heartbeat correctly reports `remain=86316s` ≈ 24h — new deadline
  file picked up automatically.
- Both drivers will exit when current batch ends; relaunch trigger armed.

### 16:17 / 16:20 — Phase 1 drivers exited cleanly (rc=0)

- RP2040 driver footer: `batches=147` at 16:17:13. All rc=0, last batch
  ran 304.3s for 2546 tests.
- RP2354 driver footer: `batches=56` at 16:20:49. All rc=0, last batch
  ran 731.0s for 8000 tests.
- The driver script truncates its log on launch (`: > "$LOG"`), so the
  RP2040 log was overwritten before I could archive it. The RP2354 log
  was archived to `fuzz-runs/probe.phase1.log` (17 401 lines) before
  relaunch — useful if we ever want to re-examine the 08:51 USB blip in
  detail. Lesson for next time: **archive both logs the moment a
  deadline-reached event lands**, not after relaunch.

## Phase 1 final summary (8h, 0 emulator divergences)

| Oracle | Batches | Cases/batch | Total ran | Failures | Skips |
|---|---|---|---|---|---|
| RP2354 (`probe_diff_rp2350`) | 56 | ~8 000 | ~448 000 | 0 | ~6 115 (USB blip) |
| RP2040 (`probe_diff_rp2040`) | 147 | ~2 550 | ~374 850 | 0 | 0 |
| **Combined** | 203 | — | **~822 850** | **0** | ~6 115 |

Notes:
- The 6 115 RP2354 skips all came from the single 08:51 USB blip; not
  emulator/silicon divergences.
- 4 RP2354 batches (6–9) were spurious rc=2 attaches in the post-blip
  recovery window. None counted toward "cases ran" because the binary
  printed `fatal: The debug probe could not be created.` and exited
  without running any cases.

### 16:17 / 16:21 — Phase 2 launched (24h extension)

- RP2040 phase-2 driver up at 16:17:36, batch 1 seed `16215130747695`,
  generating 2 546 cases. Banner correct.
- RP2354 phase-2 driver up at 16:21:11, batch 1 reached 100/8000 with
  0 failures — clean attach confirmed.
- Both phase-2 drivers reading `fuzz-runs/deadline = 1777216397`
  (2026-04-26T16:13:17). Heartbeat monitor continues unmodified — its
  deadline check reads the file each loop, so 24h of pings ahead.

Quiet until next event.

### 16:44 — Phase-2 Heartbeat 1 (27 min into phase 2, 23h 28m remain) — clean

- Phase-2 RP2354 batch 3 @ 1 200/8 000 (15%), 0 failures.
- Phase-2 RP2040 batch 8 @ 900/2 541 (35%), 0 failures.
- Both at baseline rates; heartbeat countdown showing ~24h ahead.

### 16:55–17:15 — Major incident: cascade USB lockup → RP2040 chip wedged

This is the second USB-related incident, much worse than the 08:51 blip.

**What happened:**
1. RP2354 phase-2 batch 3 (seed `2661667131565`) hit the same WinUSB
   `submit failed (os error 22)` mid-batch as the 08:51 blip — degraded
   to all-skip mode with `Passed: 7169, Failed: 0, Skipped: 831`.
2. On shutdown the binary failed to deconfigure WinUSB, leaving an
   `endpoint already in use` (kind: Busy) error in the dying log.
3. Crucially, **both** drivers entered cascade rc=2 mode shortly after
   — RP2354 batches 4–10 and RP2040 batches 11–22 all returned
   `fatal: An error which is specific to the debug probe in use occurred`.
4. The cascade kept running because each driver script's while loop
   only checks `$DEADLINE`; rc=2 doesn't break it.

**Recovery procedure (per CLAUDE.md / RUNBOOK.md zombie-tree recipe):**
1. `TaskStop` on both bash drivers — stopped the inner shells but
   orphaned binary children survived holding USB endpoints.
2. `kill -9 <POSIX_PID>` on the orphan binaries (one took two passes).
3. `kill -9` on the bash driver POSIX PIDs (75187, 76533) to stop
   re-spawning. After that, no new batch lines appeared in either log.
4. `taskkill //F //PID <WINPID>` on the most stubborn orphan; one
   timed out (likely stuck in USB I/O completion); ultimately died on
   a second `kill -9` round.

**Probe-state diagnosis after cleanup:**
- RP2354 (`probe_diff_rp2350 --fuzz 5`) — **fully healthy**: 200 tests
  passed in 13.6s.
- RP2040 (`probe_diff_rp2040 --fuzz 5`) — endpoint OK but
  `Target device did not respond to request` at the SWD layer. The
  chip itself is wedged. `probe-rs reset --chip RP2040 --probe ...`
  also fails with the same SWD error.

**Conclusion:** RP2040 chip needs physical intervention (RUN/RST button
on the Pico V1, or unplug-replug probe USB). The Pico debugger probe
itself is fine — only the target board is wedged. Software cannot
recover this state.

**Logs archived for the record:**
- `fuzz-runs/probe.phase2-cascade.log` (1 220 lines) — RP2354 phase-2
  batches 1–10 including the cascade
- `fuzz-runs/m0plus-probe.phase2-cascade.log` (2 094 lines) — RP2040
  phase-2 batches 1–22 including the cascade

**Action taken:**
- RP2354 driver relaunched (extension run resumed) at ~17:16; we keep
  ~22.5h of the 24h window.
- RP2040 driver paused; task #10 set back to pending with a hardware-
  reset note. Waiting on Arthur.

### Tech-debt followups (not actioning tonight)

The driver scripts assume rc=0 from the binary means "everything was
fine" but the binary returns rc=0 even when it skipped 76% of cases due
to USB transport errors. That's how a single transient blip turned into
a multi-batch cascade. Two complementary fixes would harden this:

1. **Binary side:** make `probe_diff_rp2350` / `probe_diff_rp2040` exit
   non-zero (or a distinct exit code, e.g. rc=3) when the
   skipped-case count exceeds a small threshold. That signals "probe
   went away" vs. "0 failures, all good".
2. **Driver side:** detect rc=2 (probe creation failed) and back off
   for ~30s before retrying. Right now rc=2 takes <1s, so the loop
   burns 60+ seeds per minute through dead-probe attaches.

Worth one short tech-debt session, but **not** mid-fuzz — these are
script changes that need careful testing, not improvisation while live
hardware is in a partially-wedged state.

### 17:27 — RP2040 chip reset by Arthur, driver resumed

Arthur physically reset the RP2040 board. Smoke test (`probe_diff_rp2040
--fuzz 5`) passed: 64 cases in 6.7s, 0 failures. Long driver relaunched.

Both probes now back in extension run. We've lost ~30 min of the 24h
window (16:55 → 17:27). Effective remaining window for the extension:
~22.7h, ending ~tomorrow 16:13.

### 17:44 — Resumed-phase heartbeat 1 (22h 28m remain) — clean

- RP2354 batch 3 @ 6 400/8 000 (80%), 0 failures.
- RP2040 batch 5 @ 1 700/2 552 (67%), 0 failures.
- Both at baseline. No further USB instability.

### 18:14 — Resumed-phase heartbeat 2 (21h 58m remain) — clean

- RP2354 batch 6 @ 7 100/8 000 (89%), 0 failures.
- RP2040 batch 14 @ 1 900/2 549 (75%), 0 failures.
- Steady at baseline rates.

### 18:44 / 19:14 / 19:44 — Resumed-phase heartbeats 3/4/5 — clean

| Time | Remain | RP2354 | RP2040 |
|---|---|---|---|
| 18:44 | 21h 28m | batch 10 @ 3 300/8 000 (41%) | batch 24 @ 2 000/2 545 (79%) |
| 19:14 | 20h 58m | batch 13 @ 7 000/8 000 (88%) | batch 34 @ 2 000/2 548 (79%) |
| 19:44 | 20h 28m | batch 17 @ 3 100/8 000 (39%) | batch 44 @ 2 100/2 548 (82%) |

0 failures throughout. Both drivers steady at baseline (RP2354 ≈ 1
batch / 22 min, RP2040 ≈ 1 batch / 3 min).

### 20:14 — Resumed-phase heartbeat 6 (19h 58m remain) — clean

- RP2354 batch 20 @ 7 000/8 000 (88%), 0 failures.
- RP2040 batch 54 @ 2 300/2 553 (90%), 0 failures.

### 20:44 — Resumed-phase heartbeat 7 (19h 28m remain) — clean

- RP2354 batch 24 @ 3 200/8 000 (40%), 0 failures.
- RP2040 batch 64 @ 2 400/2 554 (94%), 0 failures.

### 21:14 — Resumed-phase heartbeat 8 (18h 58m remain) — clean

- RP2354 batch 27 @ 7 000/8 000 (88%), 0 failures.
- RP2040 batch 75 @ 300/2 547 (12%), 0 failures.

### 21:44 — Resumed-phase heartbeat 9 (18h 28m remain) — clean

- RP2354 batch 31 @ 3 400/8 000 (43%), 0 failures.
- RP2040 batch 85 @ 900/2 543 (35%), 0 failures.

### 22:14 — Resumed-phase heartbeat 10 (17h 58m remain) — clean

- RP2354 batch 34 @ 6 800/8 000 (85%), 0 failures.
- RP2040 batch 95 @ 700/2 546 (27%), 0 failures.

### 22:44 — Resumed-phase heartbeat 11 (17h 28m remain) — clean

- RP2354 batch 37 @ 7 900/8 000 (99%), 0 failures.
- RP2040 batch 104 @ 1 800/2 553 (70%), 0 failures.

### 23:15 — Resumed-phase heartbeat 12 (16h 58m remain) — clean

- RP2354 batch 40 @ 7 000/8 000 (88%), 0 failures.
- RP2040 batch 112 @ 1 900/2 554 (74%), 0 failures.

### 23:15–23:18 — Cascade incident #2 (same root cause)

Same exact pattern as 16:55:

1. RP2040 batch 112 (seed `25532162667024`) hit USB transient mid-batch
   — 1 964 passed, 590 skipped, finished `rc=0`.
2. Shutdown emitted `endpoint already in use` (Busy) WARN, with the new
   tail message `Failed to stop DP Multidrop(f1002927)` x2 (Multidrop
   is RP2354-specific, but probe-rs is logging at the wrong level — DP
   Multidrop messaging shouldn't be reaching an RP2040 attach; harmless
   noise).
3. Both drivers cascaded into rc=2: RP2040 batches 113–115, RP2354
   batches 41–50. ~10 dead seeds on RP2354 + 3 dead seeds on RP2040
   before I caught it (~3 min after the trigger).
4. Recovery: `TaskStop` both bash drivers → `kill -9` orphan binaries
   → `kill -9` bash parents → `kill -9` re-orphaned binaries.
5. Both probes' WinUSB endpoints are now stuck at the **first**
   CMSIS-DAP `Info` transaction (different error than incident #1,
   which only stuck on `Transfer`). Probes still enumerate via
   `probe-rs list`, but cannot be opened.

**This is the second time the same failure has happened in <8 hours.**
Patched both driver scripts before relaunch to back off 30s on rc=2:

```sh
if [ "$rc" -eq 2 ]; then
  echo "=== PROBE rc=2 detected; backing off 30s before next batch ===" >> "$LOG"
  sleep 30
fi
```

That stops a future cascade burning seeds at 60/sec and gives Windows
time to clean up the WinUSB endpoint state. Won't fix root-cause USB
transients, but makes the consequences far smaller.

Cascade-2 logs archived:
- `fuzz-runs/probe.phase2-cascade2.log` (4 966 lines)
- `fuzz-runs/m0plus-probe.phase2-cascade2.log` (5 455 lines)

**Both probes need unplug-replug** before relaunch — the endpoints
won't clear without USB device re-enumeration. Pinged Arthur. Tasks
#9 and #10 set back to pending.

### 23:45 — False heartbeat + unexplained deadline mutation

The 23:45 heartbeat reported `batch 51 / batch 116` at fresh-looking
positions. Investigation found nothing actually running:

- `ps -W` for `probe_diff` is empty.
- Both log files have mtime `23:17:45.x` — frozen at the moment my
  `kill -9`s landed. The "batch 51" lines that fooled the heartbeat
  are the very last `start=` markers in the cascade-2 logs.
- The heartbeat just `grep -c`s the log file each loop, so a stale log
  yields a stale count. Easy to misread as "still running".

Separately, the deadline file `fuzz-runs/deadline` was modified at
`23:18:22` to `1777184302` (= 2026-04-26T07:18:22 +01:00), not the
expected `1777216397` (= 2026-04-26T16:13:17 +01:00). I didn't issue
any write to that file — ruled out my own commands by re-reading the
session. Possibly a Bash-shell expansion on something I typed got
misinterpreted, or one of the dying bash drivers hit a corner case
I'm not seeing. Either way, restored the file to `1777216397` at 23:47.

**No fuzz time has been lost beyond the cascade itself** — the drivers
have been dead since 23:17 anyway. The deadline was just being
miscounted by the heartbeat. Real wall-clock waiting since 23:17 still
counts against the 24h window, so we're now ~3.5h into the 24h
extension, ~16h26m remaining.

### 00:15 / 00:45 — Heartbeats keep firing, stale logs

Heartbeats reporting `batch 51 @ 200/8000` and `batch 116 @ 300/2545`
unchanged — those are still the last `start=` markers in the frozen
logs. `remain=` is now decrementing 30min per ping correctly, so the
deadline restore took effect.

Idle waiting. Not stopping the heartbeat monitor — when Arthur reseats
the probes and I relaunch, fresh log lines will appear and the
heartbeat will start showing live data automatically.

### 04:02 — Reseat confirmed, drivers recovered

Arthur confirmed both probe USB cables unplugged + replugged.

`probe-rs list` now shows both probes enumerating clean:
- `2e8a:000c-0:<RP2040 probe serial>` (RP2040)
- `2e8a:000c-0:<RP2354 probe serial>` (RP2354)

Initial smoke-test attempts (`probe_diff_*.exe --fuzz 5`) failed with
"The debug probe could not be created" — but `ps -W` revealed the
probe drivers had stayed alive across the cascade and were already
holding the USB handles, racing my smoke tests. Killed the four orphan
probe binaries (PIDs 195312, 205964, 211180, 211220) with `kill -9`.

The bash drivers (PIDs 211170, 211208) immediately respawned new
batches at 04:03:16 — and those came up healthy on real silicon:

```
=== PROBE batch=2 seed=71872642824296 start=2026-04-26T04:03:16+01:00 ===
Attached to target, using core 0
DWT baseline: 5 cycles ...
[1200/8000] 0 failures...   ← RP2354

=== PROBE batch=2 seed=150711389825474 start=2026-04-26T04:03:16+01:00 ===
Attached to target, using core 0
[1300/2561] 0 failures...   ← RP2040
```

Confirms the WinUSB-endpoint-stuck diagnosis — the probes themselves
were healthy, but the OS-side endpoint state needed a USB re-enumeration.

Also visible in `ps -W`: two QEMU drivers also running (M33 batch 5 +
M0+ batch 8, started 02:47/03:37 — predates compaction, so likely from
the prior context's autonomous-loop). Leaving them in place since
they're producing oracle data.

**Status as of 04:04**:
- 4 driver bashes alive, all 4 producing fresh batches
- Both probes attached and progressing through batch 2 of phase 3
- Deadline file intact at `1777216397` (= 2026-04-26T16:13:17 +01:00)
- ~12h10m remaining in the 24h extension window
- 0 emulator divergences since reseat (still 0 cumulative across all phases)

Tasks #9 and #10 (driver relaunch) marked complete — the drivers were
auto-recovering on their own via the rc=2 backoff, just blocked behind
the OS-side USB-endpoint state until Arthur reseated.

### Phase 3 (post-reseat) — interrupted by cascade #4 at 04:36

Drivers ran clean from 04:03 to ~04:30, then somehow the original
`fuzz-runs/probe_diff_*.211170.exe` and `.211208.exe` BIN files
disappeared from disk while the bash drivers were still alive in
the loop. `ps -W` confirmed bashes 211170 and 211208 alive but
spinning rc=127 ("No such file or directory") on every iteration.

In parallel, *fresh* driver bashes spawned at 04:36 (PIDs 22152x
visible only by their freshly-cp'd BIN files) and immediately hit
a WinUSB rc=2 cascade — same root cause as cascades 2/3, plus the
old bashes were holding the USB handles, so the new bashes couldn't
attach.

Suspected root cause for the 04:30 BIN-deletion + 04:36 fresh-bash
spawn: an autonomous-loop or sibling task fired `bash run-probe.sh`
and `bash run-m0plus-probe.sh` again during the prior context's
recovery, and the interaction of multiple concurrent driver
instances all writing to the same `LOG`/`BIN` paths produced the
chaos. The driver scripts have no PID-locking — only PID-suffixed
BIN copies — so concurrent invocations stomp each other's state.

**Action taken at 04:48–04:54** (with hindsight: should have tried
hot-restore first per `feedback_fuzz_runs_dir.md` —
`cp -f target/release/probe_diff_rp2350.exe fuzz-runs/probe_diff_rp2350.211170.exe`
would have unblocked the rc=127 spam on the legitimate driver
without losing batch progress; the WinUSB cascade from the duplicate
driver pair would still have needed cleanup but with less total
churn):
1. Stopped the three Monitor tasks (heartbeat + failure tails) —
   confirmed they don't restart drivers, just observe.
2. `kill -9 211170 211208` — killed the rc=127-spinning bashes.
3. Removed stale `.221523/.221525/.221526/.221527.exe` BINs.
4. Confirmed both probes enumerate via `probe-rs list`.
5. Smoke-test passed: RP2354 200/200 in 64s, RP2040 64/64 in 15s.
6. Archived contaminated logs as `probe.cascade4.log` /
   `m0plus-probe.cascade4.log`.
7. Also killed bashes 146697/146735 (QEMU drivers) — they were in
   the same doomed BIN-deleted state and would soon spam rc=127.
   Skipping QEMU restart since yesterday's run already produced
   12.3M clean cases.
8. Started single fresh probe drivers at 04:54:54 (RP2354) and
   04:54:55 (RP2040).
9. Re-added Monitor tasks (heartbeat + dual failure tails).

### Phase 3 (clean restart) — final tally

Drivers ran from 04:54:54 / 04:54:55 to deadline, exiting
gracefully when the while-loop saw `$(date +%s) >= $DEADLINE`.

| Chip | Driver exit | Batches | Cases | Failures | Skipped | Undef |
|---|---|---|---|---|---|---|
| RP2354 | 16:21:00 BST | 59 | 416 000 (52 batches summed; 7 still-running interrupted) | 0 | 0 | 0 |
| RP2040 | 16:14:44 BST | 145 | 364 741 (143 batches summed) | 0 | 0 | 0 |

Combined this phase: **~780 741 hardware-silicon ISA cases, 0
emulator divergences across both chips.**

Note on case-vs-batch arithmetic: `--fuzz 200` means 200 cases per
class, expanded to ~8000 RP2354 cases per batch and ~2545 RP2040
cases per batch by the harness's class-multiplier logic. The "Total:"
summary lines summed above only count batches that completed; the
batch-count includes the still-running batch interrupted by the
deadline check.

## Cumulative (across the 24h+ window)

| Phase | Window | RP2354 cases | RP2040 cases |
|---|---|---|---|
| Phase 1 (clean 8h) | 08:13 → 16:13 BST 04-25 | (see `probe.phase1.log`) | (lost — log truncated by relaunch) |
| Phase 2 (cascade 2) | 16:55–17:16 04-25 | partial — see `*.cascade.log` | partial |
| Phase 2 (cascade 3) | ~22:30–23:17 04-25 | partial — see `*.cascade2.log` | partial |
| Phase 3 (cascade 4) | 04:36–04:48 04-26 | rc=2/127 spam only — see `*.cascade4.log` | rc=2/127 spam only |
| Phase 3 (clean restart) | 04:54 → 16:21 BST 04-26 | **416 000** ✓ | **364 741** ✓ |

**Net: zero emulator divergences across the full multi-day
campaign on either chip's silicon-vs-emulator ISA differential.**
All disruptions were USB infrastructure (WinUSB endpoint cascades,
deleted BIN files in tight bash loops) and not code defects.

## Tech-debt items raised this session

1. **Driver scripts lack PID-locking** — concurrent invocations of
   `run-probe.sh` / `run-m0plus-probe.sh` share `$LOG` and write
   the same `.$$.exe` BIN namespace. Multiple instances stomp.
   Fix: `flock` on a sentinel file at script entry, or `set -e`
   + `pgrep -f run-probe.sh` early-exit. Filed in the rc=2 backoff
   patch; no code change beyond that yet.
2. **`probe_diff_*` returns rc=0 in degraded "all-skip" mode** — a
   mid-run USB blip that flips the binary into "skip everything"
   shows as rc=0, hiding the failure from any retry-on-rc!=0 logic.
   Fix: exit non-zero (e.g. rc=3) if skipped > 50% of total. Not
   patched mid-fuzz to avoid kicking the running drivers.
3. **WinUSB endpoint cascade signature** — every cascade so far
   has the same shape: a single transient `read_status: bulk read
   failed` from probe-rs, then 100% of subsequent attaches return
   rc=2 until the OS-side endpoint state clears. Manifests within
   minutes after the first symptom. Reseat (USB unplug-replug) is
   the only reliable recovery; `probe-rs reset --chip RPxxxx` does
   not help. The 30s rc=2 back-off (added late on 04-25) does not
   actually clear the cascade — it just slows the spam rate. Worth
   investigating whether `probe-rs detach` or a timed `Sleep + USB
   port reset` API call (Win32 `CM_Reenumerate_DevNode` over the
   probe's device instance) could software-recover.
4. **No PID-lock means autonomous-loop or sibling-context driver
   restarts cascade silently.** Cascade #4 was almost certainly
   triggered by a stray `bash run-probe.sh` invocation from the
   prior context's recovery path, fighting with the existing
   driver bash. PID-lock would have made the second invocation
   exit with a clear "already running" message instead of going
   into a destructive race.
