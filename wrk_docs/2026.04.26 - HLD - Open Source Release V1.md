# HLD — Open Source Release

> **Historical planning document.** The distribution identity and current
> release procedure are now defined by the root `README.md`, `NOTICE`, and
> `CONTRIBUTING.md`. The maintained derivative is
> `FuyukiYoneyama/picoem-picocalc`; references below to the upstream repository
> or to the original release plan are historical context, not current release
> instructions.

**Date:** 2026-04-26
**Version:** V1
**Scope:** Pre-publication audit and prep across the whole workspace
**Status:** Draft — pending Arthur review

## 1. Motivation

We want to publish `picoem` (this repo) as a public open-source project on
GitHub. The codebase is already dual-licensed MIT OR Apache-2.0 with a NOTICE
covering vendored bootroms, and the README is publishable as-is. The work that
remains is an audit-and-prune pass: confirming third-party licensing
obligations, sanitising 360-odd dev journals/HLDs, and adding the standard OSS
hygiene files.

Goal of this document: enumerate the work concretely so we can review
together, flag the two or three decisions that need Arthur's input, and avoid
discovering a problem after the repo is already public.

## 2. Goals

1. **Publish a self-contained, legally clean public repo** that a third party
   can clone, build, and reproduce the differential-fuzz / silicon-oracle
   results from.
2. **Preserve the project's history and HLDs** as part of what gets published —
   they document how the emulator was built and are part of the project's
   value, not a liability to strip out.
3. **No surprises post-publication** — every redistributed third-party binary
   has correct attribution, every doc has been read at least once for personal
   or sensitive content.

## 3. Non-goals

- **No CI on GitHub.** Decided. Tests run locally.
- Not adding marketing content (logo, website, demo videos). This is a code
  drop, not a launch.
- Not changing the workspace layout, build, or test topology.
- Not publishing app crates (`rp2350-emu-tui` / `rp2040-emu-tui`) or the harness
  crate to crates.io in V1. They get name-reservation placeholders only;
  real publication waits until we decide whether `cargo install` is part of
  the user story (§13).

## 4. Pre-publication checklist (summary)

| # | Item | State today | Action | Owner |
|---|---|---|---|---|
| 1 | PicoGUS firmware redistribution policy | Files `.gitignored`; demo script fetches | **Flip policy**: vendor binaries with attribution | Claude |
| 1a | RP2040 bootrom NOTICE gap | `bootrom-rp2040-b2.bin` tracked + has its own LICENSE; not in workspace `NOTICE` | Add NOTICE entry (BSD-3-Clause, RPi) | Claude |
| 2 | DOSBox-X patch attribution | Patch file in `third_party/`; not in NOTICE | Add NOTICE entry (GPLv2) | Claude |
| 3 | Monkey Island ADLIB fixture provenance | 29k-line OPL3 register-write CSV | Add fixture README documenting capture method | Claude |
| 4 | Doc audit (`wrk_journals/` + `wrk_docs/` = 360 files) | Unreviewed | Targeted grep + spot-check | Claude, Arthur reviews flags |
| 4a | Probe-serial → DUT mapping in CLAUDE.md | Inline in CLAUDE.md | Move to `docs/probe_serials.md`; CLAUDE.md keeps a pointer | Claude |
| 5 | probe-rs fork (`probe-rs-0.31.0-mdrp-patched/`) | Vendored, MIT/Apache-2.0 LICENSEs already present | Update NOTICE, document patch summary | Claude |
| 6 | `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md` | Missing | Add minimal versions | Claude |
| 7 | RPi trademark disclaimer | Missing | Add to README footer | Claude |
| 8 | `.gitignore` audit + stale artifacts at root | `bench-basic.json.gz`, `bench-basic.json.syms.json`, `wrk_debug/`, `wrk_scratch/`, `fuzz-runs/` | Audit each, gitignore or remove | Claude, Arthur confirms |
| 9 | Submodule docs | `.gitmodules` exists; README doesn't mention `--recursive` | One-line README addition | Claude |
| 10 | Crates.io publication | Decided: publish | Tier 1 as real `0.1.0`; Tier 3 as `0.0.0` reservations | Claude (mechanics), Arthur holds API token |
| 11 | Final dry-run: clone-and-build from scratch | Not yet | Fresh clone in tempdir, follow README only | Claude |

## 5. PicoGUS firmware (item 1) — flip to vendored

**Today.** `third_party/picogus/.gitignore` excludes `*.bin`, `*.uf2`, `*.exe`,
`*.zip`, `UPSTREAM_README.md`, `upstream/`. The demo script (`scripts/picogus_demo.sh
--prepare`) fetches the upstream zip, verifies SHA256 against `VERSION`, and
extracts. Local tree currently contains the unpacked artefacts as untracked
files.

**Arthur's choice:** option (a) — vendor the binaries to make reproduction
trivial.

**Upstream license** (verified): PicoGUS `sw/` and `pgusinit/` collection is
GPL-2.0-or-later; some files dual-licensed BSD/MIT. Hardware (`hw/`) is CERN
OHL v2. We only redistribute software artefacts.

**Plan:**

1. Remove `third_party/picogus/*.{bin,uf2,exe,zip}` and `UPSTREAM_README.md`
   from `.gitignore`.
2. Commit:
   - `picogus.uf2`, `pg-ne2k.uf2`, `pgusinit.exe` (as published — stock
     upstream binaries).
   - `picogus-v4.0.0.bin` (UF2→bin conversion of `picogus.uf2`; functionally
     stock).
   - `picogus-v4.0.0.zip` (the upstream release archive).
   - `UPSTREAM_README.md` (extracted from zip).
   - **`picogus-v4.0.0-patched.bin`** — only 3 bytes differ from stock. We
     need to surface what those bytes are (presumably a hot-fix or test-mode
     hook) and either commit a `.patch` file alongside, or document the
     three-byte delta inline in the picogus README. This is a hard GPLv2
     requirement: the binary's "corresponding source" must be available.
3. Add a copy of the upstream `LICENSE` file as
   `third_party/picogus/LICENSE-PICOGUS-GPL-2.0.txt`.
4. Update `NOTICE` with a PicoGUS section listing the upstream URL, license,
   and pointer to the local LICENSE copy.
5. Update `third_party/picogus/README.md`: rewrite the "**not redistributed**"
   wording to "redistributed under GPL-2.0-or-later", explain the 3-byte
   patch and where to find the patch source, keep the demo-script staging
   path as the still-valid alternative for upgrades.
6. **Don't vendor `upstream/`** (the full source tree clone). It stays
   `.gitignored`. The pinned `VERSION` + upstream URL gives reproducibility;
   redistributing 4k+ source files has no practical benefit and makes the
   repo unwieldy. The upstream URL in NOTICE satisfies GPLv2's source-pointer
   requirement.

**The 3-byte patch — identified.** From `wrk_journals/2026.04.16 - JRN -
PicoGUS End-to-End Bring-up.md` (Phase E) and confirmed in
`wrk_journals/2026.04.24 - JRN - Single-voice dump and FM trace.md`
(Session 8):

- **Location:** flash offset `0x0b199` in `picogus-v4.0.0.bin` (RAM address
  `0x20012FA4` after the firmware's `.data` copy).
- **Original bytes:** `beq 0x1000b19c; b 0x1000b90c` — the conditional return
  path of `test_psram`.
- **Patched bytes:** `movs r0, #0; bx lr` — immediately returns 0 from
  `test_psram`.
- **Why:** the stock `test_psram` self-test is *correct* but performs a
  full-PSRAM read/write sweep that takes ~1 hour on our emulator (it runs in
  milliseconds on real silicon). The patch bypasses the test so the firmware
  proceeds past PSRAM init in seconds. Original implementation was a runtime
  SRAM poke applied by the harness after `.data` copy; later materialised as
  a permanent patched binary for convenience.
- **Affects:** `picogus_diff_rp2040` and `picogus_probe_pc` harnesses only.
- **Functional impact:** none — `test_psram` is a self-diagnostic, not part
  of the audio path. Firmware behaviour with the patch is identical to
  firmware running on hardware that passed the self-test.

**Plan to ship cleanly:** alongside `picogus-v4.0.0-patched.bin`, commit a
`picogus-v4.0.0-test_psram-bypass.patch` text file containing:

1. The 3-byte hex diff at offset 0x0b199.
2. A short rationale block (the four bullets above, condensed).
3. The original Thumb disassembly + the patched Thumb disassembly side-by-side.

This satisfies GPLv2's "corresponding source" obligation for the
modification — it's literally a 3-byte hand patch, fully documented.

## 5a. RP2040 bootrom — NOTICE gap (newly identified)

**Finding.** `roms/rp2040/bootrom-rp2040-b2.bin` is the official Raspberry Pi
RP2040 B2-stepping bootrom (BSD-3-Clause, copyright Raspberry Pi (Trading)
Ltd.). It is tracked in git and required for booting real SDK-based firmware
through `picogus_diff_rp2040`. The local LICENSE file
(`roms/rp2040/bootrom-rp2040-LICENSE.txt`) and `roms/rp2040/README.md`
correctly document its provenance, but the workspace-root `NOTICE` only
mentions the RP2350 bootrom — the RP2040 bootrom entry is missing.

This is a real legal gap for redistribution under BSD-3-Clause, which
requires the copyright notice and license terms travel with the binary.

**Plan.**

1. Add a `Raspberry Pi RP2040 B2 Bootrom` entry to `NOTICE` mirroring the
   existing RP2350 entry: source URL
   (<https://github.com/raspberrypi/pico-bootrom-rp2040>), copyright line
   verbatim from `bootrom-rp2040-LICENSE.txt`, and the BSD-3-Clause text.
2. Cross-link from the new `NOTICE` entry to
   `roms/rp2040/bootrom-rp2040-LICENSE.txt`.

This is a 5-minute change but should land in Session 1 alongside the other
NOTICE updates.

## 6. DOSBox-X patch (item 2)

`third_party/dosbox-x-picogus-tap.patch` — adds GUS + OPL3/Adlib port-write
logging to DOSBox-X. DOSBox-X upstream is GPLv2.

**Plan:** add a NOTICE entry pointing to <https://github.com/joncampbell123/dosbox-x>,
noting the patch is GPLv2-derivative, and that the patch is provided as-is for
researchers reproducing our PicoGUS audio fixtures. The patch file itself
already includes context lines from upstream — those are fine under GPLv2.

## 7. Monkey Island ADLIB fixture (item 3)

**Finding.** The fixture (`crates/picoem-harness/fixtures/monkey_island_adlib.trace`,
~29k lines) is a CSV of OPL3 register writes (port 0x388/0x389) with timestamps
in nanoseconds. It contains no game data, code, or audio samples — only the
sequence of FM-synthesis register writes the game's music driver emitted while
running under our patched DOSBox-X.

**Risk assessment.** This is functional output, analogous to recording a
musical performance's MIDI rather than its audio. The chiptune / OPL-research
community treats register-dump traces as fair use for emulator research.
LucasArts/Disney are unlikely to care about a 29k-line CSV of OPL3 register
writes. **My read: low risk; ship it.**

**Plan:**

1. Add a `fixtures/README.md` explaining what the trace is, how it was
   captured (DOSBox-X with our `picogus-tap` patch), what it's used for
   (FM-replay differential testing), and noting that no game data is included.
2. Mention in NOTICE under "Test fixtures" that the trace is functional
   capture, not redistributed game content.

## 8. Documentation audit (item 4)

**Scope.** 209 files in `wrk_docs/`, 151 files in `wrk_journals/`, plus
`tech_debt.md`, `RUNBOOK.md`, `CLAUDE.md`, scattered `notes/`. Roughly 360
review-eligible Markdown files.

**Approach.** Targeted greps for known leak categories, not line-by-line read.
The grep set:

| Pattern | Looking for |
|---|---|
| `<personal email pattern>`, `<maintainer email>` | Personal email beyond commit metadata |
| `<RP2354 probe serial>`, `<RP2040 probe serial>`, `2e8a:000c:` | Probe serial numbers — already in CLAUDE.md, decide whether OK to publish |
| `[A-Z]{3,}-\d{3,}` | JIRA / Linear ticket prefixes (none expected) |
| `password`, `secret`, `token`, `api[_-]?key` | Credentials |
| Names of any third party, employer, customer | Should be none — solo project — but worth grepping |
| `TODO[(:][^)]*\b\w+\b` | TODO(person) markers naming someone |
| Slack / Discord / internal-URL hostnames | None expected |
| `Co-Authored-By:` | Already extensively used (Claude); harmless, but note for transparency |

**Plan.**

1. Run the grep battery, dump matches to a `notes/oss-audit-flags.md`.
2. Spot-read 10 random files from each of `wrk_journals/` and `wrk_docs/`
   (reservoir sample with a fixed seed for reproducibility) for "tone /
   embarrassment" issues that grep won't catch — frustration with an upstream
   project, half-formed criticism of someone, etc.
3. Surface flags to Arthur in a short table; we triage together.

**Probe serials specifically.** They're hardware identifiers, not credentials
— a probe serial doesn't grant access to anything. **Decision:** OK to
publish, but move the probe-serial → DUT mapping out of `CLAUDE.md` into
`docs/probe_serials.md` (new). `CLAUDE.md` keeps a one-line pointer
("hardware probe ↔ DUT mapping lives in `docs/probe_serials.md`"), which
keeps the developer-onboarding doc cleaner. The new file gets cross-linked
from `RUNBOOK.md` and from the `probe_diff_*` binary headers.

## 9. probe-rs fork (item 5)

`third_party/probe-rs-0.31.0-mdrp-patched/` is a vendored fork of probe-rs
0.31.0, MIT/Apache-2.0 (LICENSE-MIT and LICENSE-APACHE both present in the
subdirectory).

**Plan:**

1. Add a NOTICE entry naming the upstream (<https://github.com/probe-rs/probe-rs>),
   the version pin, and the licenses.
2. Add a `third_party/probe-rs-0.31.0-mdrp-patched/PATCHES.md` summarising
   *what* we patched and *why*. (If the patch list is short — say, two or
   three changes — this is a 10-minute job.)
3. No upstreaming required for V1. We keep the vendored fork as-is for
   reproducibility.

## 10. Standard OSS files (item 6)

Add minimal versions of:

- **`CONTRIBUTING.md`** — how to build, where tests live, that hardware
  oracles need physical probes, our commit-message conventions, and the fact
  that this is a personal project so PR turnaround is best-effort.
- **`SECURITY.md`** — how to report security issues. For an emulator project
  the surface is small (untrusted firmware images executed in a sandboxed
  emulator); a one-paragraph "email Arthur, expect a few days" is fine.
- **`CODE_OF_CONDUCT.md`** — Contributor Covenant 2.1 verbatim is the
  conventional choice; takes 30 seconds.

**Decision:** SECURITY.md contact is `<maintainer contact>`.

## 11. Trademark disclaimer (item 7)

Add to README footer:

> *Raspberry Pi*, *RP2350*, *RP2354*, *RP2040*, and *Pico* are trademarks of
> Raspberry Pi Ltd. *Arm*, *Cortex-M0+*, *Cortex-M33*, *Armv6-M*, *Armv8-M*,
> and *NEON* are trademarks or registered trademarks of Arm Limited (or its
> subsidiaries) in the US and/or elsewhere. *Sound Blaster* is a trademark of
> Creative Technology Ltd. *AdLib*, *Gravis Ultrasound*, and *MT-32* are
> trademarks of their respective owners. *Monkey Island* is a trademark of
> Lucasfilm Entertainment Company Ltd. LLC.
>
> This project is an independent emulator and is not affiliated with,
> endorsed by, or sponsored by any of the above trademark holders. All
> trademarks are used for identification purposes only.

The longer disclaimer is warranted because the codebase materially uses
all of these names — Arm cores in source, audio-hardware names in PicoGUS
integration code and journals, *Monkey Island* explicitly in the
`monkey_island_adlib.trace` fixture filename. Standard practice for
emulator projects.

## 12. `.gitignore` audit + root-directory hygiene (item 8)

**Audit findings** (verified via `git ls-files` during the §16 critical
review, not just inferred from filenames):

- `bench-basic.json.gz` and `bench-basic.json.syms.json` — already
  `.gitignored` via the `bench-*.json.gz` / `bench-*.json.syms.json`
  rules. **Not tracked. No action.**
- `wrk_scratch/` — already explicitly `.gitignored` at directory level.
  **No action.**
- `wrk_debug/` — **not explicitly gitignored**, but contents (all `.log`
  files) match the global `*.log` rule, so it works *today*. Fragile:
  any non-`.log` file dropped there gets tracked. **Action:** add
  `wrk_debug/` to `.gitignore` for explicitness.
- `fuzz-runs/` — `.gitignore` already covers `fuzz-runs/deadline` and
  `fuzz-runs/**/*.exe`. The shell drivers + README ship. **No action.**
- `notes/` — has one tracked file: `notes/2026.04.13 - QEMU Diff First
  Run Report.txt`. References the pre-restructure crate name
  `mdrp2354-test-harness` which no longer exists; it's a stale historical
  artefact. CLAUDE.md treats `notes/` as a per-task scratchpad, so
  nothing should really be tracked there. **Action:** move to
  `wrk_docs/2026.04.13 - QEMU Diff First Run Report.md` (rename to `.md`,
  align with the dated-doc convention) and `git rm` the old path. Or
  simply delete — it's superseded by the working oracle anyway.
- **No** `.env`, `.pem`, `.key`, `id_rsa`, `*.crt` found in tracked
  files. **No** secrets / API keys / passwords found in source. **No**
  host-path leaks (`<user-profile>\…`, `/home/<user>/…`) in
  source/scripts/CLAUDE.md/README.md/RUNBOOK.md.
- **No** IDE / editor configs tracked (`.vscode/`, `.idea/`, `.cursor/`,
  `.claude/`). The only tracked dotfiles are `.cargo/config.toml`,
  `.gitignore`, `.gitmodules`, all clean.

**Net plan:** two specific cleanups (`wrk_debug/` → `.gitignore`,
`notes/2026.04.13 - QEMU Diff First Run Report.txt` → moved or deleted).
Everything else in this section is already in good shape.

## 13. crates.io — defer publication, claim placeholder names now

### 13.1 Why publish to crates.io eventually

- Discoverability. `cargo search rp2350` finds us; users can `cargo add rp2350-emu`.
- Forces a tidy public API on the library crates.
- Implicit version pinning for downstream users.
- Signals "this is a real project" rather than just a code drop.

### 13.2 Why not yet

- API stability commitment. Once `0.1.0` is on crates.io, anyone depending
  on it expects semver — surprise breaks earn issues.
- Dependency-graph ordering. Publishing `rp2350-emu` requires
  `picoem-common` to be on crates.io first; publishing `rp2040-emu` also
  requires `picoem-devices`. We'd be committing the whole library tier
  to public versioning, not just the hero crates.
- Versioning discipline. We currently bump `Cargo.toml` casually within
  the workspace; crates.io publication requires real semver and
  breaking-change tagging.
- README per crate. Each published crate needs its own README that
  renders well on crates.io — not a 5-minute job across the namespace.

### 13.3 Crate inventory

The workspace has nine top-level crates. Tiered by squat-risk:

**Tier 1 — claim definitely.** Library crates that would be published if
crates.io ever happens. Dependency ordering means we can't publish a
hero crate without these.

| Crate | Role |
|---|---|
| `rp2350-emu` | RP2350/RP2354 emulator library — the hero crate |
| `rp2040-emu` | RP2040 emulator library — the hero crate |
| `picoem-common` | Shared primitives — required prereq for both heroes |
| `picoem-devices` | Off-chip device models — required prereq for `rp2040-emu` |

**Tier 2 — claim if we want `cargo install` to work.** Niche enough that
the squat risk is low, but reserving them is cheap and keeps the install
story neat.

| Crate | Role |
|---|---|
| `rp2350-emu-tui` | TUI app. `cargo install rp2350-emu-tui` is a clean install path. |
| `rp2040-emu-tui` | Same for RP2040. |

**Tier 3 — namespace consistency.** Holding the `picoem-*` prefix
intact prevents anyone publishing a confusingly-similar crate.

| Crate | Notes |
|---|---|
| `picoem-debug` | Currently a stub. If the GDB stub gets built out, we'd want this. |
| `picoem-harness` | Internal test binaries. Minimal real squat risk; held for namespace coherence. |
| `picoem` (no suffix) | Project umbrella name. Prevents anyone publishing `picoem` and confusing search results. No façade crate yet. |

**Don't claim.** `epio-sys` — the underlying `epio` is a third-party
project (piersfinlayson/epio). The `-sys` convention belongs to upstream;
squatting it would be rude. Either coordinate with upstream or leave it
vendored.

### 13.4 Decision: publish for real in V1

After discussion: the codebase is mature enough that the public surface
(`Emulator`, `EmulatorBuilder`, `ExecutionModel`, `load_flash`/`run`/`step`)
has been stable for weeks; recent churn is internal. Deferring crates.io
behind a "let the public repo settle" wait was solving a problem (API
churn from outside usage) that doesn't start until we publish anyway.
There's no real benefit to making `cargo add rp2350-emu` not work on day
one.

**Plan (revised 2026-05-02).** Publish the four library crates as real `0.1.0`
releases under the new names (`picoem-common`, `picoem-devices`, `rp2350-emu`,
`rp2040-emu`). Reserve five `0.0.0` placeholders for namespace coherence and
to lock the install path: `picoem` (umbrella), `picoem-debug`, `picoem-harness`,
`rp2350-emu-tui`, `rp2040-emu-tui`. Total: 9 publications, fits within the
crates.io 10-new-crates-per-week-per-account limit.

| Crate | Action | Version |
|---|---|---|
| `picoem-common` | Real publication | `0.1.0` |
| `picoem-devices` | Real publication | `0.1.0` |
| `rp2350-emu` | Real publication | `0.1.0` |
| `rp2040-emu` | Real publication | `0.1.0` |
| `picoem-debug` | Reserved placeholder | `0.0.0` |
| `picoem-harness` | Reserved placeholder | `0.0.0` |
| `picoem` (umbrella) | Reserved placeholder | `0.0.0` |
| `rp2350-emu-tui` | Reserved placeholder | `0.0.0` |
| `rp2040-emu-tui` | Reserved placeholder | `0.0.0` |

Tier 2 (`rp2350-emu-tui`, `rp2040-emu-tui`) is now squatted as part of the
single batched publish session — see the table above. The names go into the
`0.0.0` placeholder pool alongside the Tier 3 namespace reservations. The
deferred decision is whether to flesh out a real `cargo install` story for
the apps; squatting the names doesn't commit to that, it just keeps the
install path locked.

### 13.5 Prerequisites for real publication

The four `0.1.0` releases have non-trivial prep — these are tasks, not
blockers, but they need to land before `cargo publish` runs:

1. **Reset all four library-crate versions to `0.1.0`.** Today's versions
   are `rp2350-emu 2.4.1`, `rp2040-emu 0.19.0`, `picoem-common 0.14.0`,
   `picoem-devices 0.7.1` — internal bookkeeping bumps from the
   workspace's pre-public history. Publishing `rp2350-emu 2.4.1` as the
   first public release would imply prior 2.x.0, 1.x.0, 0.x.0 history
   that doesn't exist. **Reset all four to `0.1.0`** for V1 publication;
   the pre-public version numbers remain in git history for anyone who
   cares to look.
2. **Per-crate READMEs.** crates.io renders each crate's README on its
   landing page. Today only the workspace has one. Each library crate
   needs a quickstart, a "this is part of the picoem workspace" note,
   and a link back to the GitHub repo. Roughly 2 hours total.
3. **Per-crate Cargo.toml metadata — clean-slate for all four crates.**
   Verified during §16 critical review: `description`, `repository`,
   `homepage`, `documentation`, `readme`, `keywords`, `categories`,
   `exclude`, `[package.metadata.docs.rs]` are *all missing* across the
   four library crates today. The `license` and `rust-version` fields
   are correctly inherited from `[workspace.package]` (license =
   `"MIT OR Apache-2.0"`, MSRV = `1.88`). Remaining work is from-zero,
   not "tweak existing fields." Each library crate needs:
   `description` (required by crates.io), `repository =
   "https://github.com/0x4D44/picoem"`, `homepage`, `documentation`
   (docs.rs URL), `readme = "README.md"`, `keywords` (≤ 5, e.g.
   `["emulator", "rp2350", "rp2040", "cortex-m", "embedded"]`),
   `categories` (e.g. `["emulators", "embedded"]`).
4. **API surface audit.** What's `pub` today that's actually internal?
   What's currently `pub(crate)` that consumers will need? Particular
   attention to `picoem-common` — it grew organically as a "shared
   bits" crate (`Memory`, `ClockTree`, PIO primitives, threading
   primitives, `Pacer`) and isn't deliberately designed as a public
   API. May need a `prelude` module or some `pub(crate)` tightening.
5. **Switch workspace deps from `path = "..."` to
   `version = "0.1", path = "..."`.** Standard pattern: `cargo publish`
   resolves `version` against crates.io while local dev still uses
   `path`. Affects `rp2350-emu/Cargo.toml`, `rp2040-emu/Cargo.toml`,
   `picoem-devices/Cargo.toml`, and the apps/harness if they depend
   on libraries we're publishing.
6. **Cross-platform build verification.** `rp2350-emu` and `rp2040-emu`
   gate `Threaded` / `Pacer` behind `cfg(target_arch = "x86_64",
   target_os = "windows")`. They *should* still build on Linux x86_64
   (which is what docs.rs uses) with those features unavailable.
   Confirm with `cargo check --target x86_64-unknown-linux-gnu` for
   each library crate before publishing — if a docs.rs build fails,
   the crate's first impression is "no documentation rendered."
7. **`exclude` directives in Cargo.toml.** Don't ship test fixtures,
   ROM binaries, or HLDs through `cargo publish`. Add `exclude = [
   "tests/", "fixtures/", ... ]` per crate so the published tarball
   stays small. crates.io has a 10 MB tarball limit.
8. **Decide on docs.rs build features.** docs.rs builds with
   `--all-features` by default; if `threading` or `testing` features
   break the build on Linux, we need a `[package.metadata.docs.rs]`
   section pinning a feature subset.

**Verified non-blocker:** the workspace-root `[patch.crates-io]
probe-rs = { path = "third_party/probe-rs-0.31.0-mdrp-patched" }` has
no effect on published crates, since neither `rp2350-emu` nor `rp2040-emu`
(the libraries) actually depend on probe-rs — only the unpublished
harness crate does. Confirmed by reading `[dependencies]` in each
library Cargo.toml during the §16 critical review.

### 13.6 Publish sequence

Once prereqs land, publish in dependency order:

```
picoem-common 0.1.0    (no internal deps)
       ↓
picoem-devices 0.1.0   (depends on picoem-common)
       ↓
rp2350-emu 0.1.0           (depends on picoem-common)
rp2040-emu 0.1.0           (depends on picoem-common + picoem-devices)
```

Parallel-safe to publish `rp2350-emu` and `rp2040-emu` at the same step.
Then publish the four `0.0.0` placeholders in any order.

After publication: tag the corresponding git release as `v0.1.0` and
write a short release-notes section in the GitHub release UI.

### 13.7 Mechanics

For each placeholder crate, create a tiny `Cargo.toml` with `version =
"0.0.0"`, the standard metadata block, and a one-line `lib.rs` /
`README.md` saying "Reserved name for the picoem project — see the
repository link."

`cargo publish` once per crate. crates.io's rate limit (10 new crates
per week per account) accommodates seven in one session comfortably.

Real publication of higher versions for the placeholders happens later
if and when those crates grow real content.

## 14. Submodule docs (item 11)

`.gitmodules` declares two submodules under `crates/epio-sys/third_party/`
(`epio` and `apio`). README currently doesn't mention them, and `epio-sys` is
already excluded from `default-members` so the default `cargo build` works
without them. Plan: add one paragraph to README under "Build & Test" noting
that `epio-sys` is opt-in (`cargo build -p epio-sys` requires `clang` and
`git submodule update --init`).

## 15. Final dry-run (item 11 in the table)

Once items 1–10 are done, validate end-to-end:

1. `cd $(mktemp -d) && git clone --recursive <repo>`
2. Follow README.md only (no inside knowledge).
3. Build: `cargo build --release`.
4. Run unit tests: `cargo test`.
5. Run a no-hardware oracle: `cargo run -p picoem-harness --release --bin qemu_diff_m33 -- --fuzz 1000` (assuming QEMU is installed).
6. Note any step that requires undocumented prior knowledge; fix README until
   the dry-run is clean.

This is the single most important step. If a third party can't get to "fuzz
test passing" from the README alone, we ship-broke regardless of how clean
the licensing is.

## 16. Decisions and remaining open questions

**Decided** (this revision):

1. ~~PicoGUS 3-byte patch~~ — identified as `test_psram` bypass (1-hour
   diagnostic shortened to immediate return). Ship patched binary alongside
   a `.patch` text file documenting the change. See §5.
2. ~~SECURITY.md contact~~ — `<maintainer contact>`. See §10.
3. ~~Probe serials~~ — OK to publish; move out of `CLAUDE.md` into
   `docs/probe_serials.md`. See §8.
4. ~~Repo name~~ — `picoem` on GitHub.
5. ~~GitHub account~~ — `0x4D44/picoem`.

6. ~~crates.io publication~~ — publish four library crates as real `0.1.0`,
   reserve three namespace-coherence names as `0.0.0`. Skip the apps. See §13.

**Still open:**

7. **crates.io API token.** Does Arthur already have a crates.io account
   with publish rights, or do we need to set one up? (`cargo login` step.)
8. **GitHub repo name availability.** Confirm `github.com/0x4D44/picoem`
   is unused before we start writing READMEs that point at it.

**Resolved during the §16 critical review** (no longer questions):

- ~~MSRV pin~~ — already pinned at workspace level: `rust-version = "1.88"`
  in `[workspace.package]`. All library crates inherit via
  `rust-version.workspace = true`. No further action needed.

## 17. Plan of work

Sequencing — three sessions of work, plus a final publish step.

**Session 1: legal / third-party.**
- §5 PicoGUS — vendor binaries + write the `test_psram` bypass `.patch` file
- §5a RP2040 bootrom NOTICE entry (newly identified gap)
- §6 DOSBox-X NOTICE entry
- §7 Monkey Island fixture README
- §9 probe-rs PATCHES.md + NOTICE entry

**Session 2: hygiene + audit.**
- §8 doc audit grep + sample (delivers `notes/oss-audit-flags.md` for review)
- §8 / §4a probe serials → `docs/probe_serials.md`
- §10 standard OSS files (`CONTRIBUTING.md`, `SECURITY.md`,
  `CODE_OF_CONDUCT.md`)
- §11 trademark disclaimer in README
- §12 `.gitignore` audit + cleanup of stale root-directory artefacts
- §14 submodule docs in README
- §15 dry-run clone-and-build

**Then:** Arthur reviews `notes/oss-audit-flags.md` and we triage flags. After
that, the repo is ready to be flipped public on GitHub as `0x4D44/picoem`.

**Optional polish that can also land in Session 2** (low-priority items
from the §16 critical review):
- Add a one-line note in `CONTRIBUTING.md` that workspace `LICENSE-*`
  files cover all source files (no per-file SPDX headers expected).
- Add a minimal `CHANGELOG.md` with a single `0.1.0 — initial public
  release` entry, then maintain going forward.
- Confirm `github.com/0x4D44/picoem` is available before the dry-run.

**Session 3: crates.io prep + publish** (§13.5 prereqs, then §13.6 publish).
- Per-crate READMEs (4 library crates).
- Per-crate Cargo.toml metadata (description, license, repository,
  keywords, categories, MSRV, exclude).
- API surface audit, particularly on `picoem-common`.
- Switch workspace deps to `version + path` form.
- `cargo check --target x86_64-unknown-linux-gnu` smoke test for each
  library crate (docs.rs build sanity).
- Publish in dependency order: `picoem-common` → `picoem-devices` →
  `rp2350-emu` + `rp2040-emu`.
- Publish the three `0.0.0` placeholder crates.
- Tag git as `v0.1.0`, write GitHub release notes.

This last session is gated on Arthur having a crates.io API token
(`cargo login`) and being available for the actual `cargo publish`
calls — those are user-authored actions, not something Claude executes
unattended.

## 18. Risks

- **Doc audit miss.** 360 Markdown files is a lot; targeted grep can miss
  free-form prose embarrassments. Mitigation: random-sample spot-read; once
  public, anything missed can be fixed in a follow-up commit.
- **Hardware-only oracles look "broken" to outside observers.** A casual
  visitor sees `silicon_*` binaries that "fail" or skip on their machine.
  Mitigation: README is already explicit about this; reinforce in
  CONTRIBUTING.md.
- **Surprise Cargo.lock churn after publication.** Public CI doesn't apply
  (no CI), but anyone running `cargo update` will see drift. Not really a
  risk, just expected.
- **Semver discipline burden post-publication.** Once `0.1.0` is on
  crates.io, every breaking API change forces a `0.2.0` and release-notes
  call-out. We've never had to think about this before — the workspace
  has been free to refactor `Bus` methods or shuffle the builder casually.
  Mitigation: `0.x` semver explicitly allows breakage; the obligation is
  to bump the minor version and document, not to *not* break. Still, this
  is a new ongoing cost that didn't exist before.
- **docs.rs build failure on first publish.** If our cross-platform check
  misses a Linux-only build issue, the crate's first impression on
  crates.io is "documentation: failed to build." Mitigation: §13.5
  prereq 6 (`cargo check --target x86_64-unknown-linux-gnu`) before
  every `cargo publish`. Recoverable post-hoc by publishing a patch
  release with a fix or a `[package.metadata.docs.rs]` feature pin.
- **First user file an issue we can't reproduce.** Without CI, we don't
  catch regressions outside our own dev hosts. First external bug report
  on Linux/macOS could be a real puzzle. Mitigation: explicit "Windows
  x86_64 is the developer platform; other targets best-effort" note in
  README + CONTRIBUTING.
