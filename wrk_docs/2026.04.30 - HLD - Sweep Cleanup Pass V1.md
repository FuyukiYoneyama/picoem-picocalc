# HLD — Sweep Cleanup Pass V1

**Date:** 2026-04-30
**Workstream:** W1 of the 2026-04-29 sweep findings resolution
**Source action plan:** `wrk_docs/2026.04.29 - Codebase Tech-Debt Sweep Action Plan.md` (Phase 1 + Phase 2.1)
**Source report:** `wrk_docs/2026.04.29 - Codebase Tech-Debt Sweep Report.md`

---

## 1. Goal

Land Phase 1 (one-line fixes) and Phase 2.1 (fully-dead-code deletions) from the sweep action plan in a single bounded cleanup pass. Concretely: fix one latent panic (`Memory::load_rom` on the RP2040 16 KB ROM path), remove one silent infinite-loop footgun (`step_quantum=0` on both chip builders), shed eight stale `#[allow(dead_code)]`/dead-helper sites that no production path can reach, archive eleven superseded HLD versions, and align the CLAUDE.md device-version line to the actual `Cargo.toml`. No design changes; no public-API behaviour changes outside the two narrow bug fixes; no touch on forward-scaffolding (D9), the encapsulation migration (Theme A), or the iter7-fast-path remnants (D5). Verification rests on `cargo build --release --all-targets`, the existing unit test suites in `mdpicoem-common`/`mdrp2350`/`mdrp2040`, plus two new unit tests pinning the bug fixes (B1, B2 — one per chip for the latter).

---

## 2. Inventory of changes

| # | File:line (current) | Change | Action-plan ID |
|---|---|---|---|
| 1 | `crates/mdpicoem-common/src/memory.rs:60` | Replace `data.len().min(ROM_SIZE)` with `data.len().min(self.rom.len())`. Symmetric with `load_flash` at `:161`. | B1 (Phase 1.1) |
| 2 | `crates/mdpicoem-common/src/memory.rs` `mod tests` | Add `load_rom_clamps_to_rom_len_not_constant`: `Memory::with_sizes(16 * 1024, 0)`, push 32 KB, expect no panic and verify the first 16 KB is the input prefix and read past `0x4000` returns 0. | B1 test |
| 3 | `crates/mdrp2350/src/lib.rs:1501-1505` | Replace `debug_assert!(n > 0, ...)` with `n.max(1)`. | B2 (Phase 1.2) |
| 4 | `crates/mdrp2350/src/lib.rs` `mod tests` | Add `step_quantum_zero_clamps_to_one`: builder with `.step_quantum(0)`, build, assert `step()` returns within bounded cycles (no infinite loop). | B2 test |
| 5 | `crates/mdrp2040/src/lib.rs:1413-1417` *(action plan miscited as `:1379-1381`; the `:1379-1381` range is `mmio_read32`)* | Same `n.max(1)` change. | B2 (Phase 1.2) |
| 6 | `crates/mdrp2040/src/lib.rs` `mod tests` | Mirror test for the M0+ builder. | B2 test |
| 7 | `CLAUDE.md:97` | Strike the trailing "Version-pinned 0.7.x at time of writing" — the actual `crates/mdpicoem-devices/Cargo.toml:3` is `0.1.0`. Drop the clause. | H1 (Phase 1.3) |
| 8 | `crates/mdpicoem-harness/src/bin/qemu_diff_m33.rs:170` | Replace `"localhost:3333"` with `&QemuProfile::M33_RP2350.gdb_addr()`. Sibling pattern: `qemu_diff_m0plus.rs:150`. | H2 (Phase 1.3) |
| 9 | `crates/mdpicoem-harness/src/bin/qemu_diff_m33.rs:414` | Same replacement in `respawn_qemu`. | H2 (Phase 1.3) |
| 10 | `crates/mdrp2040/src/bus/mod.rs:389` | `pub dma: Dma` → `pub(crate) dma: Dma`. Update doc comment. | H3 (Phase 1.3) |
| 11 | `crates/mdrp2040/src/bus/mod.rs` (new method) | Add `pub fn dma_channel(&self, i: usize) -> &dma::DmaChannel { self.dma.channel(i) }`. Required because external readers exist (see §4 below) — the H3 entry in the action plan claims "1-2 sites" but the reader requires a `&DmaChannel` borrow, not bytes-via-MMIO. | H3 (Phase 1.3) |
| 12 | `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs:2663` | `emu.bus.dma.channel(ch)` → `emu.bus.dma_channel(ch)`. | H3 (Phase 1.3) |
| 13 | `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs:2758` | Same. | H3 (Phase 1.3) |
| 14 | `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs:2775` | Same. | H3 (Phase 1.3) |
| 15 | `crates/mdpicoem-common/src/pacer.rs:160` | Drop the `unsafe { ... }` wrapper around `std::arch::x86_64::__cpuid` (now safe). Demote the existing `// SAFETY:` line to a plain `//` comment. | H8 (Phase 1.3) |
| 16 | `wrk_docs/2026.04.17 - HLD - RP2350 Coverage Gap Fill V{1..11}.md` | `mkdir -p wrk_docs/archive && git mv ...V{1..11}.md wrk_docs/archive/`. Keep V12 (`2026.04.18 - ...`) in place. Verified 11 files with `ls`. | Doc1 (Phase 1.3) |
| 17 | `crates/mdrp2040/src/core/mod.rs:36` | Delete the file-level `#[allow(dead_code)]` annotation on `Fault`. All six variants verified ≥3 live constructors (§4 below). | D2 (Phase 1.4) |
| 18 | `crates/mdrp2040/src/bus/ppb.rs:73` | Delete the `#[allow(dead_code)]` annotation on `exception_priority`. Live callers at `core/mod.rs:345, 348` and `core/exceptions.rs:159`. | D3 (Phase 1.4) |
| 19 | `crates/mdrp2350/src/core_riscv/mod.rs:358, 365, 372` | Delete the three `#[allow(dead_code)]` annotations on `mip` / `set_mip` / `mie`. Live callers at `lib.rs:804, 825, 1168, 2179-2180, 2184, 2199` plus `core_riscv/tests_p4.rs` and `tests_p5.rs`. | D4 (Phase 1.4) |
| 20 | `crates/mdrp2350/src/bus/mod.rs:3234-3247` | Delete free function `sram_bank_wait` (and its `#[allow(dead_code)]`). Zero callers in production; only doc-comment back-references survive. | D6 (Phase 2.1) |
| 21 | `crates/mdrp2350/src/bus/ppb.rs:692-703` | Delete `Ppb::set_irq_pending`. | D7 (Phase 2.1) |
| 22 | `crates/mdrp2350/src/bus/ppb.rs:727-738` | Delete `Ppb::clear_irq_pending`. | D7 (Phase 2.1) |
| 23 | `crates/mdrp2350/src/bus/ppb.rs:749-761` | Delete `Ppb::irq_enabled`. | D7 (Phase 2.1) |
| 24 | `crates/mdrp2350/src/bus/ppb.rs:1180-1193, 1208-1217, 1219-1228, 1230-1254` | Rewrite the four in-file tests that used these helpers. Replace `ppb.set_irq_pending(N)` with the production-path equivalent: direct `nvic_ispr` MMIO write through `ppb.write32(NVIC_ISPR_BASE + word*4, 1u32 << bit)`. The tests that *test* the deleted helpers themselves (§4 detail below) are deleted outright; tests that *use* the helpers as setup helpers retain their original assertion targets but switch setup to MMIO. | D7 (Phase 2.1) |
| 25 | `crates/mdrp2350/src/tests.rs:21079-21084` (`set_irq_pending_out_of_range_noop`) | Delete the test outright — it tests only the deleted helper. | D7 (Phase 2.1) |
| 26 | `crates/mdrp2350/src/tests.rs:21086-21090` (`clear_irq_pending_out_of_range_noop`) | Delete the test outright. | D7 (Phase 2.1) |
| 27 | `crates/mdrp2350/src/tests.rs:21092-21100` (`set_irq_active_and_check_enabled`) | Delete the test outright — its assertions go through `irq_enabled`, the deleted helper. The same coverage exists via `ppb.write32(NVIC_ISER0, ...)` round-trips already covered by `test_nvic_iser1_bit_20_to_31_res0_for_spare_shims` and others. | D7 (Phase 2.1) |
| 28 | `crates/mdpicoem-common/src/fifo.rs:55-58` | Delete `Fifo::len`. Verified zero callers — `tx_fifo.len()` / `rx_fifo.len()` in `mdrp2350::peripherals::uart` are calls on `VecDeque<u8>`, not on this `Fifo` type. | D8 (Phase 2.1) |

---

## 3. Verification plan

Run after each commit category, then again at end of pass. All commands from `<picoem-workspace>`.

| Step | Command | What it proves |
|---|---|---|
| V1 | `cargo build --release --all-targets` | Workspace compiles after every visibility / dead-code change. Catches any orphaned `pub`-field reader I missed. |
| V2 | `cargo build --release --all-targets --features threading,testing` | Same with the optional features that the threaded panic-containment tests turn on. |
| V3 | `cargo test -p mdpicoem-common` | New `load_rom_clamps_to_rom_len_not_constant` (B1) passes; `Fifo` callers compile after `len` removal; existing `rom_load_and_read_roundtrip` still passes (it uses `Memory::new()` so the 32 KB clamp behaviour is preserved by `self.rom.len()`). |
| V4 | `cargo test -p mdrp2350` | All instruction/exception/PPB tests pass; the rewritten PPB self-tests (rows 24-27 above) pass against the production `nvic_ispr` writer path; `tests.rs` compiles after the three deletions. |
| V5 | `cargo test -p mdrp2040` | New `step_quantum_zero_clamps_to_one` (B2) passes; PPB compile-check after dropping the `exception_priority` annotation. |
| V6 | `cargo test -p mdrp2350 --features threading` (smoke) | Confirms the dual-model integration test still runs after the threaded-bus and PPB changes. Not strictly required by this workstream, but cheap. |
| V7 | `cargo run -p mdpicoem-harness --release --bin qemu_diff_m33 -- --fuzz 1000` | Smoke check that the H2 `gdb_addr()` substitution doesn't break the GDB connect path. (Optional — only if a QEMU is available; otherwise skip.) |
| V8 | `cargo build -p mdpicoem-harness --release --bin picogus_diff_rp2040` | The H3 + new accessor compile cleanly. The binary itself isn't run in CI. |
| V9 | `git ls-files wrk_docs/archive/ \| wc -l` then check V12 stays | Doc1 archive move worked; V12 (`2026.04.18`) still in `wrk_docs/`. |

**Test-count discipline:** total new tests = 3 (B1, B2-rp2350, B2-rp2040). Net unit-test delta in `mdrp2350` = +0 to -3 (three deleted PPB self-tests, but the rewritten in-file PPB tests at `bus/ppb.rs:1180-1254` keep the same assertion targets after the helper substitution).

---

## 4. Risks and mitigations

### 4.1 D7 — deleting `Ppb::set_irq_pending` / `clear_irq_pending` / `irq_enabled`

**Risk:** the action plan claims production paths use `merge_irq_pending`, direct `nvic_ispr` writes, and `any_pending_enabled`. If any production path actually does call the doomed helpers, deletion is a regression.

**Verification I did myself:**

```
grep -rn "\.set_irq_pending(\|\.clear_irq_pending(\|\.irq_enabled(" crates/ --include="*.rs"
```

Findings:

- `set_irq_pending` — 30+ callers, but **all on `CoreAtomics::set_irq_pending` in `threaded/atomics.rs`**, not the `Ppb` method. The two are name-collision twins; the dead-code report flagged this as one of the reasons to delete the `Ppb` version. Production calls (`crates/mdrp2350/src/core/mod.rs:980`, `lib.rs:1360`) are atomics-side. Tests in `core_riscv/tests_p4.rs` call `bus.atomics.set_irq_pending(...)`, not the PPB method.
- `clear_irq_pending` — only `Ppb` method definition + the one self-test in `tests.rs:21086`.
- `irq_enabled` — only `Ppb` method definition + the one self-test in `tests.rs:21092`.

So zero production callers of the three doomed `Ppb` methods. The four in-file PPB self-tests at `bus/ppb.rs:1180, 1208, 1219, 1230` use `set_irq_pending` for *setup* before testing other functions (`highest_priority_pending_irq`). Those four can be rewritten to use `merge_irq_pending` (existing public method, identical semantics for the in-range bit case) instead of being deleted. The action plan's "four self-tests" reference covers those — but they're salvageable, not redundant. Plan: keep their assertions; switch setup to `merge_irq_pending`.

**Mitigation:** the rewritten tests run under V4. If any of the four assertion targets changes behaviour (it shouldn't — `merge_irq_pending` is the production version of the same write), the test will catch it at run time.

### 4.2 H3 — RP2040 `pub dma` → `pub(crate) dma`

**Risk surfaced before writing this HLD:** the action plan says "external readers (likely 1-2 sites)". Reality from grep:

```
grep -rn "bus\.dma" crates/ --include="*.rs" | grep -v "/mdrp2040/src/" | grep -v "/mdrp2350/src/"
```

returns three sites, all in `crates/mdpicoem-harness/src/bin/picogus_diff_rp2040.rs:2663, 2758, 2775`. Each fetches `&DmaChannel` via `emu.bus.dma.channel(ch)` and reads internal-state fields (`trig_ctrl`, `ever_wrote_pio1_txf_mask`, `transfers_issued`, `dreq_observed_mask`, `ctrl`) for diagnostic verdicts. None of these are reachable through MMIO without going through the (write-side-effecting) bus reads. Also: the existing `pub dma` carries an explicit doc-comment justification:

```
/// `pub` so diagnostic harnesses (e.g. `picogus_diff_rp2040`) can
/// read per-channel observation counters on `DmaChannel` without
/// going through MMIO.
```

So the original author *deliberately* kept it `pub`. The action plan's framing of "fix the asymmetry" runs against this deliberate decision.

**Mitigation:** add a `pub fn dma_channel(&self, i: usize) -> &DmaChannel` accessor on `Bus` (row 11 above) that routes through `Dma::channel`, which is already `pub` at `crates/mdrp2040/src/dma.rs:320`. This matches the PR-friendlier "method-not-field" rule of thumb without losing the harness diagnostic observability. The `picogus_diff_rp2040` migration is mechanical (3 sites, single rename).

**Open question lifted to §7:** is the action-plan framing here actually correct — i.e. should we disregard the explicit pub justification — or should we leave `pub dma` alone and update the doc comment to call out the asymmetry as deliberate? See §7 Q1.

### 4.3 D2 — RP2040 file-level `#[allow(dead_code)]` on `Fault`

**Verification:**

```
grep -rn "Fault::Undefined\|Fault::Unaligned\|Fault::HardFault\|Fault::Svc\|Fault::InvalidExcReturn\|Fault::InvalidEpsr" crates/mdrp2040/src/
```

Each variant has multiple constructors:

- `Undefined`: 4 sites (`execute_wide.rs:191, 235, 250`, `execute.rs:999`)
- `Unaligned`: 16 sites in `execute.rs`
- `HardFault`: 2 sites (`execute.rs:847`, `exceptions.rs:144`)
- `Svc`: 1 constructor (`execute.rs:963`) + 1 match-arm (`exceptions.rs:307`)
- `InvalidExcReturn`: 2 sites (`exceptions.rs:212, 223`)
- `InvalidEpsr`: 3 sites (`execute.rs:458, 470, 835`)

All ≥1 live, so the `#[allow(dead_code)]` is genuinely stale. (The report claimed "≥3 live constructors" — `Svc` and `HardFault` are below that bar but still live, which is what matters.)

### 4.4 D6 — `Bus::sram_bank_wait` deletion

**Verification:** `grep -rn "sram_bank_wait" crates/ --include="*.rs"` returns ten hits — nine are doc-comment back-references in `decode.rs` and `bus/mod.rs` ("`sram_bank_wait` removed: bank penalty on instruction fetch only"), one is the function definition itself. There is no call site. The harness comment at `crates/mdpicoem-harness/src/lib.rs:6119` references the *algorithm*, not the function. Deletion is safe; the doc-comment back-references can stay (they are accurate even with the function gone — they describe the historical fact and the current contract).

### 4.5 D8 — `Fifo::len` deletion

**Verification:** the only `*.fifo.len()` and `*.rx_fifo.len()` calls are in `crates/mdrp2350/src/peripherals/uart.rs`, where the type is `VecDeque<u8>` (line 153-154), not `mdpicoem_common::Fifo`. Two `Fifo` importers exist (`mdrp2350::sio::mod`, `mdrp2040::bus::sio`); neither calls `len()`. Safe.

### 4.6 H8 — `unsafe { __cpuid(...) }` simplification

**Risk:** stable Rust may not yet flag the wrapper as unnecessary; the change must compile clean on the workspace's pinned toolchain. **Mitigation:** I will compile under release; if stable doesn't accept the bare `__cpuid(0x80000007)` call (because the function still requires `unsafe` on the pinned toolchain), revert this row and add a tracker entry pinned to the next-toolchain-bump. The action plan flags this as nightly-only-warns; stable should accept either form silently.

### 4.7 Doc1 — moving 11 HLDs to `wrk_docs/archive/`

**Risk:** any markdown link or cross-reference inside other HLDs/journals that points at `2026.04.17 - HLD - RP2350 Coverage Gap Fill V{1..11}.md` will break. **Mitigation:** I'll grep for cross-references before the move and either fix the links or leave them broken if they were already pointing at superseded versions (in which case fixing them is out of scope for a hygiene pass — surface in §7 if it turns up).

---

## 5. Sequencing

**Proposal:** four atomic commits, in this order, separated by category. Argument for category-batching over one-big-commit and over per-file commits:

1. **Single commit** would mix bug fixes (B1 panic; B2 infinite loop) with cosmetic deletions (D2, D3, D4 annotations) with structural moves (Doc1 11-file archive). If a regression surfaces, `git bisect` lands on a 1000-line commit with no clue which sub-change broke things.
2. **Per-file commits** dilutes review. The hygiene one-liners (H1, H8) and the annotation deletions (D2, D3, D4) have nothing to verify individually beyond "still compiles" — they're not bug fixes; they don't deserve their own review.
3. **Per-category commits** strikes the balance: bug fixes get isolated history (so a future regression `git log`-grep finds the actual fix); dead-code stays together (one block of grep evidence in the message); hygiene + archive stays together (cheapest-to-revert).

Proposed commits:

| Commit | Contents | Action-plan items |
|---|---|---|
| C1 — `cleanup: fix Memory::load_rom and step_quantum panic/loop footguns` | rows 1–6 (B1 + B1 test, B2 + B2 tests on both chips) | Phase 1.1, 1.2 |
| C2 — `cleanup: hygiene one-liners (CLAUDE.md drift, qemu_diff_m33 hardcoded port, RP2040 Bus::dma asymmetry, pacer __cpuid wrapper)` | rows 7–15 | Phase 1.3 (H1, H2, H3, H8) |
| C3 — `cleanup: archive superseded RP2350 Coverage Gap Fill HLDs (V1..V11)` | row 16 | Phase 1.3 (Doc1) — kept separate so the file-rename diff is reviewable in isolation |
| C4 — `cleanup: drop stale dead-code annotations and fully-dead helpers` | rows 17–28 | Phase 1.4, Phase 2.1 (D2, D3, D4, D6, D7, D8) |

Each commit ends in a green workspace build + green test pass. C1 is the only commit with new tests; C2-C4 are pure compile-check.

**Rationale for putting Doc1 in its own commit:** `git mv` of 11 files makes a noisy diff that drowns C2's one-line hygiene changes. Splitting them lets the reviewer skim C2 in 30 seconds and review C3 as a pure rename block.

**Rejected ordering:** running C4 before C1 would trip `cargo test` in the middle of bug-fix landing if a deletion breaks an unrelated test by name collision. Order C1 → C2 → C3 → C4 ensures every step has a green test gate.

---

## 6. Out of scope

The action plan and source report flag many items that this workstream does NOT touch:

| Item | Why deferred |
|---|---|
| Phase 2.2 (D5) — `WorkerBus::is_pure` / `FLAG_PURE` / `classify_is_pure` | Needs Arthur's iter7 decision; not part of this workstream. |
| Phase 2.3 (D9) — five forward-scaffolding items in `threaded/` subtrees | Per project instruction, "owner sign-off" required; explicitly out of scope. |
| Phase 2.4 — `Fault::MemManage` retain note | Explicitly handled in tracker by Arthur (per the workstream brief). |
| Phase 3 — tracker updates (T1, T2, T3, B3, B4, R1, H5, H7, M3, "Half-modelled architectural state" section, `debug_assert!` policy line) | Arthur is doing `tech_debt.md` and CLAUDE.md narrative updates in parallel; this workstream only edits CLAUDE.md for H1 (the one drift line) and does not touch `tech_debt.md`. |
| Phase 4 — test cleanup (R2 PPB-bypass tests, HFSR seed helper, H4 SAFETY comments) | Separate workstream. |
| Phase 5 — refactors (Du1 paced_bench, Du2 threaded helpers) | Separate workstream. |
| Phase 6 — multi-day items (encapsulation migration, MPU enforcement, LDREX granularity, mutation triage) | Each has its own trigger; explicitly multi-day. |
| The 2026-04-29 testing audit findings | Different workstream. |
| The mutation-testing v2 triage | Has its own HLD; out of scope. |
| Touching anything in `crates/mdrp2350/src/threaded/`, `crates/mdrp2040/src/threaded/`, `crates/mdpicoem-common/src/threaded/`, `crates/mdrp2350/src/tests_narrow.rs`, or `crates/mdrp2350/src/tests.rs:10520, 10570` | Other workstreams in flight per the workstream brief. |

---

## 7. Open questions

These need a one-sentence answer before Phase B implementation. Each is something I am unsure about despite verifying the action-plan claims.

**Q1.** H3 (RP2040 `pub dma` → `pub(crate) dma`). The current `pub` carries a deliberate doc-comment justification — *"`pub` so diagnostic harnesses (e.g. `picogus_diff_rp2040`) can read per-channel observation counters on `DmaChannel` without going through MMIO"*. The action plan asks me to override that decision and add an accessor (`Bus::dma_channel`). The accessor is fine; it's API-surface-equivalent. **But** — should I do this at all, or update the doc comment to acknowledge the deliberate asymmetry with RP2350 and move on? The report's stated motivation (consistency with RP2350) is weak compared to the existing in-source comment's stated motivation (harness ergonomics).

**Q2.** D7 — the action plan says "delete the four self-tests at `tests.rs:21082, 21089, 21096-21099`". The line numbers correspond to *three* test functions in `tests.rs` (rows 25-27 of §2). The fourth implied test is in `bus/ppb.rs:1180-1193` (`test_nvic_ispr_self_pend_bit_48_sets_ispr_bit`) — that one tests *only* the deleted `set_irq_pending` helper, and would be deleted, not rewritten. The other three in `bus/ppb.rs` (`test_highest_priority_pending_irq_*`) use `set_irq_pending` for setup but test other functions; my plan is to **rewrite their setup** to `merge_irq_pending`, not delete them. Confirm this matches your intent? If you want all seven deleted, that's fewer keystrokes; if the action plan really meant only the three in `tests.rs`, also fine.

**Q3.** H8 (`__cpuid` unsafe wrapper). The action plan flags this as "nightly warns; stable doesn't yet". I'll attempt the change and revert if stable refuses. Should the revert path also strip the `// SAFETY:` comment to a regular comment, or revert that too? My read of the action plan is "demote SAFETY note to plain comment" regardless of whether the `unsafe { }` actually goes away. If stable still requires `unsafe`, the comment is still SAFETY-meaningful, so I'd keep it. Confirm.

**Q4.** Doc1 — should the moves be `git mv` (preserving rename history) or plain `mv` (which `git status` will show as delete + add)? `git mv` is friendlier; plain `mv` is what the action-plan diff literally shows. Defaulting to `git mv` unless told otherwise.

**Q5.** Verification step V7 (running `qemu_diff_m33 --fuzz 1000`) requires QEMU on PATH. The CLAUDE.md doesn't mark this as available in the dev sandbox. Is that an OK skip if QEMU isn't installed, or do I need to satisfy it some other way? Default plan: if `qemu-system-arm` isn't available, skip V7 with a note in the commit message; rely on V1/V2 build-only confirmation that the GDB connect string is well-typed.

**Q6.** The action-plan line numbers for the RP2040 `step_quantum` builder are `:1379-1381`. The actual builder method in `crates/mdrp2040/src/lib.rs` is at `:1413-1417`; `:1379-1381` is `mmio_read32`. I assume the plan meant the builder (matching the RP2350 entry's structure). Confirm — and if the plan really intended `:1379-1381`, what change there?

