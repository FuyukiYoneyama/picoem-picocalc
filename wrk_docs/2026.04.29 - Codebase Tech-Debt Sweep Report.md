# Codebase Tech-Debt Sweep — Findings Report

**Date:** 2026-04-29
**Scope:** Whole-workspace sweep of `<picoem-workspace>` (~210k LOC across 244 Rust files in 9 crates).
**Method:** Five-agent multi-perspective sweep — four investigators in parallel, one devil's advocate against their findings.

---

## 1. Methodology

### 1.1 Team composition

| Role | Brief | Output |
|---|---|---|
| **Dead-Code Hunter** | Unused fns/types/modules, stale `#[allow(dead_code)]`, stub modules, code rendered obsolete by later work | `/tmp/sweep_dead_code.md` (125 lines) |
| **Duplicate-Code Detector** | Cross-crate copy-paste between mdrp2350 and mdrp2040, harness-binary boilerplate, missing abstractions | `/tmp/sweep_duplicates.md` (311 lines) |
| **Tech-Debt Scout** | Latent debt the existing `tech_debt.md` does NOT already capture: hacks, design erosion, missing tests, configuration knobs | `/tmp/sweep_tech_debt.md` (440 lines) |
| **Obvious-Wrongness Inspector** | Things that look broken: off-by-ones, broken invariants, concurrency bugs, error-handling holes, suspicious unsafe | `/tmp/sweep_wrongness.md` (329 lines) |
| **Devil's Advocate** | Read all four reports, verified the load-bearing claims against source, pushed back on false positives, surfaced blind spots | `/tmp/sweep_devils_advocate.md` (148 lines) |

Each investigator was instructed to read `tech_debt.md` (1698 lines) **first** and skip anything already tracked, so this report represents net-new findings.

### 1.2 What this report is — and isn't

This report is **post-dissent**: every finding below has either been verified by the devil's advocate or downgraded/upgraded based on what it found. Where the devil's advocate disagreed with the original framing, that disagreement is recorded explicitly — sometimes the original finding survives the challenge, sometimes it doesn't.

This report is **NOT** a re-run of `wrk_docs/2026.04.29 - Testing Audit and Risk Review.md` (the testing-coverage pass) or `wrk_docs/2026.04.29 - HLD - Mutation Testing V2 Triage.md` (the mutation-testing triage). Both are existing artefacts in `wrk_docs/`. One of the devil's-advocate blind-spot findings is that the wrongness investigator did not read the testing audit and partially re-discovered ground it had already covered — see §6.

---

## 2. Findings by category

### 2.1 Real bugs (high confidence, fixable)

**B1. `Memory::load_rom` panics on >16 KB ROM into RP2040** *[verified, devil's-advocate confirmed]*

- **Where:** `crates/mdpicoem-common/src/memory.rs:59-62`
- **What:** `load_rom` clamps to a workspace-common `ROM_SIZE = 32 KB` constant (the RP2350 size) instead of `self.rom.len()`. RP2040 constructs `Memory` with `rom_size = 16 KB`. Any caller passing >16 KB into `Bus::load_bootrom` (`crates/mdrp2040/src/bus/mod.rs:736-739`) triggers a slice-bounds panic.
- **Why it survives review:** The sister method `load_flash` at `memory.rs:157-167` already does this correctly (`data.len().min(self.xip.len())`). This is an asymmetric oversight — whoever fixed `load_flash` didn't backport.
- **Caller exposure today:** Zero — the only in-tree consumer is the 16 KB `gen_blinky.py` bootrom. But the API takes `&[u8]` with no documented size limit; a future test or larger bootrom trips it.
- **Fix:** One line — replace `ROM_SIZE` with `self.rom.len()`. Symmetric with `load_flash`.

### 2.2 Latent footguns (high severity, no current trigger)

**B2. `step_quantum=0` produces silent infinite loop in release** *[verified]*

- **Where:** `crates/mdrp2350/src/lib.rs:1501-1505`, `crates/mdrp2040/src/lib.rs:1379-1381`
- **What:** Public builder method `step_quantum(n: u32)` uses `debug_assert!(n > 0, ...)` for input validation. `debug_assert!` is elided in `--release`. With `n = 0` the Serial path enters `while clock.cycles < target` where `target == clock.cycles`, then `clock.advance(0)` no-ops — true infinite loop. Threaded path is safe (has `step_q.max(1)` at `lib.rs:871`).
- **Caller exposure today:** Zero in-tree callers pass 0.
- **Fix:** One line — `n.max(1)` clamp at the builder, or return `Result<Self, ConfigError>`.
- **Cross-cutting:** This is the canonical example of `debug_assert!` used on a user-input path. Theme C in §3 below.

**B3. MPU registers stored but not enforced — silent divergence on MPU-protected firmware** *[verified, distinct from existing TrustZone debt]*

- **Where:** `crates/mdrp2350/src/bus/ppb.rs:99,340,552` (registers), `crates/mdrp2350/src/core/execute_fpu.rs:1216-1230` (lazy-flush comment: "MemManage — *not yet wired*"), `crates/mdrp2350/src/core/exceptions.rs:760-799` (only `TT` instruction consults regions).
- **What:** PPB stores `MPU_CTRL/RNR/RBAR/RLAR` and round-trips firmware writes cleanly. There is no MPU-region predicate on actual data reads/writes. Firmware writing `MPU_CTRL.ENABLE=1` sees all loads/stores succeed regardless of RBAR/RLAR.
- **Distinct from:** `tech_debt.md:1588` (TrustZone S-bit at exception entry) — that one bites NS exception entry; this one bites every MPU-protected workload.
- **Caller exposure today:** No corpus firmware enables MPU. Phase-8 work will surface this.
- **Fix:** Add `Bus::mpu_check(addr, kind, core) -> Option<Fault>` predicate, plumb through `read*/write*` and FPU lazy-flush. Gate on `MPU_CTRL.ENABLE=1` for zero-cost when disabled.
- **Interaction:** Connects to dead-code finding D1 (`Fault::MemManage` variant currently has only test constructors — keep it; it'll come back when this lands).

**B4. `step_pair_arm` exclusive-monitor invalidation is over-eager (Serial mode)** *[devil's-advocate reframing accepted]*

- **Where:** `crates/mdrp2350/src/lib.rs:1456-1460`
- **What:** `did_write_this_quantum` is set on **any** data write by either core. A peer LDREX is invalidated even if the write hit a different word. ARMv8-M §A3.4 specifies word-granular (or exclusivity-granule) invalidation. Liveness is preserved (LDREX/STREX retry loops are well-defined), but tight LDREX/STREX cycle counts diverge from silicon.
- **Reframing:** The wrongness report called this "leftover/redundant in favour of `ExclusiveMonitors::snoop`." The devil's advocate verified that `monitors.snoop` is **only** wired on the threaded path (`crates/mdrp2350/src/threaded/bus.rs`); serial `bus/mod.rs` never calls it. The mechanism here IS the only Serial-mode cross-core invalidator — just wrong granularity.
- **Caller exposure today:** No silicon oracle exercises cross-core LDREX/STREX competing-write — verified by grep over `cycle_cases.rs`, `dualcore_cases.rs`, `dual_model.rs`. So this is silicon-untested in either direction.
- **Fix:** Either wire `ExclusiveMonitors::snoop` into Serial mode, or replace `did_write_this_quantum` with per-core "wrote-to-word X" tracking. Larger lift than the discovery suggests.

### 2.3 Tracker-visibility gaps

**T1. 22 silicon-validated narrow-access tests are `#[ignore]`d, untracked outside the source** *[devil's-advocate UPGRADED to top-3]*

- **Where:** `crates/mdrp2350/src/tests_narrow.rs:311-1243` — 22 markers `#[ignore = "narrow-audit Stage 2/3/4 not applied (reverse-merge tech debt)"]`.
- **What:** Each test pins a silicon-correct narrow-access behaviour the production code does NOT honour today (NVIC ISER/ISPR/ICPR W1C through byte/half writes, SIO interp, DMA INTR side effects, SCB CFSR W1C, etc.). The Bus Narrow-Access Audit HLD (`wrk_docs/2026.04.17 - HLD - Bus Narrow-Access Audit.md`) deferred Stages 2/3/4 production fixes "as reverse-merge tech debt."
- **Why this is the highest-leverage tracker finding:** `tech_debt.md:1635-1647` mentions only the two `s65_io_bank0_intr*` W1C tests. The 20 other tests are invisible to every audit (including the 2026-04-26 sweep audit and 2026-04-29 testing audit). The reason-string was the only place they were tracked.
- **Fix:** One paragraph in `tech_debt.md` listing each test's wait-condition, or a scheduled Stage 2/3/4 work item. **Not a code fix.**

**T2. RP2040 DMA flag-bit `#[allow(dead_code)]` mirror not tracked** *[verified]*

- **Where:** `crates/mdrp2040/src/dma.rs:73,99,115,118` — `CTRL_HIGH_PRIORITY`, `CTRL_BSWAP`, `CTRL_SNIFF_EN`, `CH_DBG_TCR_OFFSET`.
- **What:** `tech_debt.md:1352-1359` covers the equivalent RP2350 entry but not the RP2040 mirror. Same regression-shape (the BUSY-bit-position assertion test that the existing entry prescribes for RP2350 has no RP2040 equivalent).
- **Fix:** Single line under the existing tracker entry: "Same pattern in `mdrp2040/src/dma.rs:73,99,115,118`."

**T3. OneROM serving oracle has explicit O(N²) TODO that test_silicon now exercises** *[verified]*

- **Where:** `crates/mdpicoem-harness/src/onerom_serving_oracle.rs:886-903`
- **What:** TODO comment notes `evaluate_case_trace` runs from scratch every tick. Acceptable at G.1 N=60 single-case scale; the 15-case sweep is now part of `test_silicon`. The conditional ("if the 15-case sweep shows measurable cost") is currently surviving in source-only, will be lost on turnover.
- **Fix:** Track in `tech_debt.md`, or do the streaming-evaluator refactor described in the TODO (~half a day).

### 2.4 Reframed findings (where the original framing was off)

**R1. `pub memory: Memory` on `Bus` — cache-invalidation hazard real, encapsulation framing wrong** *[devil's-advocate dissent recorded]*

- **Original tech-debt claim (H3):** Same shape as the existing Emulator pub-fields entry; recommend `pub(crate)` migration.
- **Dissent:** `Memory`'s own fields (`rom`, `sram`, `xip`) are **already private** at `crates/mdpicoem-common/src/memory.rs:9-11`. The hazard is not pub-field exposure; it's that `Memory`'s mutator surface (`load_rom`, `load_flash`, `sram_write*`, `xip_*`) does not notify `Bus::pending_invalidation_regions`. Making `bus.memory` `pub(crate)` does NOT fix it.
- **Reframed finding:** `Memory` mutator API needs an invalidation hook (callback or post-mutation invalidation routine on `Bus`). Severity medium, not high.

**R2. PPB writes through `Bus` directly — protection already exists** *[devil's-advocate falsified]*

- **Original tech-debt claim (H6):** No `debug_assert!` for PPB analogous to the SIO one. The `#[ignore]`d tests at `tests.rs:10520, 10570` are silently writing to placeholders.
- **Dissent:** A PPB-targeting `debug_assert!` already exists at `crates/mdrp2350/src/bus/mod.rs:2915-2921`:
  ```rust
  debug_assert!(addr >> 28 != 0xE || Self::is_boot_ram(addr) || Self::is_coresight_trace(addr),
                "PPB/coresight-aperture-bypass: address 0x{:08X} reached Bus::write32 — use CortexM33::bus_write32 wrapper", addr);
  ```
- **Reframed action:** The two ignored tests trip *that* assert. Update them to use `Emulator::mmio_write32` (which routes correctly per `lib.rs:1346-1364`). Severity drops from high to low — test cleanup, not missing protection.

**R3. Cycle costs scattered inline — feasibility worse than original report admits** *[devil's-advocate downgraded]*

- **Original tech-debt claim (H4):** Cycle costs hardcoded across ~6k LOC of executor; extract a `core::cycles::InstructionCost` table. Blocks all future cycle-fidelity work.
- **Dissent:** Sampled `crates/mdrp2350/src/core/execute_thumb32.rs:660-670`. Bus-state-dependent cycle costs are pervasive, not exceptional: LDR-immediate has `if rt == 15 { return 5 }` for PC loads; EXC_RETURN returns `self.exit_exception(val, bus)` (variable). The "80% case (single fixed-cost instructions)" framing under-sells how often "single fixed cost" is actually conditional. Track B Cycle Oracle Fidelity HLD declined the work for "too wide blast radius" — the centralized table doesn't change that radius; it relocates it.
- **Reframed:** Severity high → low. Don't extract until a use case drives it.

### 2.5 Genuine dead code

**D1. `Fault::MemManage` variant only constructed by tests** *[verified, but DO NOT delete — tied to B3]*

- **Where:** `crates/mdrp2350/src/core/mod.rs:188-202`
- **Cross-finding interaction:** B3 (MPU enforcement) is exactly what re-introduces this constructor. Don't delete the variant — document it under B3 in `tech_debt.md`.

**D2. RP2040 `Fault` enum file-level `#[allow(dead_code)]` is stale**

- **Where:** `crates/mdrp2040/src/core/mod.rs:36`
- **Action:** Delete the annotation. All six variants have ≥3 live constructors.

**D3. RP2040 `Ppb::exception_priority` annotation "Phase 5 NVIC wiring" — Phase 5 has landed**

- **Where:** `crates/mdrp2040/src/bus/ppb.rs:73`
- **Action:** Delete the annotation.

**D4. Hazard3 `mip` / `set_mip` / `mie` annotated "Exposed for P4" — P4 has landed**

- **Where:** `crates/mdrp2350/src/core_riscv/mod.rs:358-375`
- **Action:** Delete three annotations.

**D5. RP2040 `WorkerBus::is_pure` and the `FLAG_PURE` setter `classify_is_pure`**

- **Where:** `crates/mdrp2040/src/bus/mod.rs:262-265` (accessor), `crates/mdrp2040/src/core/decode.rs:228` (setter)
- **What:** "Wired into iter7 fast-path skip" per the comment. Zero readers; the iter7 skip doesn't exist in the tree.
- **Action:** Delete the bit, the setter, and the accessor. Or, if iter7 is still planned, file a `tech_debt.md` entry pointing at the HLD section.

**D6. `Bus::sram_bank_wait` (RP2350) — bank-contention model retired**

- **Where:** `crates/mdrp2350/src/bus/mod.rs:3220-3239`
- **Action:** Delete. Comment says "no longer called from data-access paths"; verified zero callers.

**D7. `Ppb::set_irq_pending` / `clear_irq_pending` / `irq_enabled` (RP2350)**

- **Where:** `crates/mdrp2350/src/bus/ppb.rs:697-738, 754-761`
- **What:** Each helper has only its own self-test as a caller. Production paths use `merge_irq_pending`, direct `nvic_ispr` writes, and `any_pending_enabled` respectively.
- **Action:** Delete the three helpers and the four self-tests. Removes name collision with `CoreAtomics::set_irq_pending` (the production version) at `threaded/atomics.rs:283`.

**D8. `Fifo::len` in `mdpicoem-common`**

- **Where:** `crates/mdpicoem-common/src/fifo.rs:55-58`
- **What:** Public accessor with no caller workspace-wide. `Fifo`'s only consumer (the inter-core 8-deep FIFO in mdrp2350 SIO) doesn't call `len()`.
- **Action:** Delete.

**D9. Half-built scaffolding for unimplemented stages (require Arthur sign-off, not safe to delete)**

The following exist as forward-scaffolding for stages that haven't started or are mid-flight:
- `mdrp2350::threaded::PioBus` (`crates/mdrp2350/src/threaded/bus.rs:81-102`) — Stage 7 PIO worker not yet implemented; only constructor caller is its own roundtrip test.
- `WorkerBus::shared` (RP2040) (`crates/mdrp2040/src/threaded/bus.rs:151-154`) — Stage 3b.4 in flight.
- `WorkerBus::clock_tree` (RP2040) (`crates/mdrp2040/src/threaded/bus.rs:996-999`) — Stage 3b.4 coordinator gap.
- `ThreadedEmulator::with_thread_mask` (RP2040) (`crates/mdrp2040/src/threaded/emulator.rs:309-313`) — likely intended for `paced_bench_rp2040` but never wired.
- `WorkerBus::core_id` (RP2350) (`crates/mdrp2350/src/threaded/bus.rs:170-174`) — accessor only used by self-test.

**Action:** Verify with stage owners; either wire the consumer, narrow the annotation, or delete + re-introduce when the stage starts. Group decision, not individual deletes.

### 2.6 Duplicate code (genuine refactor candidates)

**Three high-leverage receivers, ranked by post-dissent verdict:**

#### Du1. `paced_bench_rp2350` ↔ `paced_bench_rp2040` — DO

- **Pattern:** ~400-500 LOC byte-identical: `Stats`/`pct_idx`/`print_stats_row`, CLI parsing (`parse_arg`/`parse_arg_u64`/`parse_workload`/`parse_model`/`match_model`), A/B harness driver (`run_ab_harness`/`run_single_model_reps`/`collect_reps`), Win32 `boost_and_pin` shim. Third copy of `boost_and_pin` in `threaded_probe.rs:24-50`.
- **Locations:** `crates/mdpicoem-harness/src/bin/paced_bench_rp2350.rs:566-619, 1246-1497, 1348-1450` ↔ `paced_bench_rp2040.rs:329-382, 765-848, 850-997`
- **Receiver:** New `crates/mdpicoem-harness/src/bench/` module under the existing harness lib. Generics on a `Workload: Copy + WorkloadEnum` trait.
- **Risk:** Workload enums diverge (RP2350 has `FpuHeavy`/`OneromShape`; RP2040 doesn't). Manageable.
- **Verdict:** **DO.** Contributors actively read these files; ~400 LOC reduction; chip-divergence risk small because the divergence is workload-shape, not orchestration-shape.

#### Du2. Threaded-runtime helpers (`spawn_worker`, `pin_to_host_core`, `panic_message`) into `mdpicoem-common::threaded` — DO

- **Pattern:** ~120 LOC byte-identical between chips. No chip dependency.
- **Locations:** `crates/mdrp2350/src/threaded/emulator.rs:705-822` ↔ `crates/mdrp2040/src/threaded/emulator.rs:508-588`
- **Receiver:** `crates/mdpicoem-common/src/threaded/`.
- **Risk:** Trivial. Both crates already depend on this module.
- **Verdict:** **DO.** Free win, no abstraction cost.

#### Du3. TUI demo apps unification — DON'T *[devil's-advocate flagged as negative-value]*

- **Original duplicates report:** ~480 LOC reducible; introduce `mdpicoem-app` library with `ChipBackend` trait.
- **Dissent:** The apps are 99-LOC ratatui showcase demos. `ui.rs` differs by one line; `sim.rs` by ~20. But chip surfaces will diverge — RP2350 needs FPU/secure panels, RP2040 will eventually need PSRAM panel, OneROM rig has different display targets. A new workspace member for ~500 LOC of straight-line ratatui boilerplate is **negative-value abstraction**: each future chip-specific panel violates the trait. Per CLAUDE.md "Don't predict the future. Premature abstractions … actively resist the correct design."
- **Verdict:** **DON'T.** Revisit only if app UI exceeds ~1500 LOC per chip.

#### Du4. Cortex-M flag/condition-code accessors — DON'T *[devil's-advocate dissent]*

- **Original duplicates report:** ~150-200 LOC of `xpsr` flag bits, `set_nz`/`set_nzcv`, SP banking, `condition_passed` — byte-identical. Pull into `mdpicoem-common::cortex_m`.
- **Dissent:** M33 `Registers` carries FPU + secure-banked + stack-limit fields that M0+ doesn't have. The two ISAs are at most 95% similar today and **certain to diverge further** as TrustZone / FPU / DSP coverage lands on RP2350. The agent's caveat ("inlining might stop across crate boundaries") is moot under the workspace's `lto = "fat"` + `#[inline(always)]` (verified at `Cargo.toml:27-30`).
- **Verdict:** **DON'T.** Cross-ISA divergence is increasing, not decreasing.

#### Du5. PLL register helpers — WAIT *[devil's-advocate dissent]*

- **Original:** `pll_reg_index`/`pll_read`/`pll_write` duplicated across `crates/mdrp2350/src/bus/peripherals.rs:7-46`, `crates/mdrp2350/src/threaded/peripherals.rs:320-360`, `crates/mdrp2040/src/bus/clocks.rs:424-470`. ~80 LOC dedupable into `mdpicoem-common::clocks`.
- **Dissent:** In-source comment at `crates/mdrp2350/src/threaded/peripherals.rs:332-336` explicitly says "Duplicated by design, ~6 LOC … Stage 5 can revisit if the duplication becomes a maintenance burden." Arthur made an explicit decision; the report overrides without showing the maintenance burden surfacing.
- **Verdict:** **WAIT.** Respect the in-source decision until it actually bites.

#### Du6. Lower-priority duplicates (track for later)

- OneROM bootrom-bypass boilerplate (~150 LOC across 6 binaries — `mdpicoem-harness::onerom::boot_emulator(fixture_path) -> Emulator`).
- Ctrl-C `SHUTDOWN: AtomicBool` boilerplate (`qemu_diff_*` × 3) — `mdpicoem-harness::cli::install_shutdown_hook()`.
- `probe_diff_rp2350` ↔ `probe_diff_rp2040` argument parser (~80 LOC).
- `silicon_isr_diff_*` orchestrator main (~50 LOC marginal).
- `isr_scenarios.rs` (3163) ↔ `isr_scenarios_rp2040.rs` (1386) — ~200 LOC of body duplication, but the ISA-divergence argument applies (don't unify the per-scenario emu helpers).

### 2.7 Discipline / hygiene findings

**H1. CLAUDE.md drift — `mdpicoem-devices` "Version-pinned 0.7.x" but Cargo.toml is 0.1.0**

- **Where:** `CLAUDE.md:97` vs `crates/mdpicoem-devices/Cargo.toml:6`
- **Action:** One-line edit.

**H2. `qemu_diff_m33.rs` hardcodes `localhost:3333`; siblings use `profile.gdb_addr()`**

- **Where:** `crates/mdpicoem-harness/src/bin/qemu_diff_m33.rs:170, 414`
- **Sibling pattern:** `qemu_diff_m0plus.rs:150`, `qemu_diff_riscv32.rs:276` use `profile.gdb_addr()`.
- **Action:** Replace literals with `&QemuProfile::M33_RP2350.gdb_addr()`.

**H3. RP2040 `Bus::dma` is `pub`; RP2350 sibling is `pub(crate)`**

- **Where:** `crates/mdrp2040/src/bus/mod.rs:389` vs `crates/mdrp2350/src/bus/mod.rs:340`
- **Action:** Tighten RP2040 to `pub(crate)`. External callers should use `tick_dma`/MMIO. Consistency with RP2350.

**H4. `unsafe` blocks lack SAFETY comments in threaded-affinity FFI paths**

- **Where:** `crates/mdrp2350/src/threaded/emulator.rs:776-797`, `crates/mdrp2040/src/threaded/emulator.rs:561-580`. Precedent: `gdb_client.rs:235, 252`.
- **Action:** Add `// SAFETY:` blocks in the gdb_client.rs style. Project convention.

**H5. RP2040 watchdog `CYCLES_RESET = 12` post-init, not silicon zero**

- **Where:** `crates/mdrp2040/src/peripherals/watchdog_tick.rs:48-55`
- **What:** Documented in source as intentional, but not in `tech_debt.md`. Firmware sniffing post-reset CYCLES sees 12 on emu, 0 on silicon.
- **Action:** Add to `tech_debt.md` under "RP2040 silicon divergence" or reset to 0 + update `hello_timer` corpus.

**H6. Stale `#[allow(dead_code)]` on RP2040 `Fault` enum and Hazard3 `mip`/`mie`** — see D2, D4 above.

**H7. Worker poison panic in production threading path** *[medium severity]*

- **Where:** `crates/mdrp2350/src/threaded/emulator.rs:480-483`
- **What:** Production `panic!` in the join-handle Send/Sync-bridge context. The sticky-after-panic contract is great, but the *display* is `panic!` rather than `tracing::error!` + `EmulatorError::WorkerPanicked` early-return.
- **Action:** Convert to typed error; the `panic_info` field at `lib.rs:360` and `EmulatorError::WorkerPanicked` at `lib.rs:976` already exist.

**H8. Single `unsafe` simplification in pacer**

- **Where:** `crates/mdpicoem-common/src/pacer.rs:160`
- **What:** Recent Rust marks `std::arch::x86_64::__cpuid` as safe; the `unsafe { ... }` block is now redundant. Stable build doesn't warn yet, nightly does.
- **Action:** Drop the `unsafe { }`; keep the SAFETY comment as a regular code comment.

### 2.8 Documentation drift (low severity)

**Doc1. 12 versioned HLDs (`...V1.md` through `V12.md`) without supersession markers**

- **Where:** `wrk_docs/2026.04.17 - HLD - RP2350 Coverage Gap Fill V*.md`
- **Action:** One-time `mv {V1..V11}.md archive/` pass.

**Doc2. 1 `FIXME(trustzone)` marker in entire emulator codebase** *[kudos, not action]*

- Already in `tech_debt.md:1588`. Codebase discipline on FIXME-vs-tracker promotion is excellent.

---

## 3. Cross-cutting themes

### Theme A — `pub`-field encapsulation footgun *[scope reduced after dissent]*

The existing `tech_debt.md:45-89` entry covers `Emulator.{cores,bus,clock}`. The tech-debt scout extended the pattern to `Bus.memory`, `Bus.dma` (RP2040), and `Registers`. The devil's advocate falsified the `Bus.memory` framing (Memory's fields are private — see R1 above). What remains:

- **Real:** `Emulator` direct-field access (already tracked).
- **Real:** `Memory` mutator API needs invalidation hook (reframed R1).
- **Real:** RP2040 `Bus::dma` pub asymmetry (H3).
- **Real:** `Registers.{r,xpsr,...}` direct mutation bypassing setters (latent, not currently bitten).

These should be addressed as **one migration**, not three. Per devil's advocate cross-finding interaction: ~2000+ call-site renaming pass; piecemeal stacks rewrites. Either commit to a single refactor that handles all four, or hold all four.

### Theme B — Half-modelled architectural state

B3 (MPU not enforced) interacts with the existing TrustZone S-bit FIXME at `exceptions.rs:191`, the FPSCR.AHP TODO at `execute_fpu.rs:1414`, and the RP2040 NVIC plumbing tracked in `tech_debt.md`. None surface as hard errors; firmware that hits any combination silently produces wrong results. Suggest adding a "Half-modelled architectural state" section in `tech_debt.md` with a checkbox-shape inventory:

| State machine | Stored | Enforced | Differential-validated |
|---|---|---|---|
| MPU regions | ✅ | ❌ | ❌ |
| TrustZone S-bit | partial | partial | ❌ |
| FPSCR.AHP | ✅ | ❌ | ❌ |
| RP2040 NVIC dispatch | partial | partial | ❌ (silicon_isr_diff_rp2040 exists but FAILs by design) |

### Theme C — `debug_assert!` on user-input paths

B2 is the worst case (zero quantum loops in release). Other `debug_assert!` sites sampled (`bus/mod.rs:1620` SIO, `lib.rs:693, 1502` placeholder guards) are reachable from internal paths only and correct. Worth a project-wide policy line in CLAUDE.md: *"`debug_assert!` is for invariants the type system cannot encode; user input on a public method gets a typed `Result`."*

### Theme D — Silicon-validated peripheral coverage gaps not in `tech_debt.md`

The 2026-04-29 Testing Audit lists 16 RP2350 peripherals never differentially validated against silicon. `tech_debt.md` tracks the silicon-FAIL'd cases but not silicon-NEVER-RUN ones. A "Peripherals not silicon-verified" tracker subsection would close the loop. Out of scope for this sweep but worth noting.

---

## 4. Cross-finding interactions (DO NOT combine)

| Combination | Why dangerous |
|---|---|
| Theme A migration + R1 (Memory invalidation) + existing tech_debt.md migration | All three rewrite ~2000 call sites. Piecemeal forces three rewrites stacked. Either single big-bang refactor or hold. |
| R3 (cycle table) + B4 (LDREX granularity) | A naive per-instruction cycle table that doesn't model bus-state-dependent costs silently regresses LDR-PC pipeline-flush, EXC_RETURN, LDM N — all silicon-validated. |
| Du1 (paced_bench unification) + Du3 (TUI app unification) | Both add workspace structure. Du3 is on the don't-do list anyway, but flagged to avoid sequencing them together if anyone reconsiders. |
| D1 (delete `Fault::MemManage`) + B3 (wire MPU) | Contradictory — A1 says delete the variant, B3 reintroduces it. Keep the variant; document under B3 in `tech_debt.md`. |

---

## 5. Things the four investigators ruled out (search transparency)

These are documented so that future sweeps don't re-investigate.

- **`mdpicoem-debug` crate** — intentional Phase-4+ scaffolding placeholder.
- **One-shot probe binaries** (`smoke_per_core_cyccnt_rp2350`, `smoke_powman_pacing_rp2350`, `probe_csrrw_riscv32`) — header-marked throwaway; team's call.
- **DMA "Not in V1" constants** — datasheet-fidelity, doc-commented to HLD sections.
- **Documented bit-layout constants** in I2C/UART/SysTick — intentional documentation.
- **`Peripheral` trait** (CLAUDE.md mentions removed) — confirmed fully removed; no orphans.
- **`epio-sys` excluded from `default-members`** — wiring intact; only consumer is its own internal binary.
- **`mdpicoem-devices`** — all three modules have external consumers; clean.
- **Bank-contention vars on RP2040** — referenced live; "deprecated-in-place" but not removable without bus refactor.
- **Harness binaries documented as silicon-only** — not flagged per scope guardrail.
- **Stage 4/5 residue test modules** — coverage-completeness `#[cfg(test)]` modules, not dead.
- **`thumb32_gen` and `riscv_gen`** — large generators (8k + 3.6k LOC) but each function wired into oracles.
- **`execute_thumb32.rs` file-level `#![allow(dead_code)]`** — kept because some helpers are exercised only by tests in `tests.rs`.
- **No `_v1`/`_v2`/`_old`/`_legacy`/`deprecated`/`wip` filenames found.**
- **No `unimplemented!()` / `todo!()` / `panic!("not implemented")` in production code.**
- **Memory `off + 1 < N` boundary checks** — verified correct for 2/4-byte reads.
- **M0+ PUSH `for i in 0..15`** — correct (Rust ranges exclude upper bound; bit 14 = LR is processed).
- **SPSC queue Relaxed/Release ordering** — correct under C++/Rust memory model, not just TSO.
- **EXC_RETURN classifier** (RP2040) — verified against ARMv6-M ARM §B1.5.8.
- **DMA `apply_alias` `_` arm fall-through** — unreachable under 2-bit alias mask; safe default.
- **QEMU child cleanup on Windows** — defence-in-depth: cooperative `Drop::drop` + JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE; field-drop ordering is correct.
- **`picogus_diff_rp2040::parse_trace`** — magic, header, monotonic timestamp, port range, value-width all checked with line-numbered errors. Solid.

---

## 6. Things this sweep MISSED (devil's-advocate blind-spot findings)

These are **process** findings — gaps the four investigators didn't even look at, surfaced by the devil's advocate.

### M1. Mutation-testing v2 results never consulted *[highest-leverage miss]*

`mutation/v2/results.jsonl` has 800 entries; **470 are `oracle_survived`** (verified by tally). Surviving mutants are the canonical "where do tests miss?" signal. Not one of the four reports references it. Worse: an HLD already exists at `wrk_docs/2026.04.29 - HLD - Mutation Testing V2 Triage.md` with the triage plan. The next pass should consult that HLD and act on the surviving-mutant list before generating new sweeps.

### M2. No CI configuration in repository

No `.github/workflows`, no `.gitlab-ci.yml`, no `Jenkinsfile`. The codebase has differential oracles, mutation testing, soak harnesses — none of it runs automatically. Structural gap.

### M3. `probe-rs` patched fork untracked

`Cargo.toml:52-53` has `[patch.crates-io]` pointing to `third_party/probe-rs-0.31.0-mdrp-patched`. Worth one entry in `tech_debt.md` tracking the de-fork condition.

### M4. `epio-sys` exclusion fragility

Documented in CLAUDE.md but not tracked. Workspace member needs `clang` + vendored submodules to build, has one consumer (`onerom_trace_gen_rp2350`), nothing in CI exercises it.

### M5. Wrongness investigator did not read 2026-04-29 Testing Audit

The wrongness report's "recommended next pass" item #2 (RP2040 threaded peripherals legacy HashMap path) is already documented in `tech_debt.md` and the testing audit. Confirms a process miss — the investigator was instructed to read `tech_debt.md` first but not the same-date testing audit. Future sweeps should read all `wrk_docs/<today>` artefacts as well.

### M6. Mutation Testing V2 Triage HLD pre-exists this sweep

`wrk_docs/2026.04.29 - HLD - Mutation Testing V2 Triage.md` was authored the same day as this sweep. None of the four reports consulted it. The triage HLD is a structurally more important signal than any single dead-code or duplicate finding. Read-before-act.

---

## 7. Summary tally

| Category | Count |
|---|---|
| Real bugs (B1) | 1 |
| Latent footguns (B2-B4) | 3 |
| Tracker-visibility gaps (T1-T3) | 3 |
| Reframed findings (R1-R3) | 3 |
| Dead-code deletions (D2-D8) | 7 + 1 retain (D1) |
| Forward-scaffolding to verify (D9) | 5 items |
| Duplicate-code DOs (Du1, Du2) | 2 |
| Duplicate-code DON'Ts (Du3, Du4) | 2 |
| Duplicate-code WAITs (Du5) | 1 |
| Duplicate-code lower-priority (Du6) | 5 |
| Hygiene findings (H1-H8) | 8 |
| Doc drift (Doc1-Doc2) | 2 |
| Cross-cutting themes | 4 |
| Process blind spots (M1-M6) | 6 |

**Net new items for `tech_debt.md`:** approximately 12 (the load-bearing ones — B3, B4, T1, T2, T3, R1, H5, H7, plus three from the half-modelled architectural state theme, plus M3).

**Things to act on now:** B1 (one-line panic fix), the 7-item dead-code cleanup batch (D2-D8), H1/H2/H3/H8 (one-line hygiene fixes). Roughly half a day of focused work.

**Things to add to tracker but not act on:** B3 (MPU), B4 (LDREX), T1 (22 narrow-audit tests), T2 (RP2040 DMA mirror), T3 (O(N²) TODO), R1 (Memory invalidation hook), R3 (cycle table — if ever), Theme B inventory, M3 (probe-rs fork).

**Things explicitly declined:** Du3 (TUI app unification), Du4 (Cortex-M flag accessors), R3 (cycle-table extraction without a use case), Du5 (PLL helpers — respect the in-source decision).

The action plan in `wrk_docs/2026.04.29 - Codebase Tech-Debt Sweep Action Plan.md` translates these into sequenced work items.
